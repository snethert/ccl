# Current status — 2026-09-19

Implementation baseline: upstream v1.13, `c994217adc56b3f8a564526cee4695893ac84d86`. Current document set: outline 0.17, acceptance 1.7, decisions 1.8. macOS is the sole reference host. The reviewed Stage 1A compiler registration unit is integrated; upstream kernel source remains unchanged. Stage 0 is accepted. Under the user's 16 September authorization, Codex authors Stage 1 in disposable clean U1 copies; Claude reviews before integration. All work is on `wasm2`.

| Work | State | Evidence scope / remaining work |
| --- | --- | --- |
| Claude's non-census branch | MERGED; six Stage 0 records ACCEPTED | [Codex R3 review](stage0/codex-review.md#r3-follow-up--16-september-2026) closes both findings. The user's conditional acceptance is recorded in [project acceptance](stage0/project-acceptance.md). Float policy and Stage 1 adoption remain separate decisions. |
| D6 floating-point policy | DECIDED by the user 16 Sep: the ARM model | [Decision record](decisions.md#16-september-2026--d6-floating-point-policy-the-arm-model) and [policy section](contracts/floating-point.v1.md): logical enable mask in the TCR, checks emitted under float safety, ARM priority, signalling comparisons; deviations recorded (nearest rounding only; no underflow/inexact from host mathematics). Executed in FLOAT-DETECTION-R4 and TCR-SCHEMA-R3, awaiting Codex review. |
| Earlier registry/body analyses | HISTORICAL, UNQUALIFIED | [Provenance correction](stage0/registry-history.md): original artifacts retained; unsupported all-body completion claim withdrawn. Current startup uses the separately retained 167-unit run; no LL15 credit. |
| On-demand startup provenance | REVIEWED; no defect found | [Startup replay](stage0/on-demand-census.md): three stages replayed from retained inputs; unchanged startup worklist combined with the reviewed finite-chain base; Claude audit 58 closes the provenance defect. Original defective combined graph retained as superseded history. No gate credit. |
| Finite-callee replay | REVIEWED; Claude fifty-seventh audit, no defect | [One committed resolver version](stage0/finite-callees.md): fresh 98 + 19 + 2 + 5 partition leaves 1,438 of 1,562 expressions open. All stage outputs and sources retained; cell values and lowerings remain unqualified. |
| Builtin slot diagnostic | REVIEWED; Claude fifty-seventh audit, no defect | [Eight builtin obligations](stage0/builtin-slots.md): 1,500 original call sites consolidated, 32 controls rejected. Original packet and executed sources retained separately; no target lowering or callee-bound credit. |
| Integrated development census | BLOCKED by design; reviewed by Claude's fifty-fifth audit, no defect, no gate credit | [Working closure](stage0/closure.md): one command reconstructs the reviewed graph and adds revision-2 seeds in a separate namespace. 369,706 nodes / 1,089,937 edges; 95 unwitnessed symbols, 5,007 original body gaps and 1,562 open computed calls. Every earlier graph record remains unchanged. LL15-b/c qualification, execution bridges and widening replacement remain open. |
| Unwitnessed binding definitions | BLOCKED by design; reviewed by Claude's fifty-fifth audit, no defect, no gate credit | [Integrated batch](stage0/binding-definitions.md): all 95 classified; 80 exact compiler-name and five accessor-declaration joins, three SETF inverse gaps and seven source-only candidates. The latest working graph has 369,710 nodes / 1,090,024 edges, with 87 compile-phase edges added and every earlier record preserved. No original runtime value or bound obligation closes. |
| U1 native Gate 0 | ACCEPTED for same-host execution/repeatability | Two clean macOS x86-64 builds, 21,843 passing eligible tests per build, 75 upstream-disabled tests disclosed, 164 identical FASLs. Claude independently reproduced the results. Second-Mac reproduction is [no longer required](stage0/second-mac-decision.md). [Native summary](evidence/native-baseline-summary.json). |
| Boundary S0-LL13-c / S0-LL19-b | ACCEPTED at retained bounds | Twelve cases and nine controls, real C stacks, TLS and exceptional restoration. |
| Integrated S0-LL20-a/b/c | ACCEPTED at retained bounds | Cons-only moving collector, admission, lifecycle and interruptible I/O; 22 cases, 14 controls, 1,000 seeded schedules. [Runtime scope](stage0/integrated-runtime.md). |
| Debugger frames S0-LL23-b | ACCEPTED at retained bounds | Eleven cases and eleven controls. Shared-cell relocation does not establish capture semantics; policy-1 unavailability is metadata only. [Frame scope](stage0/debug-frames.md). |
| Seven-record project acceptance | RECORDED | User-authorized [decision](stage0/project-acceptance.md) following Claude sign-off 52639e4e. Separate envelope preserves original bytes, timestamps, hashes and scopes. Original raw envelopes retain their earlier review flags. |
| C/C4/B dynamic-call corpus | ACCEPTED at reviewed bounds | 53 positive cases and 27 rejected controls per candidate; 159/81 total. Includes three 100,000-tail-transfer cases per candidate. All eighteen records are accepted after Claude's fourth audit. [Report and bounds](stage0/dynamic-call.md). |
| D3 / product risks | B CHOSEN; S0-ABI-selection ACCEPTED | [Decision record](stage0/abi-decision.md): simplicity, explicit argument/result ownership, 24 accepted prerequisite variants, provisional packaging and continuing obligations. Accepted after Claude's twenty-sixth audit and the user's explicit decision; no runtime or timing execution. Generated-code confirmation and product measurements remain open. |
| Native observation / qualified census | S0-LL08-b ACCEPTED at stated scope; census still open | [Reversible harness](stage0/native-census.md) and [dependency extension](stage0/native-dependencies.md) retain native compilation, startup and L0/L1/L2 binding observations. Claude's eighth audit reviewed the [dyld classification](evidence/native-loader-contexts.json) and [candidate preparation](stage0/startup-candidates.md). All pathname contexts are classified. The [revised proposal](stage0/startup-seed-revision.md) resolves 22 named entrypoints and 49 distinct root prototypes through the kernel vectors and three applicable methods; Claude's twentieth audit approved revision 2 for the native profile. Full call bounds, the widening replacement and LL15-b/c qualification remain open. |
| Census stub registration | S0-LL08-a ACCEPTED at stated scope | [Reversible registration proof](stage0/stub-registration.md), accepted at 02ff713d: two fresh target sessions pass 28 source cases; native suites pass 21,843 tests each; 163/164 FASLs match while registered and 164/164 after removal. All formal LL15 prerequisites are accepted. Full source traversal beyond the fourteen-form fixture remains open. |
| First source traversal | EXECUTED; reviewed by Claude's twenty-first audit, no defect | [Dumplisp traversal](stage0/source-traversal.md) accounts for all 16 top-level forms in the registered Wasm target context. Four definitions and two initializer bodies yield 12 code prototypes, 40 call sites and nine function references. Eight definitions retain explicit descriptor/interface gaps. Two identical native runs, two rejected native controls and 21 analysis controls. No graph edges replaced or gate credit. |
| Startup target descriptions | EXECUTED; reviewed by Claude's twenty-second audit, no defect | [Description extension](stage0/target-descriptions.md) supplies eight D1-derived subtype values and a named batch-mode dependency. Seven definitions and four deferred initializers are captured, with 74 calls and 12 references. Five native-boundary gaps and eight explicit replacement obligations remain. Two identical native runs, six rejected native controls and 26 analysis controls. No graph replacement or gate credit. |
| Source-derived target macros | EXECUTED; reviewed by Claude's twenty-fourth audit, no defect | [Macro slice](stage0/target-macros.md): 14 macros rebuilt from eight U1 source forms; seven used in ten traversal expansions. Target-sensitive expansion and acode, local-shadow identity, four native and 20 checker controls pass. Another 46 expander identities remain unqualified. Seven definitions and five boundary stops are unchanged; no gate credit. |
| Source expansion bodies | EXECUTED; reviewed by Claude's twenty-fifth audit, no defect | [Complete observed routing](stage0/source-expanders.md): all 293 hook invocations use source-rebuilt bodies; inherited traversal bytes and expansion semantics agree. Sixty compiled functions, 68 native helper names, four computed helper calls and three object references are explicit. Four native and 27 checker controls reject. Full helper qualification, five source stops and LL15 closure remain open; no gate credit. |
| Target numeric/layout helpers | EXECUTED; reviewed by Claude's twenty-seventh audit, no defect | [Helper slice](stage0/target-helpers.md): private source bodies repair inherited host-width assumptions in EQL and supply stream/vector subtag lookup. Eight numeric probes check actual front-end calls; 40 helper calls join to 21 genuine expansion events. Four native and 15 checker controls reject; six file traversals agree byte for byte. Nonconstant type inference, remaining helpers/objects and five boundary stops stay open; no gate credit. |
| Declared-type equality helpers | EXECUTED; reviewed by Claude's twenty-ninth audit, no defect | [Type slice](stage0/target-types.md): source helper bodies plus compound target-type translation repair unsafe EQ decisions for declared boxed integers. Fifteen probes distinguish seven EQ cases from eight EQL cases and respect declaration policy; sixty queries join to the genuine file traversal. Four native and 17 checker controls reject, with all six traversals identical. General type inference and other helpers remain open; no gate credit. |
| Target TYPEP helpers | EXECUTED; reviewed by Claude's thirty-first audit, no defect | [Predicate slice](stage0/target-predicates.md): source optimizer and target membership repair inherited FIXNUM/BIGNUM constant folds. Twenty-two front-end probes, two ctype guard and two unchanged-call probes pass; five native and 22 checker controls reject. Ten genuine TYPEP events join 170 trace records. Native type-system/class objects remain dependencies; no gate credit. |
| Target DEFTYPE aliases | EXECUTED; reviewed by Claude's thirty-third audit, no defect | [Alias order slice](stage0/target-aliases.md): expansion before membership and canonicalization corrects ten constant and three optimizer probes. Sixteen front-end probes, five optimizer probes, two bounded refusals, five native and twenty checker controls pass. Arbitrary aliases and native objects remain open; no gate credit. |
| Native object serialization | EXECUTED; reviewed by Claude's thirty-fifth audit, no defect | [Object witness](stage0/target-objects.md): actual IOBLOCK class and registered RESTART cells serialize by symbolic key. Five native registry roundtrips, four negative inputs and sixteen checker controls pass; seven data FASLs reproduce. Target object materialization and remaining helpers stay open; no gate credit. |
| Target architecture dispatch | EXECUTED; reviewed by Claude's thirty-seventh audit, no defect | [Source lookup witness](stage0/target-dispatch.md): four private U1 bodies, two real architecture events, nine registry probes and unchanged-native comparisons. Four native and 25 checker controls reject; six file traversals agree byte-for-byte. Current binding selection and missing-target refusal witnessed; other helpers, five boundary stops and census closure remain open. No gate credit. |
| Target SETF lookup | EXECUTED; reviewed by Claude's forty-first audit, no defect | [Source SETF witness](stage0/target-setf.md): five actual lookups, four U1 named inverse declarations and zero callable-expander calls in dumplisp. Nine separate target-context probes agree with native SETF; five native and 25 checker controls reject. Wider helpers, boundary replacements and closure remain open; no gate credit. |
| Lexical callback bounds | EXECUTED; reviewed by Claude's forty-third audit, no defect | [DECLAIM/APPLY binding joins](stage0/lexical-callbacks.md): two computed expander calls resolve to exact local prototypes through three immutable bindings in actual compiler IR. Eight target-front-end probes, 21 rejected checker mutations and two identical native captures. Reassignment, captured writes and unknown parameters remain unresolved; no graph widening removed or gate credit. |
| Kernel resource identity | EXECUTED; reviewed by Claude's forty-fifth audit, no defect | [Explicit KERNEL-PATH replacement](stage0/kernel-resource.md): original native pointer gap retained; separate target-front-end capture has one declared service call. Fourteen native reference cases, eight rejected service mutants, nineteen checker controls and two identical normal captures. Wasm strings, roots, process storage, publication and target conditions remain unimplemented; full census graph unchanged. |
| Joined LL15 projection | ASSEMBLED, UNQUALIFIED | [Exchange graph and witnesses](stage0/joined-census.md): schema and input coverage pass; 23 specific omission controls and five parser controls reject. Retains 18,907 completed compile effects, 9,362 emitted load effects and 35 completed callbacks. Its original identity gaps are addressed by the richer collection below; final candidate bounds, effect order and target lowering/dispositions still require assembly. Reproduced byte-identically by Claude's tenth audit. No new gate credit. |
| Compiler/loader identity collection | EXECUTED; reviewed by Claude's tenth audit | [Reversible identity collector](stage0/rich-census.md) captures full native compilation, actual expander/installation/initializer identities and the exact traced image. Native tests pass 21,843 before/after; 156/164 FASLs match during observation, 164/164 after removal. These are LL15 assembly inputs, with no new acceptance. |
| Initializer identity integration | EXECUTED; reviewed by Claude's eleventh to thirteenth audits, no defect | [Exchange integration](stage0/initializer-joins.md) links 18,907 compile-time calls, 4,660 loader calls and 35 callbacks to code/value witnesses and per-process entry/return order. The checker compares complete added node records; twenty-four controls reject, including eleven former escapes. Graph, witnesses and verifier controls reproduced byte-identically by Claude; independent insertion and metadata probes all reject. All three initializer packets are marked reviewed in the evidence index. No new gate credit; full LL15 closure remains open. |
| Native emission joins | EXECUTED; reviewed by Claude's fourteenth and fifteenth audits, no defect | [Observed dependency fragment](stage0/native-emission-joins.md) joins 2,052,019 emissions, 51,342 front-end functions and 207 live handler identities. The correction adds 141 explicit joins to the image's evaluated operator slots; 32 controls reject, six operand-layout cases and the unknown-offset refusal case pass. Claude reproduced the fragment, the materialized graph and every count independently. Both emission packets are marked reviewed in the evidence index. Other operand classes and static coverage remain open; no new gate credit. |
| Boot-process observation | EXECUTED; reviewed by Claude, source join closed by the origin witness | [Reversible early-boot witness](stage0/boot-observation.md): 133 cold initializers, 121 completed loads, 8,326 completed loader initializer calls and 9,094 checked function-cell histories. All 8,327 recorded initializer opcodes join retained FASL bytes; native suites pass 21,843 before/after, with 161/164 FASLs identical during observation and 164/164 after removal. All 133 cold thunks lack runtime source notes; the separate xload origin witness below supplies their module provenance. LL15 closure remains open; no gate credit. |
| Cold initializer origins | EXECUTED; reviewed by Claude's seventeenth audit, no defect | [Xload insertion witness](stage0/cold-initializer-origins.md) joins all 133 cold thunks to 31 source modules; 131 have source contexts and two retain exact FASL offsets without ranges. Fresh boot reproduces the reviewed stream after archive-root relocation. All 164 FASLs remain unchanged; 20 analysis controls and two native controls pass. Claude re-executed the capture natively and reproduced every output; the U1 push, reversal and drain sites were confirmed in source. No new source patch or gate credit. |
| Boot graph integration | EXECUTED; reviewed by Claude's eighteenth audit, no defect | [Exchange fragment](stage0/boot-integration.md) preserves all 52,441 boot event boundaries, 133 cold origins and 9,094 function-cell histories. Thirty-three controls reject, and the full graph passes structural checks. Three active frames and two absent ranges stay explicit. The 1,059 formerly opaque cell values are decoded by the wrapper follow-up below. Claude re-executed the producer and materializer byte-identically and re-derived the fragment and its seed reachability independently. No gate credit. |
| Boot wrapper contents | EXECUTED; reviewed by Claude's nineteenth audit, no defect | [Wrapper export and graph update](stage0/boot-wrappers.md) resolves all 1,059 values: 1,028 macro wrappers and 31 special-operator wrappers. Original event bytes and IDs are preserved; three native and 26 analysis controls pass. The full graph reconstructs byte-for-byte; Claude re-executed the native exports and verifier byte-identically. No native rebuild or gate credit. |
| Source-wide census collection | EXECUTED diagnostic; reviewed by Claude's forty-seventh audit, no defect | [General file driver and call worklists](stage0/source-closure.md): all 164 native units complete; 103 target units complete, with failures and affected captures retained. 51,708 native function observations, 104,081 calls and 206 proven local callback bounds; 1,517 fresh computed sites and 257 non-pass-2 output functions remain unjoined. This does not replace r7 identities, qualify the wider target environment or close LL15-b/c. |
| Sequential-build IR integration | EXECUTED diagnostic; reviewed by Claude's forty-eighth audit, no defect | [Original build call/code joins](stage0/build-flow.md): 51,601 materialized identities, 103,393 calls and 200 local bounds; 1,562 computed calls and 259 assembly bypasses remain open. Exact global symbol descriptors retained; existing graph records preserved; eight callback probes and 22 controls pass. No census gate credit. |
| Sequential-build binding versions | EXECUTED diagnostic; reviewed by Claude's forty-ninth audit, no defect | [Binding histories and consolidated obligations](stage0/binding-versions.md): 98,213 global sites join to 5,693 exact symbols; 5,598 have value witnesses and 95 remain open. Earlier versions, wrapper roles and exact FASL generations retained. 22 controls and three positive probes pass; 1,562 computed calls unchanged. No candidate-bound or census gate credit. |
| Starting-image body witnesses | EXECUTED diagnostic; reviewed by Claude's fiftieth audit, no defect | [Read-only image bodies](stage0/resident-bodies.md): complete 14,718-function region anchored to the original inventory; exact native payloads for 4,373 open prototypes and 4,130 reported source ranges. 634 prototypes need other witnesses; all source/IR dependencies remain open. 24 controls pass. No census gate credit. |
| Remaining body origins | EXECUTED diagnostic; reviewed by Claude's fifty-first audit, no defect | [First-descriptor worklist](stage0/body-origins.md): 481 initially resident prototypes (16 installer-annotated, 465 without source annotations) and 153 later installations (152 ASDF, one Swink). All 4,006,405 retained events checked; eighteen controls pass. The user clarified that Swink and its native remote-lisp clients are omitted from the browser port; no Swink implementation is planned. No graph mutation or census gate credit. |
| Resident literal dependencies | EXECUTED diagnostic; reviewed by Claude's fifty-second audit, no defect | [Exact initial references](stage0/resident-literals.md): 465 unannotated prototypes join seven read-only targets and one nested function. 466 reference edges, six newly exposed code/body obligations and eight reported U1 source extents; 24 controls pass. All prior graph records preserved. No subtype, complete body, method bound or gate credit. |
| Native method registry | EXECUTED diagnostic; reviewed by Claude's fifty-third audit, no defect | [Fresh registry checkpoint](stage0/dispatch-registry.md): 581 initialized GFs, 1,514 methods and one uninitialized entry; twelve calls, eight stale checkpoints, four field mutations and 23 checker controls. Inspector-free reproduction confirms stale direct dcode after all methods are removed. Defect retained, upstream untouched; no original-build identity join, callee bound or gate credit. |
| Same-execution registry/compiler witness | EXECUTED diagnostic; reviewed by Claude's fifty-fourth audit, no defect | [Native source reload](stage0/registry-flow.md): 240 method additions across 118 GFs; 163 installation-to-IR joins by identity and code bytes, 77 explicit construction gaps. Four native sessions, equal reference method code, 24 rejected controls and exceptional hook restoration. No original-build identity join, exhaustive callee bound or gate credit. |
| Generated B compiler | AUTHORIZED; NOT RUN | Codex is the authorized author of the functional shared-compiler changes (user decision 16 Sep, standing rule amended in CLAUDE.md); Claude reviews. The implementation starts from pristine U1, with no observed image or patched source carried forward; R6/R6a on every existing target per change. |
| Engine matrix / full object contracts | INCOMPLETE | Node/V8 hand-built execution does not qualify browsers, all profiles, full CCL object layouts or generated code. |
| Initial probes | Reviewed diagnostics only | Three PASS in r7; cannot discharge complete S0 IDs. Original inventory hash remains provenance. |
| Evidence repository | Retained separately | Immutable catalog, original failures and index snapshots; [pinned repository manifest](evidence/repository.json). |
| D3 measurement derivative review | Reviewed; S0-LL21-a ACCEPTED by the user at retained bounds | [Fifth audit](stage0/claude-review.md): full source review, fresh 486/279/72/27 run, 4,518/4,518 byte-identical binaries, independent bootstrap; no defect. The prior acceptance was withdrawn after the user clarified that its quoted authorization was reviewer text. The reviewed scope remains identity/generation publication with pre-provisioned bytes and host-actor acquisition. Timings have no selection value. |
| C/C4/B dynamic-call fixture review | Reviewed and accepted | [Fourth audit](stage0/claude-review.md): full source review, fresh 159/81 run, 864/864 byte-identical binaries, no defect; three nonblocking observations recorded. The eighteen records have a separate scoped acceptance envelope. |
| Required-inventory control S0-LL02-a | ACCEPTED at reviewed scope | [Production-gate control](stage0/inventory-control.md): two complete inventories pass; seven omissions/substitutions/duplicates reject. User acceptance follows Claude's twenty-third audit and is retained in a separate envelope; original execution unchanged. |
| Evidence-kind control S0-LL03-a | ACCEPTED at reviewed scope | [Production-gate control](stage0/evidence-kind-control.md): two complete synthetic inputs pass and 22 controls reject, including hand-built/synthetic evidence offered for generated acceptance across C/C4/B and full/reduced profiles. Classification and exact fixture identities remain visible on failure. User acceptance follows Claude's twenty-eighth audit; original execution unchanged. |
| Initializer control S0-LL01-a | ACCEPTED at stated scope | [Initializer failure control](stage0/initializer-control.md): one complete and eight refused bootstraps across early/middle/late Wasm exceptions, diagnostic continuation and module omission. Four loader mutations reject; no partial ready artifact from the correct loader. Fixed Stage 0 harness, no CCL-image or Stage 1 loader claim. Accepted by the user's explicit decision after Claude's [thirty-fourth audit](stage0/claude-review.md); initializer hangs and cross-chain dependencies remain outside the fixture's scope. |
| Required-child control S0-LL01-b | ACCEPTED by user | [Production-aggregate control](stage0/child-command-control.md): one complete synthetic workflow passes; ten real exit/kill/timeout/missing-marker stimuli fail with the exact step identified and no later command executed. Original subprocess and aggregation code are unchanged. No native build or Wasm execution claim. Claude's [thirtieth audit](stage0/claude-review.md) re-executed producer and verifier, reproduced the composite and found no defect; two nonblocking observations recorded. |
| Behavioral assertion control S0-LL02-b | ACCEPTED by user | [Wasm control corpus](stage0/behavioral-control.md): five calls exercise an escaping mutable environment, six complete values and nested normal/exceptional cleanup. Five separately emitted semantic mutants fail the unchanged assertions at the expected first case. Minimal descriptor and fixed memory; no generated-code, GC or threading qualification. Claude's [thirty-second audit](stage0/claude-review.md) re-executed producer and verifier byte-identically and found no defect. |
| Artifact identity S0-LL22-a | ACCEPTED at stated scope | [Artifact control](stage0/artifact-identity-control.md): two complete synthetic builds and 36 refused records across ten roles. User acceptance follows Claude's thirty-sixth audit, with the narrower production-role list disclosed. Production-policy enhancement is separately authorized; no full-build consistency claim. |
| Production artifact policy | IMPLEMENTED; reviewed by Claude's thirty-eighth audit, no defect | [Per-test requirements](stage0/artifact-policy.md) add applicable source, ABI, template, binary, compiler, image and option roles to ten pending tests. All 35 accepted contracts remain unchanged; local policies cannot waive the global minimum. |
| Structured diagnostics S0-LL23-a | ACCEPTED at reviewed scope | [Actual Wasm fault diagnostics](stage0/diagnostics.md): thirteen execution cases, three preflight refusals, twelve rejected reporter mutants and ten real artifact-role omissions. Exact binary offsets checked against independent disassembly; first failure and bounded output preserved. Scalar Node/V8 scope. |
| Project ledger S0-LL24-a | ACCEPTED at reviewed scope | [Ledger and decision control](stage0/ledger-control.md): actual pre-delivery project snapshot, thirty-one rejected metadata mutations, all 35 accepted records preserved. Current authority, criterion decisions, artifact links and unresolved work checked; old runtime payloads reused. |
| R6 control S0-LL22-b | ACCEPTED at reviewed scope | [Actual registration comparison](stage0/r6-control.md): fresh reversible U1 patch application, all four R6 categories and evaluated R6a joins; one complete comparison and 44 rejected controls. Accepted native build/test bytes reused; comparator specific to this patch. Claude's [forty-second audit](stage0/claude-review.md) found no defect; the user explicitly accepted the slot. |
| Typed conversions S0-LL07-a | ACCEPTED at reviewed scope | [Conversion fixture](stage0/conversions.md): 272 cases across two variants, nineteen rejected implementation/bridge mutants, ten rejected artifact-role omissions and 214 byte-identical replay files. Real memory grows above 2 GiB; scalar hand-built scope, separate ID and typed-slot registries. Claude's [forty-fourth audit](stage0/claude-review.md) re-executed producer and verifier byte-identically, recomputed the stateless expectations independently and found no defect. |
| Late Worker S0-LL13-b | ACCEPTED at reviewed scope | [Shared-state preservation](stage0/late-worker.md): four real Workers, 24 complete transitions with whole-memory and instance checks, eleven initialization/install refusals, nine rejected semantic mutants and ten role omissions. Existing shared/private state and callable slots survive lazy installation and two later Workers. Hand-built scalar scope; emitter-derived regions and private tables. Accepted by the user after Claude's forty-sixth audit. |
| Engine matrix S0-ENGINE-a | ACCEPTED at reviewed scope | [Engine and profile matrix](stage0/engine-matrix.md): Node/V8, Chrome, Firefox and Safari execute the same hand-built module; after Firefox 156 gained JSPI the single-thread JSPI profile is admitted on Node, Chrome and Firefox and absent only on Safari. Encoding pin, seven rejected mutants, nine role omissions. R1 retained as evidence about Firefox 147. |
| Template materialization S0-LL21-c | ACCEPTED at reviewed scope | [Materialization](stage0/materialization.md): no defect found in R1; rerun because the fixture pins the refreshed engine contract and its profile-not-admitted control now uses Safari's row. One flag byte, nine link cases, three profile runtimes, 23 refusals, seven mutants, ten role omissions. |
| Nested exception transfer S0-LL19-a | ACCEPTED at reviewed scope | [Nested exits](stage0/nested-eh.md): Codex's R2 review found the handler-only frame restored an invented cleanup record; every frame now owns its cleanup state and unwinding restores it, every handler entry records the state it observes, and the oracle derives every expected address from the frame model rather than saved fields. Ten cases, ten handler witnesses, thirteen mutants, ten role omissions. R1 and R2 retained unaccepted. |
| Layout schema and contract join S0-CONTRACTS-a | ACCEPTED at reviewed scope | [Schema and join](stage0/contracts.md): the 377-row [wasm32 layout schema v1](contracts/wasm32-layout.v1.md) derived from the pinned U1 sources with a closed source-to-target ledger and the one named Lisp/C disagreement; the [runtime-contract join](contracts/runtime-contracts.v1.json) verifies frames, roots, 56 TCR fields, allocation/store, twelve C-boundary cases and the re-derived 14-region ownership map against the accepted fixtures by artifact hash. Nine refused controls and four role omissions. Control execution; no Wasm executed. |
| Initializer binding S0-LL15-a | ACCEPTED at reviewed scope | [Bootstrap closure](stage0/initializer-binding.md): three phases, four modules and nine initializers bound to prerequisite completion words and service state; the loader seeds its own dependencies before any bundle, checks every completion physically and publishes ready last. Fourteen named refusals including an omitted required module and a clobbered prerequisite; eight rejected loader mutants including a no-load path caught by whole-image comparison; ten role omissions. Not the census closure. |
| Kernel-import census | EXECUTED diagnostic; awaiting Codex review (Claude-authored) | [D6 census](stage0/kernel-imports.md) and [generated contract](contracts/kernel-imports.v1.md): 65 imports joined across the C table and the x8632 enumeration, 63 x8632 caller sites over 42 imports, every definition located, per-profile dispositions with closure phase, replacement and regression obligations, and census-shaped nodes for the graph's unresolved `gap:imports` placeholder. Seven refused controls. Source inspection only; reachability is S0-LL15-b work; no gate credit. |
| Production TCR schema | TCR-SCHEMA-R3 REVIEWED; no defect at recorded scope | [D5 schema](stage0/tcr-schema.md): adds `fp_control`, the logical floating-point enable mask the ARM model keeps in the TCR, under the D6 policy decided 16 September; 48 fields, every replaced native cell mapped, every fixture field mapped or fixture-private, eight refused controls. R2 reviewed without defect. |
| Floating-point detection | POLICY DECIDED 16 Sep (ARM model); FLOAT-DETECTION-R4 REVIEWED; no defect at recorded scope | [D6 detection and policy](stage0/float-detection.md): the user chose the ARM model; the module now carries the enable mask, flags per status, ARM priority and signalling comparisons, executed for every status under seven masks and witnessed natively through comisd. 2,135 corpus cases agree with the oracle, 1,656 natively; fourteen rejected mutants. Cost measurement owed; no gate credit. |
| Stage 1 inventory and plan | ADOPTED by the user 16 Sep | [Inventory](stage1/inventory.json) of 33 tests, including the user-authorized BT-0 coverage extension, in six subgates is the criterion of the [Stage 1 ledger](evidence/current-stage1-gate-result.json); [plan](stage1/plan.md). Author Codex, reviewer Claude; single-thread JSPI profile deferred to the profiles stage. |
| Stage 0 acceptance | PASS | 48 required variants: 48 accepted, 0 missing and 0 unreviewed. [Current ledger](evidence/current-stage0-gate-result.json). |
| Stage 0 exit criteria (0F) | REVIEWED; no gap needing a scope decision | [Exit-criteria review](stage0/exit-criteria-review.md): every sentence of the outline's Stage 0 section mapped to accepted slots or recorded decisions; imports, traps, direct foreign calls and barrier-sensitive stores carried into Stage 1 by name under census contract v0.2. |
| Stage 1 subgate 1A | ACCEPTED and integrated | [Registration, build bindings and generated diagnostics](stage1/1a.md): S1-LL08-a, LL22-b and LL23-a; two native suites pass, all 164 FASLs restored, nine real generated functions. Claude replayed the verifier and a fresh three-build run with byte-identical results and probed eleven further forms. Exact reviewed 1A unit integrated. |
| Stage 1 representation | ACCEPTED after Claude’s sixty-third audit | [Generated cons access and mutation](stage1/representation.md): S1-LL04-a; 19 functions, 740 target checks, 15 compiler mutants, R6/R6a passed. Exact reviewed compiler extension integrated. |
| Stage 1 typed conversions | ACCEPTED after Claude’s sixty-fourth audit | [LL07 generated conversions](stage1/conversions.md): 17 functions, 349 cases, 14 static refusals, 20 recompiled compiler mutants; R6/R6a passed. Raw primitive ABI distinct from Lisp B. |
| Stage 1 B call core | ACCEPTED and integrated by user decision after Claude’s sixty-fifth audit; no LL05 gate credit | [Generated call core](stage1/b-call-core.md): 34 functions, 352 comparisons, 15 compiler mutants, R6/R6a passed. Required arguments and full values; remaining LL05 work stays open. |
| Stage 1 B bindings | ACCEPTED and INTEGRATED 16 Sep after Claude's sixty-sixth audit; no LL05 gate credit | [Optional/keyword binding](stage1/b-bindings.md): 51 functions, 992 comparisons, 16 compiler mutants, R6/R6a passed. Rest/APPLY and other LL05 obligations remain. |
| Stage 1 rest/APPLY | ACCEPTED and integrated after Claude’s sixty-seventh audit; no LL05 gate credit | [Rest lists and APPLY](stage1/b-rest-apply.md): 71 functions, 1,356 comparisons, 18 compiler mutants, R6/R6a passed. Fixed 64-argument ceiling removed. |
| Stage 1 result capacity | ACCEPTED and integrated after Claude’s sixty-eighth audit; no LL05 gate credit | [Runtime result capacity](stage1/b-results.md): 95 functions, 1,604 comparisons, 16 compiler mutants; up to 1,024 returned values, checked runtime reservations, R6/R6a passed. |
| Stage 1 callable objects | ACCEPTED and integrated after Claude’s sixty-ninth audit; no LL05 gate credit | [Callable objects and bindings](stage1/b-callables.md): 108 functions, 1,824 comparisons, 16 compiler mutants; live symbol cells, retained functions and recursion; R6/R6a passed. |
| Stage 1 lexical closures | ACCEPTED and integrated after Claude’s seventieth audit; no LL05 gate credit | [Closure construction and mutation](stage1/b-closures.md): 149 source functions, 177 modules, 2,296 comparisons, 17 compiler mutants and 72 closure checks; R6/R6a passed. |
| Stage 1 local functions | REVIEWED by Claude (seventy-first audit, no defect); no LL05 gate credit; integration is the user's decision | [Local calls and recursion](stage1/b-local-calls.md): 183 source functions, 241 modules, 2,748 comparisons, 16 compiler mutants, two development regression controls and independent pre-emitter root counts; R6/R6a passed. |
| Stage 1 proper tail calls | ACCEPTED and INTEGRATED at reviewed auxiliary scope; no LL05 gate credit | [Tail transfer and literal APPLY](stage1/b-tail-calls.md): 270 modules, 2,964 comparisons, 36 chains of 100,000 steps with a 2 KiB stack budget, 19 compiler mutants; R6/R6a passed. |
| Stage 1 lazy B installation | ACCEPTED and INTEGRATED on user approval after Claude’s seventy-third audit; no LL05 gate credit | [Lazy generated entries](stage1/b-lazy-calls.md): 270 cataloged modules, 267 installed, 2,964 comparisons, 36 long tail chains, 35 installation cases and 14 rejected loader/stub mutants. Unchanged reviewed compiler and native/R6 evidence reused. |
| Stage 1 direct compiled calls | ACCEPTED and INTEGRATED on user approval after Claude’s seventy-third audit; no LL05 gate credit | [Arguments in their continuation](stage1/b-direct-context.md): 276 modules, 3,044 comparisons, no compiled entry through public wrappers; R6/R6a and lazy composition pass. |
| Stage 1 generated cleanup | ACCEPTED and INTEGRATED after Claude’s seventy-fourth audit; no LL05/LL19 credit | [UNWIND-PROTECT](stage1/b-unwind-protect.md): 307 modules, 3,504 comparisons, 132 cleanup-entry inspections, 12 resource/payload checks and 18 rejected compiler mutants; native R6/R6a and lazy composition pass. |
| Stage 1 generated nonlocal exits | ACCEPTED and INTEGRATED after Claude’s seventy-fifth audit; no LL05/LL19 credit | [CATCH/THROW](stage1/b-catch-throw.md): 350 modules, 4,440 comparisons, published catch/cleanup chain, 19,280 control inspections, 18 compiler mutants and four loader mutants; native R6/R6a and lazy composition pass. |
| Stage 1 dynamic special bindings | ACCEPTED and INTEGRATED after Claude’s seventy-sixth audit; no slot credit | [Special bindings](stage1/b-special-bindings.md): 390 modules, 5,292 comparisons, 8,252 binding inspections, 44 metadata/capacity checks, 19 compiler mutants and 3 development regressions; native R6/R6a and lazy composition pass. |
| Stage 1 special parameters and PROGV | ACCEPTED and INTEGRATED after Claude’s seventy-seventh audit; no slot credit | [Dynamic parameter extents and runtime bindings](stage1/b-dynamic-bindings.md): 443 modules, 6,024 comparisons, 24,388 binding inspections through 128 bindings, 24 PROGV checks, 21 compiler mutants and three inherited regressions; native R6/R6a and lazy composition pass. |
| Stage 1 lexical exits and CONS | ACCEPTED and INTEGRATED after Claude’s seventy-eighth audit; no slot credit | [Lexical exits](stage1/b-block-exits.md): 492 modules, 6,556 comparisons, 20 expired-exit/allocation checks, 16 compiler mutants and four regressions. Native R6/R6a, compatible-loader lazy composition and retained replay pass. |
| Stage 1 multiple-value calls and bindings | ACCEPTANCE WITHDRAWN; integration reversed; improvement required; no slot credit | [Multiple values](stage1/b-multiple-values.md): 547 modules, 7,232 comparisons, 17 compiler mutants, 12 capacity checks and 48 bounded tail chains. Native R6/R6a, lazy composition and retained replay pass. |
| Stage 1 MVC storage correction | R2 ACCEPTED AND INTEGRATED on renewed user direction after Claude’s eighty-first audit | [Correction](stage1/b-mv-storage.md): 587 modules, 7,332 comparisons, 19 compiler mutants, 56 storage checks; inline small results and stable producer descriptors. R1 and audits 80 and 81 are retained; the original unit’s withdrawal remains historical. |
| Stage 1 explicit condition dispatch | R2 ACCEPTED and INTEGRATED after audit 83 | [Conditions](stage1/b-conditions.md): source CASE/LIST/POP scaffolding scoped to generated handlers; 14 new refusals and three regression controls. All 848 condition and 7,332 inherited comparisons pass, with ten dispatcher and nineteen inherited mutants. Result-mode probe exposes descriptor/release overhead; the separate result-scratch proposal narrows it. |
| Stage 1 per-callee result scratch | ACCEPTED and INTEGRATED after audit 83 | [Result scratch](stage1/b-result-scratch.md): 112 of 587 inherited modules have an independently checked four-word scratch proof; dynamic delivery stays intact. Seven storage controls and 72 additional native comparisons. The focused scalar witness removes two callee descriptors and two releases; large/unproven paths remain dynamic. |
| Stage 1 LL05 calls and adapters | ACCEPTED and INTEGRATED after Claude’s eighty-fourth audit | [Combined qualification](stage1/ll05.md): 587 inherited modules / 7,332 comparisons, 62 explicit-condition modules / 848 comparisons, 46 call-error modules / 288 comparisons; 36 installation cases, 48 bounded 100,000-transfer chains, native R6/R6a and retained replay. Both LL05 records accepted. Constants now accepted and integrated; [implementation plan](stage1/ll10-plan.md). |
| Stage 1 constants | ACCEPTED and INTEGRATED after Claude audits 85 and 86 | [LL10 qualification](../../tests/wasm/stage1/constants/README.md): 87 generated B modules plus a generated header probe, 233 native-derived comparisons, three placements through 2 GiB, four compiler and seven image mutants. Fresh-Worker persistence, cold constants, closure pools, bounded tail chains, inherited calls and native R6/R6a pass. The exact reviewed compiler and compatible loader are integrated. |
| Stage 1 constants review follow-up | ACCEPTED with LL10 after Claude’s eighty-sixth audit | [Focused tests](../../tests/wasm/stage1/constants/review-followup/README.md): distinct mutant failures, native cross-function identity and restore-by-identity with two semantic controls. 91 modules, 245 + 9 comparisons; 1,150 files replay identically. Compiler unchanged by the follow-up; its strengthened checks are included in LL10 acceptance. |
| Stage 1 repeated keyword aliases | ACCEPTED and INTEGRATED after Claude’s eighty-seventh audit | [Integration record](stage1/integration-b-repeated-keywords.json): exact reviewed compiler bytes; first formal and first actual win, later aliases default. Native CCL compatibility, with no portable-language guarantee claimed. Reviewed 22 forms / 66 new comparisons, mutants, native R6/R6a and inherited execution reused. |
| Stage 1 control/recovery S1-LL19-a | ACCEPTED and INTEGRATED after Claude’s eighty-eighth audit | [Generated LL19](stage1/control.md): 107 modules, 184 native comparisons, 32 resource/pending checks, thirteen compiler mutants, five source refusals, six publication controls; complete inherited/lazy and native R6/R6a pass. Verifier replayed byte-identically from a detached worktree; a four-case debugger-hook probe agreed. Exact reviewed implementation integrated. Auxiliary audit-88 follow-up names persistent fields in compatible TCR v2, documents the shared reserve flag and executes four native ordinary-service decline cases (16 comparisons); 45 deterministic files reproduce. Follow-up ACCEPTED by the user after Claude’s eighty-ninth audit (no defect): replay from a detached worktree reproduced all 45 deterministic files and 9 pins; no additional slot credit. |
| Stage 1 bindings S1-LL17-a | ACCEPTED and INTEGRATED after Claude’s ninetieth audit | [Binding vectors](stage1/binding-vector.md): 40 native cases, 160 comparisons, 96 owner/resource checks, twelve compiler mutants, two real Worker suspensions and eight binding-vector evacuations; native R6/R6a and inherited/lazy requalification pass. Verifier replayed 3,842 deterministic files and 176 pins byte-identically from a detached worktree; a five-case probe (20 comparisons) agreed with native. Exact reviewed compiler integrated; runtime unchanged. Audit observations retained in the integration record. |
| Stage 1 collector core | ACCEPTED and integrated after Claude’s ninety-first audit; auxiliary | [Moving service](../../tests/wasm/stage1/collector-core/README.md): 22 generated modules, 56 native comparisons, 34 collections, 75 core checks, eight C mutants and three compiler controls. Rooted capture/self reloads, non-growing unbind and the collector C service are integrated. No LL18 slot credit; complete root admission, allocation retry and growth remain open. |
| Stage 1 moving restarts and temporaries | ACCEPTED and integrated after Claude’s ninety-second audit; auxiliary | [Movement follow-up](../../tests/wasm/stage1/collector-live/README.md): 63 modules, 120 native comparisons, 62 collections; previous moving corpus also passes (56 comparisons, 34 collections). Fixes EQ operand roots, captured initializer addresses and the implicit condition retained across handlers; admits restart layout and distinguishes canonical T. 95 core checks, four compiler and five C mutants; native R6/R6a and inherited/lazy pass. No LL06/LL18 slot credit. |
| Stage 1 collector owner admission and growth | ACCEPTED and integrated after Claude’s ninety-third audit; auxiliary | [Owner boundary](../../tests/wasm/stage1/collector-owner/README.md): disjoint canonical/image/stack ownership, external and TCR roots, reclaim before growth, view refresh and actual memory growth beyond 2 GiB. 41 checks, eight mutants, eight generated native comparisons plus 25 owner comparisons; retained replay passes. Unchanged compiler/C reuse reviewed R6/R6a. Internal allocation retry and LL06/LL18 qualification remain open. |
| Stage 1 internal allocation retry | ACCEPTED and integrated after Claude’s ninety-fourth audit; auxiliary | [Retry unit](../../tests/wasm/stage1/allocation-retry/README.md): generated fixed-heap and rest allocations call the owner only on shortage. 41 modules, 189 native comparisons, one refusal after movement, six compiled mutants; 302 collections and 68 growths. Native R6/R6a and retained replay pass; default-mode Wasm/WAT comparison is byte-identical. Other constructors, new loader admission and full LL06/LL18 qualification remain open. |
| Stage 1 raw constructor retry and owner loader | ACCEPTED and integrated after Claude’s ninety-fifth audit; auxiliary | [Constructor unit](../../tests/wasm/stage1/constructor-retry/README.md): binding vectors, restarts and conditions survive collecting retry; PROGV cursors and new binding values rooted. 74 modules, 301 comparisons in each of two allocation modes, 302 cold-loader observations, six nested refusals, eight compiled mutants, 22 loader checks and 228 default-mode identical files. Native R6/R6a and fresh retained replay pass. No LL06/LL18 credit. |
| Stage 1 small-heap collector (S1-LL18-a) | REVIEWED by Claude (ninety-sixth audit, no defect); proposal, not integrated; acceptance is the user’s decision | [Qualification](../../tests/wasm/stage1/collector-qualification/README.md): 874 native comparisons across retry, explicit polls and moving/pinned literal pools; 302 identical cold-loader observations, complete declared root populations, visible reclaim, maximum-memory/EGC refusals, 17 runtime mutants and 11 publication controls. Fresh retained replay passes; unchanged compiler reuses reviewed R6/R6a. |
| Stage 1 temporary lifetime (S1-LL06-a) | ACCEPTED and integrated after Claude audits 97/98 | [Qualification](../../tests/wasm/stage1/temporaries/README.md): PROG2 and local TAGBODY/GO; 30 native cases, 120 comparisons, 300 collections, eight compiled mutants and eight source refusals. Control/root/stack restoration includes cleanup, special bindings and fatal exits. New native R6/R6a and fresh replay pass; 152 inherited WAT/Wasm files unchanged. |
| Stage 1 closed GO and ordinary loop branches | ACCEPTED and integrated after Claude audit 99; auxiliary | [Closure transfers](../../tests/wasm/stage1/closure-transfers/README.md): 44 modules, 120 new comparisons and 130 collections; inherited LL06 adds 120 comparisons and 300 collections. Seven compiler mutants, six admission refusals, nine shape checks and native R6/R6a pass. Closed handlers/restart clauses use U1 captured catch tags; eligible loops use branches without control records or exit exceptions. Callable metadata remains LL12 work; no slot credit. |
| Stage 1 callable metadata (LL12-a) | ACCEPTED and integrated after Claude audit 100 | [Qualification](../../tests/wasm/stage1/callable-metadata/README.md): 16 modules, 33 native signature comparisons, mutable escaping environments and keyword/debug records through cold installation and relocated snapshots. Seven compiler and two runtime mutants; six snapshot refusals; native R6/R6a and 614-file replay pass. Integrated bytes equal the reviewed proposal. |
| Stage 1 aliases and redefinition (LL11-a) | ACCEPTED and integrated after Claude audit 101 | [Qualification](../../tests/wasm/stage1/binding-installation/README.md): package/case-qualified aliases, distinct mutable closure environments and old functions after redefinition through an atomic owner installer. Nine generated modules, 48 invocations, 64 atomic refusals and seven mutants; fresh replay passes. Accepted compiler unchanged; installer integrated byte-identically. |
| Stage 1 empty generic dispatch (LL11-b) | ACCEPTED and integrated after Claude audit 102 | [Qualification](../../tests/wasm/stage1/generic-dispatch/README.md): generated method mutation and direct/encapsulated dispatch, actual no-applicable condition and CONTINUE. Thirty modules, 144 invocations, nine compiled faults; inherited conditions, native R6/R6a and fresh 1,076-file replay pass. Initial primary-method subset; compiler integrated byte-identically, bootstrap source forms retained in the fixture. |
| Stage 1 integer arithmetic core | ACCEPTED and integrated after Claude audits 103/104; auxiliary | [Integer service](../../tests/wasm/stage1/integer-core/README.md) unchanged. [Follow-up](../../tests/wasm/stage1/integer-core/review-followup/README.md) adds the discriminating two-result allocation control, large multiply/divide cases and diagnostic clarifications: 4,553 cases and thirteen rejected faults, fresh replay passes; follow-up reviewed by Claude audit 104, no defect. Exact service source integrated; no LL16 credit. |
| Stage 1 generated integer calls | ACCEPTED and integrated after Claude audit 105; auxiliary | [Generated calls](../../tests/wasm/stage1/integer-calls/README.md): 25 modules, 1,221 native comparisons, 429 collector copies, one growth and nine rejected faults. Inline fixnums; rooted integer-service fallback. Native R6/R6a and fresh replay pass. Numeric Lisp conditions, floating point and loader admission remain; no LL16 credit. |
| Stage 1 integer conditions | ACCEPTED and integrated after Claude audit 107; auxiliary | [Follow-up](../../tests/wasm/stage1/integer-condition-review/README.md): fixes oracle/admission gaps, matches NIL divisor and ASH check order. 1,693 comparisons, 477 collector copies, seventeen rejected controls; native R6/R6a and replay pass. Policy-dependent error priority and wrong-class reader differences retained. No LL16 credit. |
| Stage 1 numeric owner composition | ACCEPTED and integrated after Claude audit 108; auxiliary | [Packet](../../tests/wasm/stage1/integer-owner/README.md): accepted compiler unchanged; numeric and allocation capabilities admitted as one trusted owner bundle. 80 modules, 1,777 comparisons per mode, eager/cold equality, forced constructor movement, 32 admission checks and ten rejected faults. Runtime integrated; no LL16 credit. |
| Stage 1 floating-point primitive service | R2 ACCEPTED and integrated after Claude audit 110; auxiliary | [Packet](../../tests/wasm/stage1/float-core/README.md): corrected signed-zero oracle; 177,249 target comparisons, 1,386 hardware witnesses. C service and detector integrated byte-exactly. No LL16 credit. |
| Stage 1 floating-point owner capability | ACCEPTED and integrated after Claude audit 111; auxiliary | [Packet](../../tests/wasm/stage1/float-owner/README.md): 6,340 comparisons, 2,834 forced collections, real growth and 12 rejected faults. Exact reviewed service integrated in 401c1334; no LL16 credit. |
| Stage 1 generated floating calls | R2 ACCEPTED and integrated after Claude audit 113; auxiliary | [Follow-up](../../tests/wasm/stage1/float-calls/review-followup/README.md): native silent bignum coercion with small-integer and later arithmetic flags preserved. 17,516 comparisons and 7,768 collections per eager/cold run, nine rejected faults; original raw entry passes 177,249 comparisons. [Integration](stage1/integration-float-calls.json) binds the reviewed compiler, primitive, owner, adapter and loader. No LL16 credit. |
| Stage 1 numeric qualification (S1-LL16-a) | ACCEPTED at the reviewed audit-114 scope after integration of the audit-115 performance corrections | [Qualification](../../tests/wasm/stage1/numeric-qualification/README.md): 72,200 comparisons per eager/cold run, 31,000 collections, independent integer and rational oracles, dispatch/semantic/omission controls and fresh generated checking-cost measurements. Retained replay identical over 208 deterministic files at 470 pins. Auditor’s poisoned-table probe extends fallback independence to all 4,379 floating rows and the 243 division-by-zero rows. Integrated compiler/runtime unchanged; integration f2b1af41 verified against the reviewed proposals. |
| Stage 1 numeric allocation fast path | ACCEPTED AND INTEGRATED after Claude audit 115; no slot credit | [Proposal](../../tests/wasm/stage1/numeric-fastpath/README.md): sufficient-space assurance retains live ownership checks and defers image enumeration until copying. 72,200 comparisons and 31,000 collections per eager/cold run; 55 owner checks, 6,340 raw-owner comparisons plus grown-result reuse, five rejected faults. Observer-free double arithmetic improves from ~76 to ~4.8 µs/op; native CCL is 16–17 ns/op. This first step leaves a ~280–300× gap; the scalar proposal below addresses its service boundary. LL16 is accepted at its declared subset/policy scope. |
| Stage 1 direct Wasm scalar numerics | ACCEPTED AND INTEGRATED with the owner fix after Claude audit 115; no slot credit | [Proposal and replay](../../tests/wasm/stage1/scalar-floats/README.md): unchanged compiler/modules; eligible scalar operations use a direct Wasm import with existing ownership checks and pre-write fallback. 72,200 comparisons and 31,836 collections per eager/cold run; 145,804 raw comparisons, 109 direct-path checks, ten rejected faults. Double arithmetic 147–154 ns/op versus native 16–17 ns; about 500× faster than the original ~76 µs, still about 9× native. Persistent-frame addition is 93 ns versus generated 152 ns; the difference includes more than stack work. Runtime bytes equal the reviewed proposal; the digest-bound scalar binary is shipped for explicit owner selection. Auditor’s replay identical over 151 deterministic files at 488 pins (fast path: 13 files, 477 pins); 300,000-case differential fuzz of the direct path against the Lisp service found no divergence; direct-path coverage by the inherited corpora is 2,920 of 10,540 floating calls per non-pressure generated setting and 21,855 of 36,451 raw cases per plain run. |
| Bootstrap compiler throughput | ACCEPTED AND INTEGRATED — audit 142 | [Front end](../../tests/wasm/stage1/bootstrap-frontend/README.md): unchanged-source admission 33 → 426 of 2,492 readable target definitions; 268 explicit read/name skips. Four original CCL definitions execute, 180 native comparisons plus four target-layout checks and 92 collections. Native R6/R6a passes. Sample counts are not full-worklist coverage; no LL15 credit. |
| Bootstrap constants, specials and OR | EXECUTED / NOT REVIEWED — auxiliary | [Proposal](../../tests/wasm/stage1/bootstrap-values/README.md): 426 → 472 → 574 → 639/2492 source admissions; eight original definitions execute, 82 static dependency closures. 456 comparisons, 256 collections, five compiler faults rejected. Native R6/R6a passes; replay 940 files at 145 pins. No slot credit. |
| Stage 1 acceptance | BLOCKED | 33 required variants: 21 accepted, 12 missing and 0 unreviewed. The two additional missing variants are BT-0 operator and unchanged-source coverage. [Current ledger](evidence/current-stage1-gate-result.json). |
| Stage 1 LL18-b EQ table movement | ACCEPTED / INTEGRATED after Claude audit 116 | [Qualification](../../tests/wasm/stage1/hash-tables/README.md): nine generated modules, 2,588 generated/native and 2,584 primitive comparisons, 88 collections, twelve runtime faults rejected. Strong EQ backing vectors with moved-key notification and cache invalidation; fixed capacity and declared runtime-leaf boundary. Reviewed collector, hash service and adapter integrated byte-exactly. Auditor’s replay identical over 118 deterministic files at 96 pins; the fourteen-word native prefix and the track-keys/key-moved raw bits 30/29 are confirmed against xdump/hashenv.lisp and lisp-kernel/constants.h; a 718,148-operation differential movement fuzz (61,852 collections, primitive and generated paths, capacities 4–64, immediates and table-as-key) found no divergence; a leaf refusal delivered through all six ordinary generated forms leaves the table unchanged and restores the root chain, value stack and handlers. |
| Stage 1 portable runtime digests | ACCEPTED / INTEGRATED after Claude audit 117; exact reviewed runtime bytes | [Proposal](../../tests/wasm/stage1/portable-digests/README.md): synchronous factories, no Node crypto/Buffer; Node and Chromium Worker checks, exact inherited numeric/installer results, thirteen rejected faults. Reviewed runtime integrated; focused Node/Chromium integration results byte-identical. Auditor’s replay identical over 23 deterministic files with the pinned Node, clang, Chromium and Playwright; an independent 10,347-case differential against node:crypto (offsets, DataViews, shared buffers, Unicode strings, inputs of 512 MiB±1 and 1 GiB−3 bytes) found no divergence. The mixed-placement same-Worker refusal reproduces deterministically in the pinned Chromium 145.0.7632.6 on the unchanged installer extent check (4168+16 against a 2,147,549,184-byte view evaluates false), disappears with `--js-flags=--no-maglev`, persists with `--no-turbofan`, and does not occur in Node (V8 14.1) or Chrome 153.0.8010.36: an engine-tier fault in that build, not a runtime defect. Portability pause complete; LL21-a now executed for review. |
| Stage 1 LL21-a materialization | ACCEPTED / INTEGRATED after Claude audit 118 | [Qualification](../../tests/wasm/stage1/materialization/README.md): canonical production templates, shared/unshared code at three placements, native R6/R6a, eleven rejected faults and full retained replay. Compiler switch and portable materializer integrated byte-exactly. Auditor’s replay identical over 1,148 deterministic files at 94 pins including the native R6/R6a build; all 36 installed binaries link only against memory of their recorded sharedness (36 LinkError refusals for the other); 25 crafted import-section inputs to the offset parser refuse with the named reason or locate the byte; every template’s text equals its baseline with the single “ shared” token removed, including the five nested closure modules. The integration of the portable digests at eadec6c9 is byte-identical to the reviewed proposal. Engine identity in D2 records is declared provenance, not detected at install. |
| Stage 1 LL21-b module granularity | ACCEPTED / INTEGRATED after Claude audit 119 and README correction | [Qualification](../../tests/wasm/stage1/granularity/README.md): one generated function per module, 19 modules / 38 entry mappings, four code generations, native-derived answers in three Workers. Cold decode ~1.93 ms under the default lazy tier (about 8 ms with eager compilation), warm instantiation ~0.18 ms, full validated install ~37.1 ms per 19-module set. Retention grows explicitly; engineering choice with no statistical ranking. Nineteen refusals, eight faults and seven publication controls pass. Auditor’s replay identical over 190 deterministic files at 111 pins; timing medians and the eight retention raw joins recomputed independently; all 38 entry body ranges re-parsed from the code sections; the sixteen base binaries equal the accepted LL21-a bytes and each revision differs from its origin by one constant. The current README now matches the retained eager-Liftoff record, 458,786→549,124 bytes; the immutable R1 README has the superseded r3 figures. Two of the nineteen modules are byte-identical, so the engine retains eighteen NativeModules. Owner/build module integrated byte-exactly; compiler and prior runtime files unchanged. Integrated checks and three-Worker observations reproduce. |
| Stage 1 LL09-a symbols and packages | ACCEPTED / INTEGRATED after Claude audits 120/121 | [Qualification](../../tests/wasm/stage1/symbols/README.md): thirteen generated B modules, 582 native trace rows and 1,884 generated comparisons. Package identity, canonical NIL/T, keywords, uninterned symbols and bindings before/after six relocated image loads; versioned hash rebuild before lookup. Seventy-two refusals, twelve implementation faults, eight publication controls and twelve role omissions. Auditor’s replay identical over 179 deterministic files at 96 pins; an independent package model reproduces all 582 native answers and the native log re-parses to the retained answers; a 7,500-operation differential fuzz of the symbol service against that model, filling all five tables to capacity and the image to its last byte with loads to the 2 GiB base and back, found no divergence. By trace construction MAKE-SYMBOL reaches three of the six generated calling forms, no trace name pre-existed as a native keyword, and no retained load targets the 2 GiB base. Fixed-capacity pinned image and sealed topology; production package installation/GC remain open. The corrected service and reviewed adapter are now integrated.  Corrected service and unchanged adapter integrated; integration replay byte-identical. |
| Stage 1 LL09-a audit follow-up | ACCEPTED / INTEGRATED after Claude audits 120/121 | [Follow-up](../../tests/wasm/stage1/symbols/review-followup/README.md): explicit MAKE-SYMBOL through all six forms before/after load, known native keyword identity, and primitive/generated loads into 2 GiB. Native CODE-CHAR refuses surrogates; one derived service guard now agrees. Original 1,884 generated comparisons and all controls pass; 36 directed form checks, 12 keyword checks, 96 character checks and ten new controls. Auditor’s replay identical over 253 deterministic files at 103 pins; the thirteen modules and adapter equal the reviewed R1 bytes and the service differs by the one guard line; the audit-120 differential fuzz passes unchanged against the corrected service, and 60 surrogate shapes at every position refuse with state preserved where the R1 service admitted them. R1’s own pin enumeration now includes this nested directory, so R1 replays only from 3ebb85d6. Integration takes the corrected service from this follow-up.  Corrected service and unchanged adapter integrated; integration replay byte-identical. |
| Stage 1 LL13-a initialization | ACCEPTED / INTEGRATED after Claude audits 122/123 | [Qualification](../../tests/wasm/stage1/initialization/README.md): five generated modules and six real Workers; process-once initialization and Worker-owned setup preserve shared mutations, runtime sentinels and original published code through late lazy installation at 4 MiB and 2 GiB. Forty owner refusals, ten faults, seven publication controls and twelve role omissions. Auditor’s replay identical over 315 deterministic files at 104 pins; 36 independent layout and module refusals (including defined-global, passive-data, passive and declarative-element and table-definition modules) refuse with the named reason and write nothing; the single-thread state machine, a tampered layout digest, and three-Worker races on the bootstrap claim and on one late-Worker claim each admit exactly one callback; the generated Worker initializer and reader caller reach the reader through the lazy tail table. The intermediate LL09-a acceptance is verified: the integrated service equals the follow-up source and rebuilds to the reviewed corrected binary, the adapter equals the R1 bytes, and the combined ledger differs from its predecessor by the one accepted row. The corrected owner is now integrated as initialization-owner.mjs; compiler and earlier runtime files unchanged. |
| Stage 1 initialization admission follow-up | ACCEPTED / INTEGRATED after Claude audits 122/123 | [Audit-122 follow-up](../../tests/wasm/stage1/initialization-review/README.md): actual Worker table capacities checked before claim; fresh and reserved control storage checked without writes. R1 generated results identical; 616 directed refusals, four races with twelve Workers, four new behavioral faults, ten inherited faults and seven publication controls. Auditor’s replay identical over 405 deterministic files at 111 pins, with the R1 pins re-asserted at HEAD; the retained owner and harness equal the derivation output, and the owner differs from R1 by the table-capacity guard, the reserved and fresh scans and their three call sites only; the audit-122 probe set (40 crafted refusals, state machine, digest tamper, two three-Worker races) passes against the derived owner, and dirty reserved words refuse on bootstrap, ready-process and Worker entry without a claim while digest words are not treated as reserved. The owner accepts one table object for both roles; the accepted lazy loader refuses that at installation. Accepted and integrated after audits 122/123. The integrated owner is the follow-up derivation, with actual tables supplied. Original R1 replay uses d6bf93fb after the added runtime module changed its recursive enumeration; follow-up frozen inputs replay at HEAD. |
| Stage 1 bootstrap schedule | ACCEPTED / INTEGRATED after Claude audit 124 — auxiliary | [Generated phase runner](../../tests/wasm/stage1/bootstrap-schedule/README.md): 14 modules, four fresh Workers, nine initializers, 36 native comparisons and 140 refusals. Twelve faults including forged completion without execution, plus seven publication omissions, rejected. Auditor’s replay identical over 103 deterministic files at 116 pins, with the 104 LL13-a R1 pins re-asserted at HEAD; all 36 installed-module digests in the retained record equal the compiled module bytes; 34 further admission refusals write nothing; the admitted plan and module bytes are private snapshots; a catalog module substituted behind the trusted installation callback is caught only through its effects, so a behaviour-identical binary with an appended custom section reaches ready unobserved, which is within the declared trusted-callback scope. The intermediate LL13-a acceptance is verified: the integrated owner equals the reviewed follow-up byte for byte, the integration replay outputs equal the retained follow-up records, and the combined ledger differs from its predecessor by the one accepted row. The scheduler is now integrated byte-exactly as bootstrap-schedule.mjs; actual production imports reproduce the reviewed execution and all 36 installed digests are checked. Trusted-adapter limits remain; no LL15 credit, native startup selection and implementation remain open. |
| Stage 1 native startup resets | ACCEPTED after Claude audit 125; installer adapter INTEGRATED — auxiliary | [Native reset effects](../../tests/wasm/stage1/startup-resets/README.md): thirteen U1 callbacks run against two dirty states and their generated replacements. Fifteen modules, two Workers at 4 MiB and 2 GiB, 52 native comparisons, 92 generated invocations, 42 refusals, nine faults and nine publication controls. Auditor’s replay identical over 130 deterministic files at 151 pins, with the parent’s 116 pins re-asserted at HEAD; all 13 selected source forms are literal resets and all 22 open forms are computed or procedural, so the selection omits no literal reset; all 60 retained installed digests equal the compiled bytes; the digest-bound adapter refuses a behaviour-identical custom-section catalog substituted behind the scheduler, a row or record that differs from its private catalog, and a second installation, and ignores mutation of the caller’s catalog, rows and bytes after construction. The intermediate acceptance and integration of the bootstrap schedule is verified: the integrated module equals the reviewed bytes, the acceptance binds the audit blob, the integration replay through production imports equals the retained record, and the 30 pinned files are unchanged at HEAD. Undeclared writes outside the 8,600-byte image window (thread-local binding vector, allocation area) reach ready unobserved, within the declared scope. The private-catalog adapter is integrated byte-exactly as bootstrap-install.mjs; production imports reproduce execution and assessment. Generated reset effects remain evidence pending production symbol materialization. No LL15 credit; 22 callbacks and native startup membership remain open. |
| Stage 1 browser startup configuration | R2 ACCEPTED / portable modules INTEGRATED after audit 127 — auxiliary | R2 CPU-cache effect and full-TCR harnesses accepted; exact configuration modules integrated. Production Node and Chromium reproduce reviewed records, including the corrected reset checks. Retained R2 effects supersede R1; seventeen callbacks and wider membership remain open. No LL15 credit. |
| Stage 1 GC counter startup storage | WITHDRAWN from implementation by user direction, 20 September; original evidence retained | [Fixture](../../tests/wasm/stage1/startup-counters/README.md). Two more startup effects: fresh owner-reserved pinned buffers for TOTAL-GC-MICROSECONDS and TOTAL-BYTES-FREED, zeroed by a checked C/Wasm service and published by generated SET. Untouched native callbacks verify zero bytes, pointer/global identity and fresh repeated allocations. Four generated modules pass 32 scenarios at 4 MiB and 2 GiB, 38 refusals and six faults. Compiler and adapter unchanged; R6/R6a reused. Production allocation/disposal, moving macptrs and statistics consumers remain open. Auxiliary proposal, not integrated or joined, no LL15 credit. |
| Stage 1 database reset admission follow-up | WITHDRAWN from implementation by user direction, 20 September; original evidence retained | [Fixture](../../tests/wasm/stage1/startup-db-review/README.md). Audit-130 F1: consistent forged and shortened DLLs isolate membership and exact-visit admission. A third case isolates shared descriptor refusal. Six directed refusals preserve the arena, outside object, publication and full TCR. All three remove-one-check mutants escape R1 and reject here; four prior faults reject. Positive record byte-identical, compiler and service unchanged. Auxiliary, not integrated, no LL15 credit. |
| Stage 1 portable startup runtime | FOLLOW-UP REVIEWED — not accepted | [Audit-131 follow-up](../../tests/wasm/stage1/startup-runtime-review/README.md): audit 132 found no defect; all four original findings closed. NIL-snapshot publication residual tested in the table follow-up. No integration or LL15 credit. |
| Stage 1 termination exclusion | ACCEPTED — reviewed components integrated | [Corrected follow-up](../../tests/wasm/stage1/termination-exclusion-review/README.md). Audit 136 closes audit-135 F1; replay reproduces 459 files at 111 pins and three additional compiled faults are refused. Cancellation, lookup and draining return exactly one NIL under the admitted empty-state invariant; registration remains refused. Actual CCL bindings, image-slot discovery, scheduling disablement and READY join remain owed. Corrected entries and guard integrated byte-exactly; real image installation remains open. No LL15 credit. |
| Stage 1 bootstrap strong tables | ACCEPTED — reviewed components integrated | Accepted after audits 132–134; policy and population builders integrated byte-exactly. [Scope and count clarification](../../tests/wasm/stage1/bootstrap-heap-census/README-errata.md). EQL/EQUAL, ordinary-population integration, growth and root installation remain open. [Termination exclusion](stage1/termination-decision.md) is accepted; corrected components are integrated. No LL15 credit. |
| Host providers, startup callback scope and foreign Wasm modules | PROPOSAL by Claude, revision HOSTFM-P2 — Codex reviewed P1 (AMEND before adoption); Claude verified its four findings and adopted them; not adopted by the user | [Proposal](host-and-foreign-modules.md): relays the user’s 20 September direction (browser is the aim; Node is a supported host; foreign modules get a separate heap). One Lisp-visible host contract with browser and Node providers and a capability table; all 17 callbacks open before the winners unit classified from their U1 source forms as runtime-internal, host-input, deferred or excluded; recommends withdrawing STAGE1-STARTUP-DB-R1 because outline §05 already excludes the interface database; fifteen requirements, a C++ worked example and nine future tests for calling foreign Wasm modules; ten questions for the reviewer. P2 corrections: a Wasm handler cannot catch a foreign trap (both reviewers’ probes agree), so foreign entries pass through a catching JavaScript boundary and multi-memory is no longer a baseline dependency; every foreign entry including the allocator is bracketed by FOREIGN and Lisp bytes are copied only while admitted; retained selection inputs are not edited, a new classification manifest binds them; a deferred callback stays an unresolved dependency until its consumers are excluded or replaced; a native image holds one interface directory with no database open, so the withdrawal of the database unit stands with a no-live-handle condition owed. No gate, ledger or shared-source effect. |
| Stage 1 database-handle startup reset | WITHDRAWN from implementation by user direction, 20 September; original evidence retained | [Fixture](../../tests/wasm/stage1/startup-db/README.md). RESET-DB-FILES now clears all seven cached handles per interface directory through an owner-admitted pinned D1 DLL service and unchanged generated calling forms. The untouched native callback supplies six cases; 96 placement/order/calling scenarios, 30 preserving refusals and four compiled faults pass. Complete DLL validation precedes mutation; all four publication words are poisoned and observed. Reuses compiler R6/R6a by hash. Auxiliary proposal, not integrated or joined; production FTD materialization and moving structures remain open, no LL15 credit. |
| Stage 1 startup clear publication completion | AUXILIARY, REVIEWED by Claude (hundred-and-thirtieth audit): no defect found, audit-129 F1a closed; replay identical over 24 files at 208 pins; two further partial-width store mutants refused; acceptance is the user’s decision | [Fixture](../../tests/wasm/stage1/startup-winners-publication/README.md). Audit-129 F1a: poison all four result words before direct CLR success. Six omitted/stale-store faults reject, including five that escape the prior guard; twelve older faults remain rejected. Positive execution is byte-identical. Compiler, service and adapter unchanged; audit-129 registry-order evidence reused. Auxiliary, not integrated, no LL15 credit. |
| Stage 1 startup publication review | AUXILIARY, REVIEWED by Claude (hundred-and-twenty-ninth audit): one defect found (F1a: the raw-clear guard follows a SET that already published NIL, 1, 0, so an operation 5 writing only word 0 passes; poisoning the result words first refuses it with an identical positive record); ordering item closed, native execution order equals the 34 snapshot ordinals; replay identical over 73 files at 204 pins; follow-up recommended before hash.c integration, the decision is the user’s | [Fixture](../../tests/wasm/stage1/startup-winners-review/README.md). Audit-128 F1: direct CLR success checks all four publication words. Three faults reproduce their complete R1 escape and reject under the corrected harness; nine prior faults still reject and the positive execution record is identical. Unchanged compiler, service, adapter and native oracle. Registry-order execution of all eighteen callbacks matches grouped R1 semantics in Node and Chromium. Auxiliary, not integrated, no LL15 credit. |
| Stage 1 startup EQ table reset | REVIEWED — audit 128 F1, follow-up awaiting review | [Fixture](../../tests/wasm/stage1/startup-winners/README.md). RESET-WINNERS native registered callback and six generated calling forms over the strong EQ backing vector. Clear preserves identity/capacity and retires buckets, cache, tombstones and moved state; collection reclaims prior keys/values. Proposed hash service extension, unchanged compiler and adapter, inherited hash corpus replayed. No LL15 credit or production hash wrapper claim. |
| Stage 1 population access | REVIEWED — audit 140 closes all remaining service-test findings | [R1](../../tests/wasm/stage1/population-access/README.md) lacks six isolated admission controls. [Follow-up](../../tests/wasm/stage1/startup-review-137/README.md) adds each refusal and a matching C mutant; binary unchanged. Not accepted; no LL15 credit. |
| Stage 1 startup image and arguments | REVIEWED — audit 138 closes the target-branch correction | [R1](../../tests/wasm/stage1/startup-host-inputs/README.md) incorrectly applies Darwin composition. [Follow-up](../../tests/wasm/stage1/startup-review-137/README.md) reads the native function with Wasm target features and preserves namespace input unchanged; 288 Node/Chromium scenarios and collections. R1 superseded, not accepted; no LL15 credit. |
| Stage 1 equality tables | REVIEWED — audit 140 closes all remaining service-test findings | [R1](../../tests/wasm/stage1/equality-tables/README.md) lacks NIL/cons and pending-depth controls. [Follow-up](../../tests/wasm/stage1/startup-review-137/README.md): 48 native objects, 73,728 comparisons, deep-key refusals and two new faults. Binaries unchanged. Not accepted; no LL15 credit. |
| Stage 1 audit 138 follow-up | REVIEWED — audit 139 closes the prior findings | [Portable replay and admission controls](../../tests/wasm/stage1/startup-review-138/README.md): root-relative dependencies, separate cons/number depth checks, five backed-region refusals, opaque static-header check and clause coverage. Services unchanged; no acceptance or LL15 credit. |
| Stage 1 population consumers | EXECUTED — not accepted or integrated | [Generated consumer entries](../../tests/wasm/stage1/population-consumers/README.md): contents/SETF/PUSH, keyword types, closures, cleanup and transfers; 22 modules, 64 native comparisons, 80 collections and four compiled faults. [Audit-139 supplement](../../tests/wasm/stage1/startup-review-139/README.md): 26 directed checks, five faults. No shared source edit or LL15 credit. |
| Stage 1 population scaffold admission | REVIEWED — audit 141, no defect | [Follow-up](../../tests/wasm/stage1/population-consumer-review/README.md): audit-140 F1, thirteen explicit refusals, eight controls, sixteen unchanged positive rewrites. Rewriter stays scaffolding, not for integration. PUSHNEW/POP and native REQUIRE-TYPE remain owed. No LL15 credit. |
| Stage 1 typed strong populations | REVIEWED — audit 141, layout redesign required | [Proposal](../../tests/wasm/stage1/typed-populations/README.md): exact two-field D1 population shape, strong copying scanner and pinned-image member roots; native weak layouts remain refused. 64 native comparisons, 80 collections, 18 shape/20 image/40 owner checks, twelve controls. No shared source changed; native accessor compilation and installation remain owed. No LL15 credit. |
| Stage 1 population PUSHNEW | REVIEWED — audit 141, no defect | [Proposal](../../tests/wasm/stage1/population-pushnew/README.md): generated branch-loop membership, shared EQL entry and callable :test; native duplicates, numeric keys, effect order, collection and exits. 27 modules, 68 comparisons, 80 collections, 40 exact allocation checks, 28 shared-service checks, seven source refusals and eight controls. Source adapter remains scaffolding; POP and native REQUIRE-TYPE/installation remain owed. No LL15 credit. |
| Stage 1 joined startup callbacks | REVIEWED — no defect after audit 128 | [Fixture](../../tests/wasm/stage1/startup-joined/README.md). Eighteen selected reset/configuration effects execute in one schedule over 21 live globals; the CPU reset and cache publication share a cell. Thirteen reset completion constants recompiled through unchanged compiler; native resets rerun, R2 configuration bytes and native answers reused. Node and Chromium at both placements: 5184 callback comparisons, 5996 invocations, 180 refusals. Full-TCR and foreign-region checks; no LL15 credit. |
| On-demand census | LL15-b/c ACCEPTED under v0.2 | [Qualification](stage0/census-qualification.md) passed Claude's fifty-ninth audit (`d507f5a7`); the user accepted both records on 16 September. The 167-unit startup worklist preserves 272 body gaps and all other unknowns. Instrument qualification does not establish exhaustive bootstrap closure. |

The new ABI fixture adds a separate C extension and new linked kernel. Existing boundary/runtime/frame sources remain unchanged. Fresh prerequisites execute before the ABI corpus. ABI acceptance now follows its own fourth audit and explicit eighteen-record decision. Original ABI development failures exposed duplicate result scanning and a stale frame result count. Both fixes are confined to the new fixture and have rejection controls.

Claude's sign-off includes the v2 evidence binding tooling and evidence repository. The acceptance producer and ABI implementation are Codex-authored; Claude independently reviewed the ABI implementation. Version 2 bindings preserve unaffected accepted records when new tests or runners are added. See [verification](evidence/verification.json), [combined gate](evidence/current-stage0-gate-result.json), [history](history/changes.md) and [next work](stage0/plan.md).

H(G) is optional future work and blocks no scheduled stage. Historical H1 archives remain explicitly unavailable where not retained; their prior claims are not promoted to U1.

The isolated [measurement fixture](stage0/abi-measurements.md) passes 486 correctness cases, 279 controls, 72 batch checks and 27 batch controls. Its original report remains NO_SELECTION: the exploratory timings do not support a speed ranking. The separate [B choice](stage0/abi-choice.md) uses simplicity and correctness, not that statistical rule. The [LL21-a correction](stage0/project-acceptance.md) preserves all original execution evidence and the 26 valid acceptances. It changes no runtime execution. Policy version 3 explicitly defers comparative timing, retaining version 2 and its numerical parameters for optional future work.

Bootstrap front-end entry: **accepted and integrated**, audit 142 (`01012704`); [integration](stage1/integration-bootstrap-frontend.json). Compiler admission 426/2492, four original definitions executed. No additional LL15 credit.

Bootstrap values lowering: **accepted and integrated**, audit 143 (`1cf0b8df`); [integration](stage1/integration-bootstrap-values.json). Eight original definitions executed against native. Historical 639/2492 admission and 82 closure counts contain audit-143 F1/F2 and must not be treated as runnable coverage. P3 adopted by the user; next: target predicates and whole-file compilation. No additional LL15 credit.

Bootstrap core: **accepted and integrated**, audit 145 (`af7c0075`) — [packet](../../tests/wasm/stage1/bootstrap-core/README.md). 1,539/2,492 in the diagnostic source census with F1/F2 guards (denominator correction remains owed); 129 original definitions execute against native in 6,936 comparisons. Both requested files compile whole (78 named definitions); all 44 callee-closed definitions in those files execute. New Wasm branches implement the predicate stubs, and generated vector/structure operations survive collection. Native R6/R6a passes with explicit source-debug metadata comparisons for the three changed native source files. Eight reviewed files integrated byte-exactly after Steve adopted the R6 source-location allowance; all three files read identically under all 17 existing targets (51 comparisons). No LL15 slot credit; 21 accepted, 12 missing of 33.

Bootstrap library: **audit 146 found a byte-store defect and a false execution claim; unaccepted, superseded**. The original [packet](../../tests/wasm/stage1/bootstrap-library/README.md) is retained unchanged. Its two string-copy definitions only compiled; they did not execute.

Bootstrap execution: **R2 ACCEPTED and integrated after audit 148**. R1 stays superseded; the accepted R2 fixes retention and carries the corrected byte-vector store and computed-callee resolution. See the final [dependency integration](stage1/integration-bootstrap-dependencies.json).

Bootstrap integer division: **ACCEPTED and integrated after audit 148**. Exact integer quotients return normally, including bignums; nonintegral quotients refuse instead of truncating. Division-by-zero/type errors match native through handlers. The integrated signal loop now resolves handler class symbols to masks correctly.



Audits 144 and 145 are imported verbatim in order. [R6 decision](stage1/r6-source-locations.json), [acceptance](stage1/acceptance-bootstrap-core.json), and [integration](stage1/integration-bootstrap-core.json) bind the user’s decision, original review hashes and the reader evidence. Carry items C-1–C-4 belong in the next substantive packet.

Bootstrap dependencies: **ACCEPTED and integrated after audit 148**. **252 original definitions execute and match native; 193 have a non-NIL witness and 59 have NIL-only inputs.** The folded final backend emits byte-identical WAT/Wasm and reproduces 9,888 comparisons, 5,270 collections, 38 checked refusals and 14 rejected faults. Fresh R6/R6a passes; all 164 FASLs restore, with 21,843 native tests reused only after exact rebuilt FASL/snapshot equality. LENGTH and REGISTER-ISTRUCT-CELL run unchanged; ASSQ, fixnum LOGAND/LOGIOR, subtraction, constant TYPEP/REQUIRE-TYPE and LDB close callers. Admission remains 1,864/2,492 historically and 1,522/1,919 in the incomplete worklist. [Acceptance](stage1/acceptance-bootstrap-dependencies.json), [integration](stage1/integration-bootstrap-dependencies.json). Member inputs, 136 closed definitions without recipes and 22 reader-error files come next; no LL15 credit.

Bootstrap witnesses: **ACCEPTED and integrated after audit 149**. [Packet](../../tests/wasm/stage1/bootstrap-witnesses/README.md). **301 original definitions execute and match native (+49), with 290 having a non-NIL witness (+97).** All previous input recipes remain. Positive cases include native arrays/locks, canonical type members and pinned raw predicate objects; generated environments, type records and restarts move under collection. The integrated architecture supplies missing D1 layout constants; the existing dispatcher selects `%I<>`/`%IZEROP` from typed source; the collector and image owner trace general nonempty ISTRUCTs. Per-case global post-state is compared. Condition/bit-vector refusal cases are retained. Three reader environments are repaired: 38/57 files read completely, 1,574/2,005 parsed definitions admitted; 19 foreign-interface failures remain, so this is still a lower bound. Historical admission stays 1,864/2,492. Fresh native R6/R6a passes with exact-hash reuse of the native tests and reader matrix. No new C/JS runtime service or CCL source edit. The remaining 80 previously closed original definitions without input recipes remain listed explicitly; LL15 stays open.

Audit 149 is imported verbatim at `d43f816f` (original `8c3a6cf5`). Steve accepted with “accept”; the earlier “accept as advised” was also his direct message. [Acceptance](stage1/acceptance-bootstrap-witnesses.json) and [integration](stage1/integration-bootstrap-witnesses.json) bind both provenance and remaining observations. The final CASE fold preserves 885 generated/result files byte for byte. All 40 inherited collector-owner checks pass; final-backend R6/R6a passes with exact-hash native test reuse. No added slot credit: 21 accepted, 12 missing of 33.

Bootstrap carry items: **ACCEPTED and integrated after audit 150**. [Packet](../../tests/wasm/stage1/bootstrap-carry/README.md). Lisp ASSQ replaces its backend loop; unknown native handler-class literals refuse during macro expansion, including macro-introduced handlers; SIGNAL/ERROR spread refusals are exercised. Written harness replaces the patch chain. **301 originals still match native, 290 with non-NIL witnesses**; admission corrects to 1,862/2,492, with 1,573/2,006 in the incomplete target list. 11,012 native matches and four declared signed-zero differences are counted separately. Fresh R6/R6a passes. The forty collector checks were completed in b9de543d and are reused by source/binary identity. Compiler and target ASSQ match the reviewed bytes; runtime unchanged. No slot change. Audit 150 imported verbatim at ecf98002; Steve explicitly answered “Accept and integrate”.

Bootstrap execution inputs and portable I/O constants: **REVIEWED in audit 151; acceptance pending; module-list removal HELD**. [Packet](../../tests/wasm/stage1/bootstrap-inputs/README.md). **332 original definitions execute (+31), 318 with a non-NIL witness (+28)**; 49 closed original definitions still lack recipes. Native returns, mutations and global post-state agree over 2,913 native rows and 11,648 target comparisons, with four separately asserted signed-zero differences; 6,192 collections. The accepted backend is unchanged. Seven source files gain Wasm seek/error constant branches; The historical proposal removed linux-files from both lists; audit 151 withdraws that advice, and those edits are held. The shared lists are unchanged. Target provider constants are declared protocol values, not host errno acquisition. The historical 56-file proposal list reads 39 files completely, with 1,582 of 2,027 parsed definitions admitted and 17 foreign read stops. Historical admission is 1,864/2,492. Fresh R6/R6a passes 21,843 tests, keeps 150 native FASLs identical and restores all 164. The reader proof compares substitutions across all 17 target profiles and restores surrounding source byte for byte; full cross-platform foreign-interface loading is not claimed. Audit-150 replay repair passes from a clean committed checkout: 885 identical files and 40 collector checks. No new runtime service or LL15 credit.


Bootstrap host execution: **ACCEPTED after audit 152; all source HELD. 349 originals executed (+17), 323 with a non-NIL return witness (+5)**; eight have error-only witnesses, which do not inflate the non-NIL count. [Packet](../../tests/wasm/stage1/bootstrap-host/README.md). Restore the 57-file worklist, retaining linux-files: 42 complete reads and 1,685/2,145 lower-bound admission. Branch 108 constant sites in twelve CCL files with 58 target protocol identifiers. Inventory all 409 foreign constant/function sites and join 100 active foreign calls to their source locations; three foreign type reads remain. Seven target OS entries compile whole and execute against explicit provider expectations, excluded from original-definition counts; CPU-COUNT remains a named GLOBAL-SETQ refusal. Of 49 prior closed originals without recipes, 17 execute and 32 have named remaining dependencies; the review's seven additional fixture/primitive names are separate. All shared compiler/runtime/Lisp files remain unchanged. Fresh native R6/R6a: 21,843 tests, 146 identical FASLs, 164 restored; 204 compositional reader comparisons. No slot credit or claim of a ready image.

Proposal `3d8fb085` replays unmodified from its clean detached checkout: **1,127 deterministic files identical at 260 source pins**. Evidence commit `51a8981e7f7a7277a1db574be4ebcf2aec5ff5ee` binds the packet and verification. The final current index is byte-identical to its retained snapshot, closing audit-151 F1. Audit 152 reviewed execution with no defect. Steve explicitly accepted with “Go ahead and accept”; acceptance is limited to execution evidence. Every source change remains held under F1.

[Execution-only acceptance](stage1/acceptance-bootstrap-host-execution.json) binds audit `9518d154`, imported verbatim as `a08a0a72`, and Steve’s authorization. No compiler, runtime or CCL source is integrated. Excluded subsystems must be removed at their calling definitions, without POSIX protocol constants; the next proposal must re-cut those rows and add compiler admission and execution together. Routing capabilities remain a prototype: scheduler blocking and owner authority are not qualified. The Stage 1 ledger remains 21 accepted, 12 missing, zero unreviewed.

Bootstrap admission and foreign boundaries: **ACCEPTED and integrated after audit153. 382 original definitions execute and match native (+33); 355 have non-NIL witnesses (+32).** [Packet](../../tests/wasm/stage1/bootstrap-admission/README.md). The 57-file worklist reads 44 files completely and admits 1,983/2,231 parsed definitions (still a lower bound). New AUX/LEXPR binding, global SETQ, constructors, shifts and typed access execute through unchanged CCL definitions. All seventeen prior TYPE-ERROR compiler crashes become explicit native-FFI refusals. Native function-immediate reflection remains a named layout dependency. 13,336 target comparisons comprise 13,248 native matches, four declared signed-zero differences and 84 protocol comparisons; 96 internal allocation retries collect. No new C/JS service; the reviewed compiler, architecture and source are now integrated.

Audit152's foreign-source scope is re-cut: 409 sites, 124 excluded with their definitions, 169 existing-platform branches, 61 constant substitutions using 31 retained identifiers, and 55 active foreign functions. Remove all nineteen excluded-subsystem constants plus eight boundary constants. Keep linux-files in both lists. Compiled CPU-cache and wait callers qualify only the routing prototype, not production scheduler/capability authority. R6/R6a passes 21,843 tests, keeps 146 FASLs identical and restores 164; edited native code bytes and non-location data agree. Debug-note presence and empty debug-info slots are recorded under the source-location allowance; 204 existing-target reader comparisons pass. No LL15 credit: 21 accepted, 12 missing, zero unreviewed slots.

Proposal `93603a17` replays unmodified from a clean detached checkout: **1,247 deterministic files identical at 267 source pins**. Evidence commit `fda758ccf29fcb36a4e9d8f9694f91e1a423b551` binds the new packet, clean verification and byte-identical index snapshot. All reviewed/accepted records and slot counts are unchanged.


Audit **153** (`2a724011`, imported verbatim as `0e0bb4a2`) found no defect. Steve authorized “accept and integrate.” [Acceptance](stage1/acceptance-bootstrap-admission.json) and [integration](stage1/integration-bootstrap-admission.json) bind the review and files. The two architecture macros moved intact into the architecture file. The installed compiler reproduces **1,152** reviewed files; final-file R6/R6a passes **21,843** tests, **146** identical and **164** restored FASLs. Runtime files and the held OS prototype are unchanged.

Bootstrap math and execution: **PROPOSED; 423 original definitions execute (+41), 394 with non-NIL witnesses (+39)**. [Combined packet](../../tests/wasm/stage1/bootstrap-math/README.md). The actual target list admits **1,993/2,231** parsed definitions, with **44/57** files read completely, still a lower bound. New portable coercion/word-shift lowering, unsigned-word logical arithmetic, and recipes execute original CCL bodies. A missing ARRAY-ELEMENT-TYPE target arm is repaired in CCL's source. The review's 89 closed names comprise 82 originals and seven fixture/primitive names; 38 of those originals now execute, and 44 have individual remaining dispositions.

All **26** single/double transcendental primitive entries (22 foreign-call sites plus four equivalent external-call sites) execute through an isolated extension of the existing float service. **504** finite native comparisons have **68 one-ULP differences** and are kept separate from exact execution credit. Steve has adopted pinned musl and a two-ULP comparison limit, with signed zeros, exact results and domain conditions exact. This numerical decision does not itself accept the implementation packet. Checked inexact/underflow trap modes refuse. Raw arithmetic operations retain their 59,083-case regression; independent libm checks cover 12,480 sampled calls. Target 30-bit versus native 61-bit shifts have 756 asserted comparisons and 212 declared width differences. No speed or full-domain numerical claim.

The combined packet binds the accepted integration and leaves the new compiler, CCL source and runtime proposal under tests for independent review. Native qualification of the new proposal passes 21,843 tests, with 144 identical/164 restored FASLs and 34 new existing-target reader comparisons. No LL15 credit: the ledger remains **21 accepted, 12 missing, zero unreviewed**.


Combined implementation `cef139cf` replays unmodified from its clean detached checkout: **1476 deterministic files identical at 355 source pins**. The documented admission integration command also passes there, reproducing 1,152 reviewed files. Evidence commit `46ac290f0a62d57c2e9320b469f91debf87f29f0` binds both results and the byte-identical current index snapshot. New math work remains proposed for independent review; slot counts are unchanged.


Audit **154** (`12f738e3`, imported verbatim) verifies the admission integration and finds one math harness defect: FP traps were selected by case name. Math R2 sets native default mask 7 for every case and adds an ordinary-name condition witness plus the old-harness failure control. The compiler/source/runtime proposal is unchanged; native R6/R6a is reused by exact source hash. The numerical limit is recorded in acceptance.md on Steve's “Adopt musl and two ULP”. The present closed frontier has 77 names without inputs; the earlier 44 was only the remainder of the previous cohort. Immediate-bignum uint32 type proof remains owed. No new original execution or slot credit; R2 awaits independent review.

Math R2 `897adfea` passes the documented verifier from its clean detached checkout: **1481 deterministic files identical at 356 pins**, 15,636 generated comparisons, and the old case-name trap switch rejected. No new original-definition or slot credit. Evidence `c0d2b18c204c28d32a1442c2cc4d7f55d5a240ae`.


Audit **155** (`2cc3d73d`, imported verbatim) finds no defect in math R2. Steve authorized “accept, integrate and proceed”. The reviewed compiler, CCL source branches and floating runtime are integrated, with pinned musl sources and licence. Final-file R6/R6a passes **21,843** native tests, **144** identical FASLs and all **164** restored. The production float build is byte-identical to the reviewed binary; generated execution matches R2 and all **40** owner checks pass. [Acceptance](stage1/acceptance-bootstrap-math.json), [integration](stage1/integration-bootstrap-math.json), and [runner](../../tests/wasm/stage1/bootstrap-math-acceptance/README.md). No LL15 slot credit.


Closure execution and unsigned masks: **PROPOSED; 436 original definitions execute (+13), 407 with non-NIL witnesses (+13)**. [Packet](../../tests/wasm/stage1/bootstrap-closure/README.md). Admission remains **1,993/2,231**, with 44/57 complete files; 64 current closed names have no inputs. Immediate unsigned 32-bit literals now enter the existing checked word-logic lowering, and MINUS1 has a source-emitted witness. Thirteen original destructive double-float primitives run against native compiled entries with independent input snapshots and observed destination mutation. **15,888** target comparisons include **14,540** exact native comparisons; inherited numerical differences remain separately declared. Seven faults and the old trap-mode harness are rejected. Final-backend native R6/R6a passes **21,843** tests, 144 identical/164 restored FASLs. No runtime or CCL source changes and no LL15 credit.


Proposal `5612391e` passes its documented verifier unmodified from a clean committed detached checkout: **1506 deterministic files identical at 354 pins**. The documented math integration command also passes there, including the forty owner checks and the reviewed binary/execution identities. No further execution or slot credit; the new closure proposal awaits Claude review.


Bootstrap recipes: **PROPOSED; 458 original definitions execute (+22), 427 with non-NIL witnesses (+20)**. [Packet](../../tests/wasm/stage1/bootstrap-recipes/README.md). Twenty-one of the requested 64 names now execute; whole-file hash compilation adds NEED-USE-EQL as the twenty-second. The cohort also contains eight fixture/target primitives, thirteen 32-bit single-float interfaces with existing counterpart coverage, nine excluded native interfaces, and thirteen entries needing lowering, graph/function/frame state, target representation or a native-source repair. All 64 have individual dispositions. The current unexecuted static list is 43; it is not 43 ordinary missing input tables.

New callers observe array metadata, multidimensional indices, returned closures after collection, dispatch-table state, stream buffers and indices, symbol state, a TOPLEVEL transfer and detached thread state. CCL's file compiler processes l0-hash.lisp: 76 of 95 records admit, with named refusals retained. This repairs INVALID-HASH-KEY-P's missing file-local symbol-macro environment; restoring the standalone output fails the marker test. **16,340** target comparisons include **14,992** exact native comparisons; inherited numerical differences remain separate. Eight faults and the bad trap-mode control are rejected. Compiler, runtime and CCL source are unchanged from the previous proposal, so its native qualification is reused by hash. Admission is 1,986/2,231 with 44/57 complete files: the real hash-file macro environment exposes seven unsupported %ILLEGAL-MARKER uses previously counted as global reads. The retained README's inherited 1,993 figure is corrected in the [errata](stage1/bootstrap-recipes-errata.json). No LL15 credit; Stage 1 remains 21 accepted, 12 missing of 33, zero unreviewed slots.


Proposal `7267519e` passes the documented verifier unmodified from a clean committed checkout: **1,709 deterministic files identical at 356 pins**. Native qualification is reused by exact final-source hashes. The retained summary correctly reports 1,986/2,231 admissions; the README inherited 1,993 from its predecessor. The [errata](stage1/bootstrap-recipes-errata.json) records the seven newly exposed %ILLEGAL-MARKER refusals after loading the hash file's real macro environment. Execution remains 458 originals, 427 with non-NIL witnesses.


Audit **156** (`5fe0ce3c`, imported verbatim as `57df0b76`) verifies the math integration and finds no defect in the closure or recipes packets. Both are recorded as reviewed; acceptance is still pending. Its F1 identifies standalone compilation without file-local macro environments. Historical admission 1,986/2,231 is an upper bound, replaced by the recount below.

Bootstrap file environments: **PROPOSED; 460 original definitions execute (+2), 429 with non-NIL witnesses (+2)**. [Packet](../../tests/wasm/stage1/bootstrap-file-environments/README.md). Real file compilation corrects the comparable cohort to **1,895/2,231**; 98 formerly admitted definitions now refuse, seven formerly refused ones now lower. The compiler encounters 893 additional definitions, separately reported: 2,747/3,124 total DEFUN records lower. All 57 files run in fresh processes; 44 complete, thirteen retain explicit stops. All 57 audit-156 macro-callee cases are dispositioned, including six outside the target cohort. None of the thirteen suspect macro names remains a callee.

The three numeric files compile to the end: **147/224** definitions lower, **155** modules assemble, and **21** definitions execute through their actual whole-file outputs in **644** native comparisons. The full inherited run passes **16,560** target comparisons. MULTIPLY-FIXNUMS and UPGRADED-COMPLEX-PART-TYPE are the two additional executions. Native numeric file compilation supplies the source-stage oracle; a retained failed run exposed the saved image's later UPGRADED-COMPLEX-PART-TYPE definition. Three omitted compile-time environment controls and the empty/self-only body guards pass. Shared compiler, runtime and CCL sources are unchanged; native qualification is reused by exact source hash. LL15 remains open, with 21 accepted and 12 missing of 33 criteria.


Proposal `13388525` passes the documented verifier unmodified from a clean committed detached checkout: **2,261 deterministic artifacts identical at 369 pins**. The 57-file recount, compile-time environment controls, numeric module assembly and all 16,560 target comparisons reproduce. Native qualification remains reused by exact source hashes. No additional execution or slot credit.


Audit **157** (`01f01d98`, imported verbatim as `a82d31ed`) finds no defect in the file-environment recount and execution evidence. It is recorded as reviewed, with acceptance still pending. Its next-work observations identify ten missing arch macros, eleven foreign reader stops and the funcallable-instance immediate layout.

Bootstrap arch proposal: **1,950/2,231 admitted (+55); 463 originals executed (+3), 432 non-NIL witnesses (+3)**. [Packet](../../tests/wasm/stage1/bootstrap-arch/README.md). Six numeric/identity macros are implemented; the four native kernel/function-vector macros have explicit capability/layout refusals. Native float construction, copy/store and fixnum-conversion paths execute through existing helpers and services. Whole-file numeric admission is **199/224**, with 207 modules assembled and 24 numeric definitions executed. **16,728** comparisons pass, including 100 direct arch checks; symbol hashing has an explicit x8632-versus-x8664 representation observer. The initialized WITH-STACK-SHORT-FLOATS caller remains unexecuted because its full conversion dependency is open.

Two native FFI pointer definitions are excluded under target reader branches; **46/57** files now read to completion. The other nine foreign stops remain named implementation obligations, not fabricated foreign entries. [O-5 layout proposal](../../tests/wasm/stage1/bootstrap-arch/function-immediates.md) awaits the user's decision. Final R6/R6a passes 21,843 tests and restores all 164 FASLs. Shared compiler/runtime/Lisp sources are unchanged; no LL15 credit or new acceptance.


Proposal `06066644` passes its documented verifier unmodified from a clean committed detached checkout: **2,506 deterministic artifacts identical at 391 pins**. The recount and all 16,728 comparisons reproduce; final native qualification is reused by exact compiler/arch/source hashes. No extra execution or slot credit.


Bootstrap hyperbolic proposal: **467 original definitions execute (+4), 436 with non-NIL witnesses (+4)**. [Packet](../../tests/wasm/stage1/bootstrap-hyperbolic/README.md). Comparable admission remains **1,950/2,231**; **47/57** files now read completely. `l1-numbers.lisp` compiles whole with **67/68** definitions admitted. The ASINH/ACOSH/ATANH foreign stop is replaced by six target entries on the existing pinned musl float service. Four unchanged destructive double-float arithmetic definitions supply the new execution credit. No host Math imports or fabricated foreign readers.

The generated run passes **17,064** comparisons, including **176** new hyperbolic comparisons (one ULP observed maximum) and **80** exact condition comparisons. Raw sampled checks pass **15,360** comparisons plus **837** directed checks; all **177,249** accepted primitive comparisons pass. Three changed-site faults are rejected. Final R6/R6a passes **21,843** tests with **164** FASLs restored and the reader inverse proof covers 17 existing target profiles. This is an unintegrated proposal over the arch packet; no acceptance or slot credit. Eight foreign reader stops, the boot kernel-global/TIMEVAL stops and the pending generic-function layout remain open.


Hyperbolic retention R2 corrects a packaging failure caught in the clean-checkout verifier: symlinked fault-control inputs are stored as ordinary file bytes. All **3,076** deterministic artifact hashes are unchanged; R1 remains retained as superseded. No compiler, runtime or source behavior changed.


Hyperbolic R2 at `34842a31` passes its documented verifier unmodified from a clean committed detached checkout: **3,076 deterministic artifacts identical at 435 pins**. Whole-file recount, all generated and raw comparisons, three fault controls and the 17-profile reader proof reproduce. Native evidence is reused by exact final compiler/arch/source hashes. No additional execution or slot credit.
