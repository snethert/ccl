# Current status — 2026-09-11

Implementation baseline: upstream v1.13, `c994217adc56b3f8a564526cee4695893ac84d86`. Current document set: outline 0.16, acceptance 1.6, decisions 1.7. Historical files and evidence claims are preserved separately.

| Work | State | Evidence scope / remaining work |
| --- | --- | --- |
| Baseline selection and document changes | Implemented | Baseline manifest, clarified R6, initial contracts, subgates, benchmark policy, machine inventories and generated DOCX copies. Document consistency is checked by tools/manage.py. |
| U1 native Gate 0 | Executed PASS; NOT ACCEPTED | [Native summary](evidence/native-baseline-summary.json): two clean macOS x86-64 kernel/Lisp builds; 21,843/21,843 eligible release-corpus tests pass per run, with 75 upstream-disabled tests recorded. All 164 FASLs are byte-identical. The [2026-head diagnostic](evidence/native-current-tests-diagnostic.json) separately retains three failures on unchanged U1. Claude independently re-executed the pinned runner on 2026-09-11: identical pass counts and all 164 FASL hashes identical to r3; the 2026-head diagnostic reproduced its same three failures. NOT ACCEPTED. |
| Historical H1 E1–E5 | Reported historical results; not independently verified here | Packs/status record are absent. Locator and full-hash fields remain null in evidence/index.json. S0-LL08-c now qualifies U1 on a second Mac and remains NOT RUN; the supersession is recorded in history. |
| Qualified native census / bootstrap closure | NOT RUN | Schema, conservative closure checker and explicit acceptance IDs exist. Actual native instrumentation and its omission mutants remain to be implemented/executed. |
| Layout / debugger-frame contracts | S0-LL23-b executed PASS; r3 independently reviewed; NOT ACCEPTED | [Frame proof](stage0/debug-frames.md): eleven positive cases and eleven rejection controls, including eight frames through moving GC, lexical/source identities, unavailable values, nested debugging and EH restoration. Claude reproduced all 49 r3 objects/modules byte-identically and found no defect. r5 verifies the new evidence wrappers; those wrappers await independent review. The shared-cell case proves relocation with a permanent root, not capture semantics. Full object schema and generated-frame coverage remain outstanding. |
| Initial hand-built Wasm probes | Executed: 3 PASS in r7 | [Retained results](evidence/runs/2026-09-11-probes-r7.zip): cons fixture, profile materialization and late-Worker sentinels only. The initial failed run and intermediate runs are archived; PROBE results cannot satisfy complete S0 IDs. r7 binds the C/C4/B-only inventory. Claude re-executed the unchanged r5 sources on 2026-09-11 with byte-identical modules; that historical review remains distinct from current execution and acceptance. |
| C/runtime boundary slices S0-LL13-c and S0-LL19-b | Executed: 2 PASS; NOT ACCEPTED | [Fresh boundary and runtime prerequisites](evidence/runs/2026-09-11-debug-frames-r5.zip) and [runner](../../tests/wasm/stage0/runtime-boundary/README.md). Real freestanding C, linker-derived ownership, concurrent live C frames, final Wasm EH, 12 boundary cases and 9 rejection controls on Node/V8 on macOS. Claude reviewed r8, then re-executed the r10 prerequisite run on 2026-09-11 with byte-identical modules and matching case/control counts; no defect found. NOT ACCEPTED: acceptance is a separate project decision. |
| D3 ABI selection / product risks | OPEN | No C/C4/B candidate has been qualified or benchmarked. [Scale, startup and module granularity](stage0/product-risk-plan.md) require coupled measurements and stated assumptions. Numerical ABI policy is predeclared; product startup/memory budgets and production-sized evidence are absent. |
| Integrated D4/D5 runtime slices S0-LL20-a/b/c | Executed: 3 PASS; NOT ACCEPTED | [Execution summary](evidence/integrated-runtime-summary.json) and [scope/review](stage0/integrated-runtime.md). A cons-only copying collector moves cyclic graphs across live C/emitted roots, nested binding frames and complete result regions. Controlled schedules cover collector ownership, admission, child handoff, code installation, interruptible I/O, nested requests and reclamation. Current frame-proof prerequisites: 22 positive cases, 14 rejected controls and 1,000 seeded schedules, freshly executed from unchanged runtime source. Both earlier C-boundary slices pass again in the same run. Claude's second external audit (2026-09-11) re-executed r10 byte-identically, confirmed the park-on-exception fix, the new idle-collection case and the omitted-park control, and found no further defect; NOT ACCEPTED. |
| Stage 0 acceptance | BLOCKED | Required acceptance runners/results and reviews are absent; document generation and initial probes do not pass the gate. |

See [dated history](history/changes.md) for changes rather than inferring completion from document versions. See [Stage 0 plan](stage0/plan.md) for the next implementation work and [evidence index](evidence/index.json) for retained execution records.

Document projections and original hashes pass consistency checks. The three DOCX files were parsed by macOS textutil. The checker suites pass 32 existing controls and 42 contract-binding/migration controls. The Wasm checks do not qualify native CCL, the complete engine feature matrix, the production collector or D3; native execution has its own evidence. Claude’s debugger-frame audit covers r3, with 49 identical rebuilt modules/objects and no defect. The new evidence-binding tooling awaits independent review. Original frame/native r3 reports were upgraded into separate v2 envelopes after verifying their artifacts; their execution timestamps and review dispositions are preserved, with no new native execution. The [combined current gate result](evidence/current-stage0-gate-result.json) remains BLOCKED for missing results and acceptance reviews. Superseded probe/boundary records retain their original hashes in the external store; unchanged r7 probes are labeled CURRENT_DIAGNOSTIC and cannot discharge acceptance IDs. See [Claude’s audit dispositions](stage0/claude-review.md), including the second audit of r10, r5 and native r3 on 2026-09-11.

H(G) is optional future work under the user’s explicit instruction; it has no scheduled gate or generated-code deadline. The [evidence repository](evidence/repository.json) is committed locally with an immutable file catalog, exact Git-blob verification and original review dispositions retained; the pinned manifest records the current counts. No remote backup is configured.

Version 2 [evidence bindings](contracts/evidence-binding.md) retain original inventory snapshots and compare each test plus its transitive prerequisites against the current contract. Adding an unrelated test no longer forces existing Wasm/native evidence to be rerun or re-bound. A test/version change invalidates the affected evidence; missing new results still block the gate. The [verified migrations](evidence/debug-frames-verification.json) demonstrate this against the actual seven-record aggregate.
