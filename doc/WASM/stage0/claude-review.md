

## Sixth Claude audit — reversible native observation, NATIVE-CENSUS-r5 at 3ce72d35 — 12 September 2026

Reviewer: Claude Fable 5.1, in its own session on the reference host. Author: Codex, under the 12 September standing-rule exception for reversible startup instrumentation. Scope: `tests/wasm/native-census/` in full (`observation.patch`, `patch.json`, `observer.lisp`, `probe.lisp`, `inspect-native.lisp`, `run.py`, `reversible.py`, `record.py`, `analyze.py`, `trace-startup.py` and the four self-tests), `doc/WASM/stage0/native-census.md`, the S0-LL08-b/native envelope and the retained r5 evidence. Reviewer disposition only; project acceptance is separate.

### Execution evidence

Fresh run at HEAD in the reviewer's session from the pinned macos-u1-inputs into a separate disposable tree: execution PASS; both native suites 21,843 / 21,843 with 0 failed, missing or unexpected; 161 of 164 FASLs identical during observation with exactly the three patched files differing; all 164 FASLs identical after removal and clean rebuild; every archived source file restored; 167 clean outputs restored; the work tree's observation state reads inactive with all original digests matching. Against retained r5: all 164 FASL hashes identical in the baseline, observed and restored-clean builds; the evaluated snapshot and the 35-callback cold-start sequence identical; `analyze.py` yields the same counts (279 slots, 12 reserved, 599 templates, 51,342 function observations, 164 sources, 103,313 call forms, 3,647 unknown targets). Kernel and image bytes differ from r5, as they already did between the two accepted Gate 0 builds; the probe FASL differs from r5 only through its embedded source path and is identical between the reviewer's own baseline and observed compilations. `test-observer.py` against the reviewer's build: one positive, six rejected, identical probe FASL. `test-reversible.py` (8 controls), `test-clean-outputs.py` (3) and `test-record.py` against r5 (6 rejections) pass. The three original file digests in `patch.json` equal the U1 git blobs at c994217a. The r5 artifact list verifies (563 files), the producer copy of `record.py` matches the committed file, the 26 prior accepted records are unchanged apart from the disclosed path rebasing, and the gate is BLOCKED with 22 missing and one unreviewed. The main checkout has no change outside `doc/WASM`, `tests/wasm` and `CLAUDE.md`.

### Mechanism review

- **The patch is observation only.** Seven hook sites in three files, each guarded by `boundp` and a non-nil test on `ccl::*startup-census-hook*`, so an image without the observer never calls anything. The two sites that wrap an evaluated form (`%compile-time-eval`, the startup callback loop) use `multiple-value-prog1`, so values and the surrounding restart are preserved. No site touches acode, operator tables, backend state or results. The disassembly delta of the six patched functions is hook loads, `BOUNDP` calls and label renumbering.
- **Reversal is real and defended.** `ObservationUnit` refuses a Git checkout or symlinked source, checks all three original digests before touching anything, persists recovery metadata before the first mutation, applies with `git apply --check` first, verifies the observed digests, and on any exit restores from the backed-up originals and re-verifies. Recovery accepts only known original or patched bytes and refuses unexplained edits or damaged backups. The runner then restores the 167 clean outputs, rebuilds from the original bootstrap and requires 164 identical FASLs, and asserts the hook symbol is absent from the restored image.
- **The R6 comparison is fair.** Baseline and observed builds both start from the original bootstrap image, both load the same three compiled files first, and both rebuild with the same declared gensym seed; the observer isolates its own gensym use and blocks re-entry. No normalization is applied. The r2 development failure, where observer DEFVARs shifted later gensym names, is retained and was fixed by removing the initializers rather than by normalizing output.
- **The observations are checked against independent controls.** The probe requires named functions, a direct dependency, an unknown indirect target, macro expansion, a compile-time effect and multiple values, and six injected observer mutations are each rejected while the probe FASL stays byte-identical. The analyzer validates sequence numbers, a single completion, operator ID and flag consistency with the evaluated lookup, reserved-slot emptiness, and callback order equal to the evaluated registrations.

### Findings

No defect found. Six observations, none blocking:

1. **Codex amended the standing rules.** The commit adds a "clean implementation start" rule to `CLAUDE.md` attributed to a user clarification. The rule is consistent with the user's direction, but the reviewer cannot verify the attribution; the user should confirm it.
2. **Four harness files changed after r5 ran** (`test-reversible.py`, `test-observer.py`, `trace-startup.py`, `README.md`) and three were added (`record.py` and two tests). The changes strengthen controls and retention and are disclosed in the history; the six files the producer pins are byte-identical to the r5 runner copy, and the retained producer copy matches the commit.
3. **Kernel and image bytes are not reproducible across runs.** FASLs are. Any future claim about images must rest on FASL identity or a deterministic kernel build, not on image hashes.
4. **67 operator slots never appear in the build observation** (12 reserved plus 55 defined). This is census input, not a defect, but the closure work must not treat "observed during rebuild" as the reachable set; the analyzer already reports both lists.
5. **The external cold-start file trace remains unexecuted.** The runner is correct in structure, holds the client before exec and refuses to count an untraced run, but it needs administrator access that the agent session lacks. This is the one piece of the startup record only the user can produce.
6. **Startup coverage begins at the callback loop.** Runtime and stream initialization before `restore-lisp-pointers` reaches its callback list is outside the hooks, as the report states.

### Disposition

| Record | Reviewer disposition |
| --- | --- |
| S0-LL08-b, variant native (NATIVE-CENSUS-r5) | REVIEWED_NO_DEFECT_FOUND_NOT_ACCEPTED, at the declared scope: evaluated operator IDs, flags and reserved slots unchanged under a reversible observation patch, with two passing native suites and 164 identical FASLs after removal. Reproduced independently. |
| Build, probe, cold-start and test event logs | Reviewed as census inputs; they do not discharge S0-LL15-b or S0-LL15-c, which still need the seed set, conservative closure, joins, initializer semantics and the external trace. |

Not covered by this audit: the external file trace, the complete census graph, the census checker against a real graph, and any Wasm backend work. The implementation baseline remains pristine U1.

### Follow-up to the sixth audit

The user confirmed the `CLAUDE.md` clean-start line, resolving observation 1, and accepted S0-LL08-b at its stated scope. Claude produced the acceptance envelope at the user's direction; that operation is recorded in `stage0/project-acceptance.md` and is not part of the audit.

Correction: the sixth-audit report and the reviewer's notes described S0-LL21-a as accepted, carrying forward an acceptance whose authorization quote the fifth audit had already flagged as reviewer text. The user has since clarified that no such acceptance was given and Codex withdrew it (db395218). S0-LL21-a stands at REVIEWED_NO_DEFECT_FOUND_NOT_ACCEPTED; the 27-record aggregate the reviewer composed for LL08-b is historical, and the corrected 26-record aggregate is current.

## Seventh Claude audit — dependency extension, external trace and the LL21-a correction, at db395218 — 12 September 2026

Reviewer: Claude Fable 5.1, in its own session on the reference host. Author: Codex, under the reversible-observation exception. Scope: `tests/wasm/native-census/dependencies.lisp`, `dependency-probe.lisp`, `redefinition-probe/`, `dependency-graph.py`, `reconcile-trace.py`, `test-dependencies.py`, `test-trace.py`, the `run.py` and `trace-startup.py` changes, `doc/WASM/stage0/native-dependencies.md`, the r6/r7 native runs, the Terminal trace r2 and its reconciliation, and the LL21-a acceptance correction. Reviewer disposition only.

### Execution evidence

Fresh r7-equivalent run at HEAD in the reviewer's session with `--observer-extension`: execution PASS; both native suites 21,843 / 21,843; 161 of 164 FASLs identical under observation with exactly the three patched files differing; 164 of 164 after removal; all sources and 167 clean outputs restored; the graph built from the reviewer's own build log matches Codex's analysis r3 in every summary count, including 55 cross-layer definition groups. Against retained r7 and r5: all 164 FASL hashes identical in the baseline, observed and restored-clean builds; the extension hash in the report equals the committed `dependencies.lisp` and the retained runner copy; all 573 r7 artifact identities verify. `dependency-graph.py` run by the reviewer over the retained r7 build log produces a `graph.json` byte-identical to Codex's analysis r3 with the same summary counts. `test-dependencies.py` against the reviewer's own r5-equivalent build: two native variants, four identical FASLs, eight controls rejected, repeated empty observation preserves dependencies. `reconcile-trace.py` over the retained Terminal trace reproduces Codex's reconciliation exactly; `test-trace.py` passes with the actual trace positive and five mutants rejected. Document, evidence, acceptance and checker tools pass; the gate on the corrected aggregate is BLOCKED with 22 missing and one "unreviewed", which is LL21-a awaiting a project decision. No file outside `doc/WASM`, `tests/wasm` and `CLAUDE.md` differs from v1.13.

### Mechanism review

- **The extension is observation only.** It redefines four observer functions in the observer's own package and reads compiler objects: afunc identity, parent, variables, acode call operands, the evaluated builtin table, `fboundp` and `macro-function` states. It appends to the observer's JSON records, never to acode. The three-file source patch is unchanged (same hash). Identical FASLs across all three builds and the four probe FASLs confirm no output effect.
- **Identity, not names.** A weak EQ table assigns identities to afuncs, root variables and call-site nodes, so two same-named lexical functions are distinguished and a self call targets its own afunc. Self calls are read correctly: the first operand is the argument list. Builtin calls are resolved through the evaluated builtin vector and cross-checked by the graph builder against the snapshot. Anything not exactly resolvable is kept as an unresolved category with no fabricated targets, and the graph builder rejects targets on unresolved calls, unknown categories, operator/category disagreement, missing callee bodies and name/identity disagreement.
- **Declarations versus installations.** Definition forms are recognized from top-level and expanded forms only, never from quoted data or function bodies, and the probe checks that a quoted DEFUN is not reported. Native binding state is sampled at source transitions, initializer entry and return, and explicit load checkpoints, with previous state and sample interval retained; the builder rejects discontinuous histories. The report is explicit that compilation order is not load order and that samples miss transient replacements.
- **The trace.** The v2 tracer holds the launcher before exec, filters on both the PID and a byte-identical uniquely named kernel copy, and requires a pre-exec read, a post-exec read inside CCL, the image open, a matching native PID, clean exits and no loss report. The reconciler parses every line, excludes other processes, classifies host loader and device activity, and reports the four remaining anonymous or relative dyld path contexts rather than dropping them. The trace shows one image open and no source or FASL reads, as expected for a saved image.
- **The correction.** The corrected aggregate differs from the previous one only in LL21-a's disposition and review pointer; all 26 other result objects are byte-equal, including LL08-b. Original envelopes and the withdrawn decision are retained.

### Findings

No defect found. Four observations, none blocking:

1. **The LL21-a acceptance was never the user's.** The fifth-audit observation flagged the quote; Codex has now recorded the user's clarification and withdrawn it. The reviewer's own sixth-audit follow-up and memory repeated the misattribution and have been corrected. An acceptance quote must be verified as the user's own words before an envelope is produced.
2. **`dependency-graph.py` and `README.md` changed after r7 ran.** The retained runner copy is the earlier builder; analysis r3 and the reviewer's byte-identical rebuild use the committed one. Disclosed through the r1 to r3 analysis history.
3. **Unresolved work is large and honestly stated.** 1,729 dynamic call sites, 951 referenced bindings with no compiled candidate in this capture, and four loader path contexts remain. None of this discharges S0-LL15-b or S0-LL15-c.
4. **No inventory slot covers this evidence.** The dependency and trace records are census inputs indexed as diagnostics. They earn gate credit only through the LL15-b/c records once closure exists.

### Disposition

| Record | Reviewer disposition |
| --- | --- |
| NATIVE-DEPENDENCIES-r7, DEPENDENCIES-GRAPH-r3, TRACE r2 and reconciliation r2 | REVIEWED_NO_DEFECT_FOUND, diagnostic census inputs; no gate acceptance applies. |
| LL21-a acceptance correction | VERIFIED: only the LL21-a disposition changed; S0-LL21-a remains REVIEWED_NO_DEFECT_FOUND_NOT_ACCEPTED pending the user's decision. |

Not covered: the LL15-b/c closure algorithm, the census exchange format, generated code. The implementation baseline remains pristine U1.

### Follow-up to the seventh audit

The user accepted S0-LL21-a directly in the reviewer's session. Claude produced the acceptance envelope at the user's direction; recorded in `stage0/project-acceptance.md`, not part of the audit.

## Eighth Claude audit — dyld context classification and clean-image candidates, at eaa0a273 — 12 September 2026

Scope: `reconcile-trace.py` and `test-trace.py` changes, `startup-closure/` (inspector, candidates, seeds, run, controls), `native-loader-contexts.json`, `startup-candidates-summary.json`, retained packet r1. No shared source changed; checkers and gate unchanged (27 accepted, 22 missing, 0 unreviewed).

Reproduced in the reviewer's session: the candidate packet from the retained r7 kernel and clean image is byte-identical to r1 in all four output files; the retained artifact hashes and every input identity match the committed files; the 14 candidate controls and 16 trace controls pass; the reconciler classifies the four dyld operations and leaves none unresolved. The five explicit pre-callback seeds are the literal first statements of `restore-lisp-pointers`, and the other seeds name real definitions.

No defect. Three observations, none blocking: the dyld rule is a fingerprint of this host's loader sequence, which is acceptable because everything before the image open is by construction pre-Lisp; the candidate universe is deliberately the full superset with no address-taken pruning, so it is sound but not yet informative for the dynamic calls; and 183 referenced names have no candidate in either capture and are the next worklist. Disposition: REVIEWED_NO_DEFECT_FOUND, diagnostic census inputs, no gate credit.

## Ninth Claude audit — census stub registration, S0-LL08-a at 21c6aed7 — 12 September 2026

Scope: `tests/wasm/native-census/stub-backend/` in full (registration unit, patch, two payload sources, session, corpus, snapshot, runner, producer, oracle and tests), `doc/WASM/stage0/stub-registration.md`, the CENSUS-STUB-REGISTRATION-r1 packet and its envelope. No shared source changed in the checkout; checkers pass; the standalone record is not yet composed into an aggregate, so the gate still lists S0-LL08-a as missing.

Reproduced in the reviewer's session from the pinned inputs and the retained r7 kernel: PASS; both native suites 21,843 / 21,843 with 75 disabled; 163 of 164 FASLs identical while registered with only `bin/systems.dx64fsl` differing; 164 of 164 after removal and clean rebuild; all 164 FASL hashes, the changed systems FASL, both census module FASLs, the evaluated snapshot and the fourteen target-session rows byte-identical to r1; the same ten target outcomes. The six unit controls and eleven producer controls pass in the reviewer's session; the patch's before-hash equals the U1 blob of `lib/systems.lisp`; all 74 retained artifacts and the executed fixture and dependency identities verify.

Mechanism: the unit adds two module records to `lib/systems.lisp` and two files under `compiler/WASM-CENSUS/` in a disposable copy only, with ownership, identity and unexplained-edit refusals mirroring the observation unit. The backend registers a real `backend` struct with a D1 subset `target-arch`, zero argument registers for B, and a pass-2 entry that throws the front-end function object to a capture tag and errors if pass 2 ever returns, so no target code can be emitted. Sessions establish backend, features, FASL target, foreign data and TARGET/OS nicknames through the same `with-cross-compilation-target` the real cross-build uses, then read the corpus; the oracle checks read-time literals, reader features, the 32-bit natural-access acode, the five-on-stack argument partition, and direct, lexical and unresolved dependencies. Native state is checked identical on return and on nonlocal escape.

No defect. Three observations, none blocking: the record's `test_revision` is the hash of a gzip archive and so changes on every producer run, which is harmless but not reproducible; the standalone envelope must be composed into the aggregate at acceptance, at which point the gate will show LL08-a as unreviewed rather than missing; and the architecture record deliberately leaves uvector subtags and most layout rows empty, so it cannot yet compile the general corpus, which the report states. Disposition: S0-LL08-a / native REVIEWED_NO_DEFECT_FOUND_NOT_ACCEPTED at the fourteen-form registration and target-state scope.

### Follow-up to the ninth audit

The user accepted S0-LL08-a in the reviewer's session. Claude produced the acceptance envelope at the user's direction; recorded in `stage0/project-acceptance.md`, not part of the audit.

## Tenth Claude audit — census projection and compiler/loader identity collection, at ab5490de — 12 September 2026

Scope: commit 58c99e7e (`tests/wasm/native-census/startup-closure/{assemble,exchange,effects,opcodes,check_exchange,test_exchange}.py`, `exchange-inputs.json`, `stage0/joined-census.md`, packet NATIVE-JOINED-CENSUS-R1) and commit ab5490de (`tests/wasm/native-census/rich-observation/` in full: nine-file observation patch and manifest, observer, installer, unit, runner, joiner, three-layer corpus with its checker and mutant drivers, reader/unit/control tests, traced-image inspector, foreign inventory; `stage0/rich-census.md`; packet NATIVE-RICH-CENSUS-R1). Neither commit claims gate credit; both are inputs to S0-LL15-b/c. No shared source changed in the checkout; checkers pass; gate unchanged at 28 accepted, 21 missing, zero unreviewed.

Reproduced in the reviewer's session. Projection: `assemble.py` re-run from the pinned evidence root produced `census.json.gz`, `joins.json.gz`, `summary.json` and `controls.json` byte-identical to the retained packet, with the 23 omission controls and five opcode-parser controls executed inside that assembly; the unchanged census checker rejects the projection with exactly the retained counts (17,329 unresolved reachable edges: 9,362 emitted load initializers, 5,989 bindings, 1,729 dynamic calls, 247 handlers, one native and one stub edge; 28,298 unimplemented nodes). Collection: `analyze.py` re-run over the retained full build stream of 4,006,405 events, the probe stream and the corrected cold-start stream reproduced all three joins files byte-identically. The nine original source digests equal the U1 blobs of those files; the patch digest matches the manifest; the eight changed-FASL copies match the observed digests; the restored FASL set equals the baseline. `test_controls.py` (16 rejections), `test_reader.py` (8 rejections plus the retained image-word regression) and `test_unit.py` (2 positive cases, 5 refusals) reproduce. `verify.py` was re-executed natively against the leftover disposable archive: the three collector omissions, two unsafe-window mutants, sixteen semantic mutations, five unit refusals and twelve JSON byte cases all reproduce, the three probe FASLs equal the packet's baseline and observed copies, and source restoration verified. `inspect_traced.py` re-run against the traced kernel and image reproduced `foreign.json` and `kernel-import-joins.json` byte-identically, with `image.json` differing only in printed heap addresses. The evidence catalog check passes at 160,116 files.

Mechanism, from source: installation is observed after the primitive store and removal intent before the clear, so no logger runs while a helper it uses is unbound; the three expansion sites are observed inline rather than through a shared helper, which is what restored the 156/164 comparison; the observer guards recursion with a per-thread special, rebinds the gensym counter while logging, and pairs effects by actual nesting under a lock rather than by printed names; the reader mode is taken from the identity of the dispatch table actually in use, which is how the cross-dumper's tagged image words were separated from host functions; writer and reader FASL positions are joined with the one-byte opcode offset made explicit. The completion script rebuilds the observed state from the retained image and changed FASLs before the corrected cold start, so the continuation is a genuine resumption, not a relabeling.

Findings, none blocking. First, the report's statement that eight function rows differ between the traced and r7 inventories undercounts: the retained comparison lists 28 differing rows, of which eight are combined-method names carrying heap addresses and twenty are the inspector's own diagnostic definitions from `observer.lisp` and `image-inventory.lisp`, whose recorded source path or position differs between the two runs; corrected in `rich-census.md`. Second, this packet's R6 is relative to its own preparation recipe, in which the selected definitions are loaded and the gensym counter is fixed at 100,000 before the rebuild; that baseline is not the pristine clean build, and three of its 164 FASLs (`bin/compile-ccl`, `bin/nxenv`, `l1-fasls/nx`) differ from the accepted r5 baseline. The README discloses the recipe difference in general terms; the count is now stated in `rich-census.md`. Both sides of R6 share the recipe, so the observation delta remains isolated to the eight intended FASLs. Third, the level-0 hooks in `%fhave` and `%fasl-dispatch` are compiled into the boot image, but the boot-image subprocess never loads the observer, so L1 installation in that subprocess is unobserved, as the report already states; the census assembly must carry it as a separate witness rather than infer it from the parent's compile records.

Disposition: NATIVE-JOINED-CENSUS-R1 and NATIVE-RICH-CENSUS-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. No inventory record changes state; S0-LL15-b and S0-LL15-c remain missing until the closure is assembled from these inputs.


## Eleventh Claude audit — initializer integration, at f3b4b07e — 13 September 2026

Recorded by Codex from the user's supplied Claude review; the executions below are Claude's, not new Codex verification. Scope: initializer identity and phase-boundary integration and NATIVE-INITIALIZER-JOINS-R1. Verdict: no defect in the deliverable, with a control-coverage gap and an evidence-retention gap to fix before marking the packet reviewed.

Claude re-executed the integration in about 90 seconds: all four analysis outputs were byte-identical, all source/input/packet bindings matched, and all thirteen controls rejected with their expected reasons. The 64,900 build boundaries and 70 cold boundaries, conservative per-process order, lack of false initializer cycles and three explicit scope nodes were verified. No shared source changed; the gate remained 28 accepted, 21 missing and zero unreviewed.

Two insertions escaped both the integration checker and the generic contract: a complete cross-process edge and an invented implemented function. A duplicate initializer escaped the integration checker but was caught by the generic contract. Claude requested an independently derived exact bound on added nodes and edges plus insertion controls. The first temporary r1 output also lacked retained evidence or an explanation of its supersession. The packet's preparation revision predates the new sources; the index supplies its later commit binding.

## Twelfth Claude audit — exact addition bounds, at c421e39a — 13 September 2026

Recorded by Codex from the user's supplied Claude review. Verdict: both eleventh-audit findings addressed; no defect found, with a low metadata residual and a provenance caveat. This records the supplied assessment without treating it as project acceptance.

Claude's fresh focused verification reproduced controls.json byte-identically in about 90 seconds, with matching source/artifact bindings. A full producer rerun retained byte-identical graph and witness files; only summary/control counts changed from thirteen to eighteen. Claude's independent cross-process edge, invented function, duplicate initializer, surplus conservative edge and duplicate unobserved-module probes all rejected. The expected additions come from capture streams; full edge comparisons include endpoints, phase, origin, resolution, evidence and multiplicity. The original graph packet remained untouched and the five new catalog entries matched.

Residual: added nodes were compared only by ID and kind, allowing altered implementation text. Comparing full records was recommended. Provenance caveat: development.json asserted that the first temporary r1 succeeded and was superseded by a pin edit, but the reviewer found no supporting on-disk evidence. Claude left acceptance of that explanation to the user. The supplied review did not mark either packet reviewed or accepted; their retained dispositions remain NOT_REVIEWED.

Codex follow-up, 13 September: the original session log has now been located, including the first command's session ID, its exit code 0, printed integration summary and subsequent pin edit/r2 launch. Relevant original log records are retained in the new metadata-checker packet. This supplies evidence for the success claim but does not recover the first run.json or artifact files. The new full-record checker and recovered excerpts postdate Claude's audit and need their own review.

## Thirteenth Claude audit — full node records, at 4a7bbb36 — 13 September 2026

Recorded by Claude directly. Scope: the full added-node-record checker in `identity_join.py`, its six new controls, the extended focused verifier and NATIVE-INITIALIZER-METADATA-R1. The recovered first-run session material was excluded from review at the user's direction.

Verification: the focused verifier re-executed from HEAD in about 140 seconds and produced a controls.json byte-identical to the retained packet, with identical source and artifact hashes; 24 controls reject, five escapes reproduce against the f3b4b07e checker and six against the c421e39a checker. All 14 source blobs at HEAD match the run record; the c421e39a checker blob matches the recorded prior hash; the six packet files match packet.json; packet.json matches the evidence index; the catalog at 8a303b5 matches repository.json and holds exactly the seven new rows; the two earlier initializer packets are untouched. Independent probes, separate from Codex's controls, all reject under the corrected checker: implementation text on a code node, evidence on a front-end function node, a boundary node's reason, a module node's test list, a gap node's reason, and a swap of evidence between two front-end function nodes. The swap rejecting shows the first-reference evidence derivation is order-sensitive rather than a multiset. That derivation (earliest boundary use, reader before callee before callback within a boundary, serialized ancestry walked depth-first) was traced against the producer's recursion and matches.

Findings: none blocking. The eleventh-audit heading above was misdated 12 September and is corrected. The evidence commit was amended after a publisher assertion miscounted six files as seven; the pre-amend commit d1c10b86 remains as a dangling object and the evidence repository has no remote, so nothing external is affected.

Disposition: NATIVE-INITIALIZER-JOINS-R1, NATIVE-INITIALIZER-CHECKER-R1 and NATIVE-INITIALIZER-METADATA-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. The eleventh and twelfth audits' findings are closed. No inventory record changes state; S0-LL15-b and S0-LL15-c remain missing until the closure is assembled from these inputs.

## Fourteenth Claude audit — native emissions, at 363fc2d4 — 13 September 2026

Recorded by Codex from the user's supplied Claude review. Scope: NATIVE-EMISSION-JOINS-R1 and the observed-emission joiner. Verdict: acceptable as an observed-emission join at its stated scope, with no defect in its claims; one medium finding must be fixed before the fragment feeds closure. This is a scoped review, not project acceptance or a review of the subsequent correction.

Claude re-executed the joiner in about 270 seconds. All five analysis outputs were byte-identical, source/artifact hashes matched, and materialization reproduced the retained graph hash. An independent decoder using the pinned U1 vinsn definitions and image subprimitive table reproduced every reported count: 20,297 CALL-SUBPRIM and 309 JUMP-SUBPRIM calls, 36 targets, zero unmapped offsets. All six operand layouts matched the pinned definitions. The 17 controls rejected, exact added-node/edge bounds held, and Claude's unknown-offset probe produced an unresolved node and one additional generic census refusal. The 17 source bindings, eight payloads plus manifest, six input pins, same-kernel check and catalog matched; earlier packets were unchanged.

Finding: the base already contains 279 evaluated operator slots, while the fragment introduces 141 build-process representations without an explicit join. All 141 match the image by ID, name and handler. The reviewer requested equality-witness edges or an explicit obligation before closure, because name-only correspondence would leave duplicate classifications. Low notes: add an unknown-offset control; explain that 196 template names each have two identities with sequential, nonoverlapping event ranges, consistent with recompilation/reloading during the build. That explanation is an inference from the ranges, not a witnessed reload cause.

The supplied review left the original packet NOT_REVIEWED in its immutable run record and did not grant gate credit. Its scoped verdict is now recorded in the index. Codex's follow-up adds explicit slot-equality edges and witnesses, checks all five evaluated slot fields against the pinned image, retains separate process-local code IDs, and exercises the unknown-offset refusal. The correction requires its own independent review before acceptance.

## Fifteenth Claude audit — operator-slot reconciliation, at e9f5d0c6 — 13 September 2026

Recorded by Claude directly. Scope: the format-2 emission fragment in NATIVE-EMISSION-SLOT-JOINS-R1, the same-slot equality edges and witnesses, the new controls and the retained development failure. The second-Mac retirement at 937b1705 and the bootstrap design review at 6f7b53bc were read but not audited; the former is a user scope decision and the latter changes no result.

Verification: the joiner re-executed from HEAD with all five analysis outputs byte-identical to the retained packet and matching source and artifact hashes. Format-2 materialization reproduced the recorded graph hash, 248,555 nodes and 622,466 edges. Independently of the checker, the retained delta was compared with the original fragment: node records identical, non-slot edges identical, plus exactly 141 same-slot edges and 141 witnesses. Each edge runs from the base's evaluated image slot to the build-process slot of the same number, each witness carries both records and the common kernel digest, and every witness field was checked against the base slot node and the pinned image table. The 141 joined slots are the 141 slots my own decode of the raw stream observed in handler frames. All 16 payloads plus the manifest, the index entry, the 17 source bindings at HEAD, the unchanged input pins and the catalog at 92182abf verified; the original emission packet is untouched. The 32 controls reject with distinct reasons; the six operand layouts and the unknown-offset refusal case pass, closing the fourteenth audit's low note, and the report now explains the 196 duplicated template names.

Findings: none. The retained first correction run failed on a shadowed variable in the test driver and is kept with its source and log, as the rules require.

Disposition: NATIVE-EMISSION-SLOT-JOINS-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC; the fourteenth audit's closure finding on NATIVE-EMISSION-JOINS-R1 is closed. No inventory record changes state; S0-LL15-b and S0-LL15-c remain missing. Gate 28 accepted, 20 missing, zero unreviewed of 48 after the second-Mac retirement.


## Sixteenth Claude audit — native boot observation, at 2b875096 — 13 September 2026

Recorded by Codex from the user's supplied Claude review. The following executions and independent checks are Claude's; no reviewer commit was supplied. Scope: NATIVE-BOOT-OBSERVATION-R1 and the reversible three-file early-boot fixture. Verdict: acceptable at its stated scope, no defect found, with one required source-identity disclosure before census integration.

Claude re-executed the whole native unit in about 4.5 minutes. All 164 baseline FASLs, the three intentionally changed FASLs and all 164 restored FASLs match the retained ones byte-for-byte; both native suites pass 21,843 with 75 upstream-disabled; snapshots agree and the disposable source files restore exactly to U1. Events, analysis and file joins match after normalizing the disposable archive's absolute path. Saved image bytes differ, as expected for image serialization. The retained-stream verifier, three saved-image controls and two positive/five refusal recovery cases reproduce. Packet/index/catalog/source bindings and unchanged prior packets were checked. The recorded analyzer differs from HEAD only by the disclosed path-relocation change.

Independent recount reproduces 133 cold initializers in queue order, 121 completed loads, 8,326 matched reader/call pairs, 9,335 installations, 9,289 removal intents and 9,094 symbol histories. Replay equals the independently read final bindings, with three frames active at handoff. All 8,327 initializer opcodes match retained bytes and pinned U1 lfuncall constants. Source inspection confirms the inline store/call/handoff sites, primitive cons-list recording, owner/refusal checks, no new special bindings and one handoff form; native R6 confines changes to exactly three FASLs.

Required finding: every one of the 133 cold thunks has name NIL and no source file or position. No existing input observes the xload-side push site, so the packet cannot associate them with a source form or module. Disclose this now; retain order-only nodes with unresolved source dependencies until a new insertion witness supports an explicitly justified positional join. Low notes: STATUS must say initializer opcodes, not all reader opcodes; the first failed boot's missing binary remains disclosed, with its log, command and patch retained.

Disposition: REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC for execution at the recorded bounds; source integration remains open. The disclosure and STATUS terminology are corrected alongside this record. The raw packet retains its original disposition and bytes. No project acceptance is claimed; gate stays 28 accepted, 20 missing and zero unreviewed required records.

## Seventeenth Claude audit — cold initializer origins, at 336b1f5f — 13 September 2026

Recorded by Claude directly. Scope: NATIVE-COLD-ORIGINS-R1 and the process-local xload origin fixture. Verdict: acceptable, no defect. This closes the sixteenth audit's source-join finding, and by a better route than the reviewer implied: runtime wrapping of the existing xload reader, loader and image dumper in a disposable process needs no source patch, no recompilation and no new R6 comparison.

Verification: the full native capture was re-executed from the pinned inputs. Origins, joins, summary, controls and the fresh boot stream are identical to the retained packet after normalizing the disposable archive path, and the fresh boot stream is identical to the reviewed NATIVE-BOOT-OBSERVATION-R1 stream, which is the link that makes the positional join witnessed rather than assumed. The retained-analysis verifier reproduces its 20 controls. The rule's premises were checked in U1 source: one push site in the xload lfuncall handler, one reversal before the image is written, an in-order drain at boot. Independent checks on the retained data confirm 133 positions and distinct thunks, insertion order strictly increasing by load order and FASL offset, genuine lfuncall opcode bytes at every offset, source-context slices hashing to the exact U1 source bytes, and joined boot identities equal to the reviewed cold queue. All three wrappers restore after success and after the injected abort, no image is written on abort, the native omission is rejected, and all 164 FASLs, the U1 sources and the checkout's shared source are unchanged. Twenty-seven payloads plus manifest, the index entry, the catalog at 7f03f412 with 28 new rows and nine source hashes at HEAD verified; the boot-observation packet is untouched.

Result: all 133 cold initializers have a module origin across 31 files, 77 in nfasload; 131 carry a compiler source context; entries 65 and 106 in nfasload have module and exact FASL offset but no source range and are left null. Notes, no action: plain and observed boot image bytes differ, as image serialization is not byte-reproducible, and both are retained; the packet reuses the boot-observation native qualification, which is appropriate because it compiles nothing and changes no FASL.

Disposition: NATIVE-COLD-ORIGINS-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC; the sixteenth audit's source-join finding is closed. No inventory record changes state; S0-LL15-b and S0-LL15-c remain missing. Gate 28 accepted, 20 missing, zero unreviewed of 48.

## Eighteenth Claude audit — boot graph integration, at b90771bb — 13 September 2026

Recorded by Claude directly. Scope: NATIVE-BOOT-INTEGRATION-R1 and the boot integration fixture (`boot_join.py`, `join_boot.py`, `check_boot.py`, `test_boot_join.py`, `materialize_boot.py`, `boot-inputs.json`). Verdict: acceptable, no defect.

Verification: the producer and the standalone materializer were re-executed from the nine pinned inputs. The fragment, controls and summary are byte-identical to the retained packet, and both full graphs hash to the recorded materialization digest. Packet manifest hashes, the index entry, the catalog at 07b2c6d8 with exactly 11 new rows and no changed rows, and all executed source hashes at HEAD were verified. Independently of the producer and its checker, the 79,406 fragment nodes were decomposed from the retained boot joins, files and origins: 52,441 events, 9,094 cells, 16,657 function identities, 1,059 opaque values, 120 FASL modules, 31 source modules, one dispatch table, one execution surface and one closure gap. The event chain is linear and contiguous, there are no duplicate edges and no dangling targets, and the 133 origin entries are in cold-queue order with entries 65 and 106 null. Only 32 edges cross from the prior namespace into the boot namespace, all conservative: the historical boot-process gap and 31 source-inventory path edges; no boot object is equated with a prior-run identity. A separate traversal from the 13 seeds reaches every boot node, and the census checker rerun raises the unimplemented reachable count from 28,301 to 29,361, exactly the 1,059 opaque values plus the closure gap, with unresolved edges unchanged at 17,329. The two loader aliases bind identical bytes, and the retained first-run failure and correction are as described.

Observation, no action: the materializer pins nine script dependencies but not the boot-observation analyzer it loads for its three-line event reader; the analyzed joins it compares against are separately hash-pinned, so no result depends on it.

Disposition: NATIVE-BOOT-INTEGRATION-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. No inventory record changes state; S0-LL15-b and S0-LL15-c remain missing, with opaque cell contents, target traversal, seed review and dynamic-call bounds open. Gate 28 accepted, 20 missing, zero unreviewed of 48.

## Nineteenth Claude audit — macro and special-operator wrappers, at 55ca26e9 — 13 September 2026

Recorded by Claude directly. Scope: NATIVE-BOOT-WRAPPERS-R1 and the boot-wrappers fixture, including the one-argument extension of the reviewed boot-observation exporter. Verdict: acceptable, no defect.

Verification: the three native exports (legacy, extended, repeat) and the standalone verifier were re-executed from the pinned image, kernel and inputs. The wrapper description, patch, controls and summary are byte-identical to the retained packet, the full graph hashes to the recorded materialization digest, and the legacy stream is identical to the reviewed 52,441-event stream in all three modes, which is the control that licenses the change to the reviewed exporter. Packet manifest, index entry, catalog at 1a89085b with 16 new rows and no changed rows, and all executed source hashes at HEAD were verified; the retained development failure is the checker comparing the decoded fixnum against the raw word, corrected by recording both. The representation was confirmed in U1: a macro cell holds a two-element vector of the xloader-set macro-apply word and the expander (l0-def.lisp:209-213, xfasload.lisp:1101 and 1719), a special operator is the same vector with a symbol (l0-def.lisp:216-221), the raw word is #xc9cd0000000000 for x8664 (xx8664-fasload.lisp:33) read as a fixnum shifted by three, and handlers live in the front end's alphatizer table (nx-basic.lisp:163, nx1.lisp:51). Independently of the fixture: the 1,059 wrappers are exactly the opaque values in the reviewed binding events; the 31 special operators are the 25 standard operators plus COMPILER-LET, FBIND, NFUNCTION, WITH-C-FRAME, WITH-VARIABLE-C-FRAME and X86-LAP-FUNCTION, each with its own NX1 handler; 1,028 macro cells resolve to 1,012 distinct expanders, the shared ones being BACKEND-ARCH-MACROEXPAND; edge and node counts decompose exactly (1,059 trap edges, 1,028 expander edges, three per special; one trap, one table, 31 markers, 1,042 new functions); the census unimplemented count falls by exactly 1,059 with unresolved edges unchanged.

Disclosure, no action: the boot-observation and cold-origins packets bind the previous revision of the shared exporter; the legacy-mode control here shows the new revision produces the same bytes.

Disposition: NATIVE-BOOT-WRAPPERS-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. No inventory record changes state. Gate 28 accepted, 20 missing, zero unreviewed of 48.

## Seed review — 13 September 2026

Recorded by Claude directly as the independent reviewer of `startup-closure/seeds.json`. Disposition: REVIEWED_WITH_FINDINGS; the proposed set is not approved. The full record is [seed-review.md](seed-review.md). In brief: the 13 seeds name the right functions but miss six kernel entry surfaces (the saved toplevel closure chain, thread start, the signal callback XCMAIN, the trap dispatcher %XERR-DISP, the callback dispatcher %PASCAL-FUNCTIONS% and the 23 builtin-vector functions), omit an explicit exclusion for %FOREIGN-THREAD-CONTROL, and are untestable while the clean-image and asdf widening edges make every node reachable. A revised seed set and the removal of the widening are required before closure.

## Twentieth Claude audit — startup seed revision 2, at 2ef830cb — 13 September 2026

Recorded by Claude directly. Scope: NATIVE-STARTUP-SEEDS-R2, the revised seed manifest, the extended clean-image inspector and the reachability diagnostic. Verdict: acceptable, no defect; seed revision 2 approved for the stated native profile.

Verification: the native inspector was re-executed from the retained kernel (executable disposable copy) and image; candidates, summary and controls are byte-identical to the retained packet, and image.json is identical except eight combined-method print names that embed heap addresses, which the join never reads. The reachability diagnostic was re-executed on the reviewed wrapper graph and reproduces all three scenarios exactly. Packet manifest, index entry, catalog at b2ca818a with 13 new rows and no changed rows, and all sixteen repository source pins at HEAD were verified; seeds-v1.json is byte-identical to the previous manifest and the historical assembler pins it and refuses revision 2. Independently: the 22 seeds resolve to one native binding each; the three live callback slots are %FOREIGN-THREAD-CONTROL, XCMAIN and %XERR-DISP with symbol values matching their trampolines, agreeing with the four defcallback forms in U1 level-1 for this target; the 23 builtin slots match xdump/xfasload.lisp:323 in order and each resolves to the function of the same name; the applicable methods are the two primaries and the before method in l1-application.lisp; the root union of 49 equals the set I derived from those four sources. Controls: 27 revised and 14 historical, all rejecting with the stated reasons.

Note, no action: anonymous combined-method names in the inventory carry heap addresses, so image.json is not byte-reproducible across sessions even though every joined output is; a stable label would remove the noise.

Disposition: NATIVE-STARTUP-SEEDS-R2 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC; seed set revision 2026-09-13-kernel-entry-surfaces REVIEWED_APPROVED for the native profile, recorded in seed-review.md. Finding 8 of the seed review is restated per the diagnosis and remains open. No inventory record changes state. Gate 28 accepted, 20 missing, zero unreviewed of 48.

### Addendum to the twentieth audit — manifest carry at 1a9e95c4

Claude reviewed 1a9e95c4, a metadata-only commit. The manifest changed in exactly three text fields (review disposition, review basis, first scope-limit sentence); entrypoints, required effects, required bindings, method selection, exclusions, revision and profile are byte-unchanged. The retained R2 packet still pins the pre-approval manifest hash, which Codex disclosed; running the candidate producer against the retained image and graph with the current manifest reproduces the retained candidates except the two fields derived from that text, and the coverage checker passes. No native execution, no evidence change, no defect.

## Twenty-first Claude audit — first source traversal, at 4c7f2162 — 13 September 2026

Recorded by Claude directly. Scope: NATIVE-SOURCE-TRAVERSAL-R1 and the source-traversal fixture (driver, runner, checker, controls, verifier). Verdict: acceptable, no defect.

Verification: all four native sessions (normal, repeat, stop-after-first-capture, host-reader) were re-executed from the pinned U1 source archive, bootstrap data, clean r7 kernel and image, and the accepted LL08-a registration binaries loaded through their module entries. The raw normal capture, the two native mutants, the projected facts, the controls and the summary are byte-identical to the retained packet, and the focused verifier passes. Packet manifest, index entry, catalog at 5cb08e61 with 18 new rows and no changed rows, and all ten source pins at HEAD were verified. Independently of the fixture, a separate scanner over the pinned source reproduces the sixteen top-level forms, their sixteen end offsets, the twelve definition names in order, and confirms that each of the eight stopped definitions contains the operation the capture names as its first failure: a basic-stream type in CLEAR-IOBLOCK-STREAMS, the foreign exit in SAVE-IMAGE, SEEK_END, SEEK_SET and O_RDONLY constants, the two kernel-global macros in KERNEL-PATH and RESTORE-LISP-POINTERS, and the callback-entry accessors in RESTORE-PASCAL-FUNCTIONS. The target context is the registered 32-bit census backend with zero argument registers and no Darwin or x86 features, as the fourteen-form fixture established. No FASL was written, the pristine source is unchanged and all twelve definitions remain unbound after traversal.

Assessment of scope: the packet claims accounting, not closure, and the claim holds. The four captured bodies yield real call and reference sites with named targets, and the two computed calls keep empty target sets rather than being widened. The eight gaps are first-error stops, so the requirement list for those bodies will grow when each first item is supplied; the driver handles only in-package, defvar, declaim and simple defun forms. Both limits are stated in the report and enforced by controls.

Disposition: NATIVE-SOURCE-TRAVERSAL-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. No widening edge is replaced and no inventory record changes state. Gate 28 accepted, 20 missing, zero unreviewed of 48.

## Twenty-second Claude audit — startup target descriptions, at 01a96597 — 13 September 2026

Recorded by Claude directly. Scope: NATIVE-TARGET-DESCRIPTIONS-R1, the description ledger, the Lisp descriptions and pass-2 extension, the driver, analyzer, controls and verifier. Verdict: acceptable, no defect.

Verification: all eight native sessions (normal, repeat and six controls) were re-executed from the same seven pinned inputs as the reviewed traversal. The raw normal capture, the six native mutant captures, the facts, the controls, the summary and the copied ledger are byte-identical to the retained packet, and the focused verifier passes. Packet manifest, index entry, catalog at 79940bf2 with 27 new rows and no changed rows, and all 22 source pins at HEAD were verified. Independently of the fixture: the eight subtag values follow from x8632-arch.lisp by its own definition, fulltag plus index shifted by three, giving 50, 114, 122, 130, 159, 234, 242 and 250, and the classification-boundary reading of 159 matches the min-cl-ivector-subtag constant; the deferred literal shape the pass-2 extension expects, the token followed by a funcall of the compiled initializer, is what nx1-load-time-value constructs at nx1.lisp:1164; the four initializers with one FIND-CLASS-CELL and three ENSURE-SLOT-ID calls correspond to the typep, slot-boundp, slot-value and setf slot-value forms inside the %map-areas lambda of CLEAR-IOBLOCK-STREAMS, whose inner function is the recorded owner of all four. The batch-flag macro matches only the exact quoted form in RESTORE-LISP-POINTERS and emits a named service call that refuses execution; KERNEL-PATH still stops on the pointer variant, so no kernel-global offset entered the census. The retained development record shows the real guard-aliasing defect and its correction, and the first formal failure was a checker expectation, not a capture error.

Assessment of scope: the ledger's eight obligations are the FFI, POSIX and image-save boundary this project excluded, recorded as replacement contracts rather than discharged as unsupported, which is what the census contract requires for required startup calls. The subtype rows are type-classification metadata under D1 and claim no payload layout. Five definitions still stop at foreign names and constants, the macro environment remains the native image's, and seven computed calls keep empty target sets. All of that is stated and enforced by controls.

Disposition: NATIVE-TARGET-DESCRIPTIONS-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. No widening edge is replaced and no inventory record changes state. Gate 28 accepted, 20 missing, zero unreviewed of 48.

## Twenty-third Claude audit — required-inventory gate control S0-LL02-a, at 24f8805e — 13 September 2026

Recorded by Claude directly. Scope: STANDING-INVENTORY-CONTROL-R1, the inventory-control runner and cases, the registration of S0-LL02-a in the inventory, and the scoped gate result. Verdict: acceptable, no defect; the slot is complete at its stated scope and ready for the user's acceptance decision.

Verification: the runner was re-executed into a fresh directory. All nine quarantined gate inputs, the observations, the summary, the cases and the slot-gate assessment are byte-identical to the retained packet; the results envelope differs only in its timestamp, command line and the hashes of the two run-varying files it lists. The retained packet replays through the runner's own verifier. The production gate and the binding module are unchanged since 9c2ef44d. Packet manifest, index entry, catalog at b8ad0621 with 31 new rows and no changed rows, and all four source pins at HEAD were verified. The composite count was reproduced without accepting the scoped assessment on trust: the production gate run on the prior accepted envelope against the updated inventory still yields 28 accepted with S0-LL02-a missing, so registering the runner changed no other contract, and the gate run on the new result against its own slot yields exactly one reason, unreviewed S0-LL02-a. Together those are 28 accepted, 19 missing, 1 unreviewed of 48. The inventory edit is confined to the S0-LL02-a status and runner fields. The seven rejection cases exercise the obligation's assertion, omit an expected ID and require BLOCKED, and go beyond it with a missing variant, a same-count substitution, a duplicate that masks a missing record, and the empty inventory, each with the exact diagnostic text checked rather than only the exit status.

Observation, no action: the packet's retained inventory snapshot is semantically identical to the committed inventory but not byte-identical, because non-ASCII characters were written raw at execution time and escaped in the committed file; the gate binds each result to its packet snapshot and to a per-slot contract hash over parsed content, so nothing depends on the whole-file bytes, and my re-execution against the committed file produced identical observations and slot gate.

Disposition: STANDING-INVENTORY-CONTROL-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. S0-LL02-a stays unreviewed in the gate until the user records acceptance. Gate 28 accepted, 19 missing, 1 unreviewed of 48.

## Twenty-fourth Claude audit — source-derived target macro slice, at cb516b22 — 13 September 2026

Recorded by Claude directly. Scope: NATIVE-TARGET-MACROS-R1 and the target-macros fixture (driver, runner, checker, verifier, source pins). Verdict: acceptable, no defect.

Verification: all six native sessions (normal, repeat, and the host-subtag, wrong-slot, bypass-source and name-only mutants) were re-executed from the same pinned inputs as the reviewed traversal. Both raw captures of every session, the controls and the summary are byte-identical to the retained packet, and the verifier passes. Packet manifest, index entry, catalog at 0680144e with 25 new rows and no changed rows, and all 28 source pins at HEAD were verified. Independently of the fixture, the eight pinned spans were read from U1 at their recorded offsets and are the named forms: the type-keyword-code helper in sysutils.lisp, the define-accessors generator in lib/macros.lisp, gvector, allocate-typed-vector and %istruct in lispequ.lisp, %null-ptr in lib/macros.lisp, the basic-stream accessor group with slots wrapper, flags, state and info in that order, and the pfe accessor group with slots routine-descriptor, proc-info, lisp-function, sym, without-interrupts and trace-p in that order. The probe expansions match those bodies: gvector to %gvector with the D1 istruct subtag 130, allocate-typed-vector to %alloc-misc with 250, %istruct to gvector over register-istruct-cell, %null-ptr to %int-to-ptr 0, and each accessor to %svref at its source index. Routing is by the original macro function's identity, so a same-named compiler macro or local macrolet is not intercepted, which the shadow probe and the name-only mutant demonstrate. The 293 hook events join in order to the unchanged traversal's records; ten are source-routed and 283 keep 46 distinct native expander identities as explicit remaining dependencies, dominated by IF, LET*, PROGN, AND and EQL.

Assessment of scope: this replaces inherited native expander bodies for seven macros in one file, and the packet says exactly that; the expanders still execute on the host compiler, the descriptor accessors and bootstrap helpers remain dependencies of the clean native image, and the macro environment is marked unqualified. The retained development record includes two path failures before any native execution and two checker-development corrections, labeled as reproductions.

Disposition: NATIVE-TARGET-MACROS-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. No widening edge is replaced and no inventory record changes state. Gate 28 accepted, 19 missing, 1 unreviewed of 48, the latter S0-LL02-a awaiting the user's decision.

## Twenty-fifth Claude audit — source expansion bodies for dumplisp, at b1843dfa — 13 September 2026

Recorded by Claude directly. Scope: NATIVE-SOURCE-EXPANDERS-R1 and the source-expanders fixture (manifest, selection, driver, runner, checker, verifier). Verdict: acceptable, no defect. Also confirmed on this pass: the S0-LL02-a acceptance at 35fa02b6 records the user's words "I accept it." against the twenty-third-audit commit, and the production gate run on the combined accepted envelope against the current inventory returns BLOCKED with 19 missing reasons and none unreviewed, so the ledger reads 29 accepted, 19 missing, 0 unreviewed of 48.

Verification: all seven native sessions (source, repeat, inherited reference, and the bypass-if, host-layout, copy-no-change and name-only mutants) were re-executed from the pinned inputs. Both raw captures of every session, the controls and the summary are byte-identical to the retained packet; the inherited session's traversal bytes equal the source session's, and its expansion events differ only in the selected callable identity. The verifier passes. Packet manifest, index entry, catalog at 6b3ecb4a with 28 new rows and no changed rows, and all 33 source pins at HEAD were verified. Independently: nine of the 47 manifest offsets were read from U1 and each begins the named definition; the construction of each rebuilt expander is the parse-macro-1 path that U1's own define-compiler-macro uses at nx0.lisp:191 to 202, without the global installation; the one escaped event, the %GET-KERNEL-GLOBAL-PTR refusal, matches the dispatcher body at backend.lisp; the 293 events join the unchanged traversal's records in order with 91 macro, 200 compiler-macro and 2 dispatcher invocations, all 293 source-routed in the source session and none in the reference session; 61 registry entries over 60 distinct rebuilt functions and 60 distinct originals, the two architecture names sharing one dispatcher; 214 helper rows over 212 sites and 68 names, all marked unqualified, with four computed helper calls left empty; and the three native literals are the compiler environment, the IOBLOCK class cell and the RESTART wrapper, as stated. The no-change probe preserves EQ identity, the layout probe follows the descriptor value 50 and then 122 in both expansion and acode, and the local DOTIMES shadow still wins.

Assessment of scope: the packet closes the expander-body half of the macro-environment gap for this file and says plainly that the helper half is open; the 68 helpers include target-sensitive functions such as NX-LOOKUP-TARGET-UVECTOR-SUBTAG and OPTIMIZE-TYPEP whose bodies still come from the native image. That is the next qualification obligation, and it is recorded, not hidden. The retained development record explains the identity-namespace correction and the two scanner refusals.

Disposition: NATIVE-SOURCE-EXPANDERS-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. No widening edge is replaced and no inventory record changes state by this review. Gate 29 accepted, 19 missing, zero unreviewed of 48.

## Twenty-sixth Claude audit — S0-ABI-selection desk record, at 629534d2 — 13 September 2026

Recorded by Claude directly. Scope: ABI-DESK-DECISION-R1, the abi-decision producer, its decision and input pins, the registration of S0-ABI-selection in the inventory, and the scoped gate result. Verdict: acceptable, no defect; the slot is complete at its stated scope and ready for the user's acceptance decision, with one confirmation the user must supply.

Verification: the producer was re-executed into a fresh directory. The basis, controls, decision, scoped gate result, summary and inventory snapshot are byte-identical to the retained packet, and the production gate's direct output on the new envelope is identical; the results envelope differs only in command and timestamp. The retained verifier passes. Packet manifest, index entries, catalog at e6e9e4a4 with 24 new rows and no changed rows, and all five source pins at HEAD were verified. Independently: the transitive prerequisite closure of S0-ABI-selection is 14 records over 24 variants, every one accepted in the current envelope; the commit changes exactly one contract hash of 37, the desk record's own, as the corrected preparation check also records; the production gate run on the accepted envelope against the current inventory still returns 29 accepted with the desk record missing, which composed with the new envelope's single unreviewed reason gives 29 accepted, 18 missing, 1 unreviewed of 48. The B protocol as recorded, self and count parameters, two results, arguments at incoming VSP plus four times the index, a sixteen-aligned reserved extent, caller-owned rooted output region, matches the reviewed protocol document. The performance fields correctly state that policy v2 was never applied, its minima never met, and no speed superiority is claimed, consistent with the 12 September policy-change record and the retained NO_SELECTION summaries.

What the reviewer cannot verify: assertion 2 rests on a quoted user direction in decision.json, attributed to the user speaking to Codex in a conversation this reviewer does not see. No committed record carries that quotation; the 12 September policy-change record paraphrases the direction in different words. The quotation is plausible and consistent with that record, but the user should confirm it is theirs before accepting, since the decision is the user's and the record puts words in the user's mouth.

Observation, no action: the current-stage0-gate-result.json schema version field went from 2 to 1 with this update; the fields are a superset of the previous record, so nothing reads differently, but the number should not move backwards.

Disposition: ABI-DESK-DECISION-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. S0-ABI-selection stays unreviewed in the gate until the user records acceptance. Gate 29 accepted, 18 missing, 1 unreviewed of 48.

## Twenty-seventh Claude audit — target numeric and layout helper paths, at 2c08b98c — 13 September 2026

Recorded by Claude directly. Scope: NATIVE-TARGET-HELPERS-R1 and the target-helpers fixture (driver, runner, analyzer). Verdict: acceptable, no defect. Also confirmed on this pass: the S0-ABI-selection acceptance at 8e7a4221 records the user's words "lets accept the ABI" against the twenty-sixth-audit commit and states that the quoted earlier direction appears verbatim in the user messages Codex holds; the production gate run on the new combined envelope against the current inventory returns BLOCKED with 18 missing reasons and none unreviewed, so the ledger reads 30 accepted, 18 missing, 0 unreviewed of 48.

Verification: all six native sessions (normal, repeat, inherited, host-reader, host-range, host-subtag) were re-executed from the pinned inputs. Both raw captures of every session, the controls and the summary are byte-identical to the retained packet, and the retained verifier passes. Packet manifest, index entry, catalog at 90bf9fc8 with 25 new rows and no changed rows, and all 38 source pins at HEAD were verified. Independently, the claims about U1 were checked in source: EQL-IFF-EQ-P at optimizers.lisp:177 carries two 64-bit-target-only single-float branches and tests integers with the host FIXNUMP; NX1-TARGET-FIXNUMP at nx0.lisp:1917 tests against the target architecture's most-negative and most-positive fixnum; the x8632 architecture declares a 32-bit word and fixnum shift 2, giving the signed 30-bit range 536870911 to -536870912 the probes use as the boundary; NX-LOOKUP-TARGET-UVECTOR-SUBTAG is at nx0.lisp:738; and EQL is at index 10 of the builtin vector in xfasload.lisp, which is the builtin call the boxed probes retain. The eight numeric probes therefore test the right boundary, and the inherited and host-range mutants fail exactly at the boxed-integer oracle while the host-reader mutant fails at the single-float oracle. The 40 helper calls join to the 21 EQL, BASIC-STREAM-P and VECTORP events of the unchanged 293-event traversal, with short-circuit behavior and answers checked per event.

Assessment of scope: this is the first census packet to find a concrete host-versus-target error in the inherited compiler environment, single floats and integers above 2^29 being folded from EQL to EQ under 64-bit assumptions, and it corrects it by reading the source under target state and binding the target range predicate, without touching the global helper. The packet is careful to say that two of 68 helpers are supplied, that nonconstant type inference still uses native facts, and that the file traversal did not encounter the defect. All of that is enforced by controls.

Disposition: NATIVE-TARGET-HELPERS-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. No inventory record changes state by this review. Gate 30 accepted, 18 missing, zero unreviewed of 48.

## Twenty-eighth Claude audit — evidence-kind gate control S0-LL03-a, at 23738a1c — 13 September 2026

Recorded by Claude directly. Scope: STANDING-EVIDENCE-KIND-CONTROL-R1, the evidence-kind-control runner and cases, the registration of S0-LL03-a in the inventory, and the scoped gate result. Verdict: acceptable, no defect; the slot is complete at its stated scope and ready for the user's acceptance decision.

Verification: the runner was re-executed into a fresh directory. All 26 quarantined entries (the synthetic inventory, the 24 case inputs and the payload directory), the observations, the summary, the cases and the slot-gate assessment are byte-identical to the retained packet; the results envelope differs only in timestamp, command line and the hashes of the run-varying files. The retained packet replays through the runner's own verifier, which rebuilds the fixture base, reconstructs each declared mutation, re-invokes the production gate on the retained bytes and compares every observation. The gate and binding tools are unchanged since 9c2ef44d. Packet manifest, index entry, catalog at 61cd80a1 with 66 new rows and no changed rows, and all four source pins at HEAD were verified; the retained inventory snapshot differs from HEAD only in the escaping of two characters, which Codex disclosed, and parses identically. The composite count was reproduced independently: the production gate on the accepted envelope against the updated inventory returns 30 accepted with S0-LL03-a missing, and on the new result against its own slot returns exactly one reason, unreviewed S0-LL03-a, giving 30 accepted, 17 missing, 1 unreviewed of 48. The binding check records that only the S0-LL03-a contract changed of 37 and that all 30 accepted bindings remain current.

Against the obligation: the assertion asks the gate to reject a hand-built or synthetic record offered for compiler-generated acceptance and to keep candidate, profile, substitution and artifact labels visible regardless of pass or fail. The twelve wrong-kind cases offer the complete hand-built or synthetic record, with its own declared kind, configuration and payload hashes intact, under the generated requirement across C, C4 and B in full and reduced profiles, and each is refused with the exact wrong-kind diagnostic. The remaining ten cover variant substitution masked by duplication, an undisclosed import substitution, a missing substitutions field, a skipped execution, a failed record in each of the three classes, a mismatched payload hash and a missing review, each with its exact diagnostic, and the failed-status cases assert that every label field is unchanged from the base record. The packet is explicit that this is metadata enforcement, not compiler-origin authentication, and that its positive synthetic records pass deliberately.

Disposition: STANDING-EVIDENCE-KIND-CONTROL-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. S0-LL03-a stays unreviewed in the gate until the user records acceptance. Gate 30 accepted, 17 missing, 1 unreviewed of 48.

## Twenty-ninth Claude audit — declared-type equality paths, at 43f360fd — 13 September 2026

Recorded by Claude directly. Scope: NATIVE-TARGET-TYPES-R1 and the target-types fixture (driver, runner, analyzer). Verdict: acceptable, no defect. Also confirmed on this pass: the S0-LL03-a acceptance at cd3188c2 records the user's words "I accept S0-LL03-a" against the twenty-eighth-audit commit, and the production gate run on the new combined envelope against the current inventory returns BLOCKED with 17 missing reasons and none unreviewed, so the ledger reads 31 accepted, 17 missing, 0 unreviewed of 48.

Verification: all six native sessions (normal, repeat, prior, shallow, host-range, trust) were re-executed from the pinned inputs. Both raw captures of every session, the controls and the summary are byte-identical to the retained packet, and the retained verifier passes. Packet manifest, index entry, catalog at 1f15ddf6 with 25 new rows and no changed rows, and all 40 source pins at HEAD were verified. The cause the packet names was confirmed in U1: NX-TARGET-TYPE in nx-basic.lisp rewrites only a bare FIXNUM to signed-byte 30 on a 32-bit target and returns any compound specifier unchanged; NX-FORM-TYPEP applies that translation to the requested type before calling SUBTYPEP, so the FIXNUM nested inside the equality helper's OR query kept the host's 61-bit meaning and a declared signed-byte 31 or exact boxed integer was approved for pointer comparison; NX-FORM-TYPE translates a declared type through the same scalar function; and NX-TRUST-DECLARATIONS returns nil at safety 3, which is why the last probe keeps the ordinary EQL call. The three newly pinned spans begin the named definitions. The fifteen probes go through the real front end, seven produce an EQ operator with no call, seven retain the builtin EQL at index 10 and the safety-3 case retains the global call, and the sixty type queries in the file traversal join to the seventeen EQL events with their translation steps checked against an independent translator in the analyzer.

Assessment of scope: the private adapter descends only into OR, AND and NOT and leaves MEMBER payloads and integer bounds alone, which the literal-fixnum probe and its control check; the packet states that general result-type inference, aliases, TYPE-EXPAND, SUBTYPEP and the other helpers remain native dependencies. A preliminary printout with reversed EQ/EQL counts was corrected before the first formal run and its log retained.

Disposition: NATIVE-TARGET-TYPES-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. No inventory record changes state by this review. Gate 31 accepted, 17 missing, zero unreviewed of 48.

## Thirtieth Claude audit — required-child failure control S0-LL01-b, at 9285ee40 — 13 September 2026

Recorded by Claude directly. Scope: STANDING-CHILD-COMMAND-CONTROL-R1 and the child-command-control fixture (runner, adapter, README, scope report). Verdict: acceptable, no defect. The inventory edit is confined to the S0-LL01-b slot's status and runner fields; no shared compiler, kernel, native-baseline or production tool source changed between 8e8cf13b and this commit.

Verification: the retained verifier was run against the packet with a fresh replay directory and passed; the producer was re-executed from scratch and its observations, cases, summary, slot gate and slot inventory are byte-identical to the retained packet, with results.json equal in every field except the timestamp, command, toolchain and the quarantine archive hash, which carry execution-specific paths and times as the packet discloses; the fresh producer output was then itself verified with a second replay. Both gate composites were reproduced: the production gate on the unchanged accepted envelope against the HEAD inventory returns 17 missing reasons including S0-LL01-b, and on the new result against its slot inventory returns only the unreviewed reason, so the ledger is 31 accepted, 16 missing, 1 unreviewed of 48. Index locator, packet and inventory hashes, all eight source pins at HEAD, and the catalog at ccl-evidence 7f1b85fe with 29 new rows were verified. The retained quarantine archive was opened and the ten failure cases inspected: every inner results.json carries the native writer's label but SYNTHETIC-NOT-U1 and SYNTHETIC-NOT-UPSTREAM revisions and zero completed runs before the failing step, so none could be mistaken for native evidence.

Assessment of the mechanism: the adapter loads the production runner with runpy under a non-main name, so the module-level argument parsing and directory checks run and only the global binding of the command function is replaced by a wrapper that substitutes the child argv and then calls the original. The subprocess call, non-zero exit check, marker check, exception recording, command log, partial-run accounting, report writer and aggregate exit code are therefore the production code at lines 47 to 165 of the native-baseline runner. The oracle reads commands.json and results.json, which the production code writes, and requires the exact command prefix, so a run that continued past a failure would be caught.

Two nonblocking observations. First, the SIGKILL and exit-23 stimuli exercise the same production branch, the non-zero return code check; the distinct production branches covered are three: non-zero exit, timeout and missing marker. Second, on a timeout the production failure string is the raw Python TimeoutExpired message, which names the child argv but not the step name; the oracle's check that the step name appears in that string passes here only because the synthetic argv contains the step name, and would not hold for the real make or kernel argv. The step remains identified through the command record, whose name and error the oracle also binds to the failure string, so the inventory assertion is met, but the timeout diagnostic in results.json should not be read as self-identifying.

Disposition: STANDING-CHILD-COMMAND-CONTROL-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. S0-LL01-b [control] is complete at its stated scope and ready for the user's acceptance decision, which would make 32 accepted. Gate 31 accepted, 16 missing, one unreviewed of 48.

## Thirty-first Claude audit — target TYPEP membership and optimizer paths, at 949333c4 — 13 September 2026

Recorded by Claude directly. Scope: NATIVE-TARGET-PREDICATES-R1 and the target-predicates fixture (driver, runner, analyzer). Verdict: acceptable, no defect. No shared compiler, kernel, production tool or stub-backend source changed between 48270967 and this commit; the commit also updates the S0-LL01-b scope report to carry the thirtieth audit's two observations, which is accurate.

Verification: the retained verifier passed against the packet; all seven native sessions (normal, repeat, inherited, fixnum-only, shallow, member-data, host-ctype) were re-executed from the pinned inputs into a fresh work tree, and both raw captures of every session, the controls and the summary are byte-identical to the retained packet. Packet manifest with 27 files, the index locator hash, the catalog at ccl-evidence 33a4b0bd with 28 new rows and all 42 source pins at HEAD were verified. The two newly pinned spans in compiler/optimizers.lisp begin the TYPEP compiler macro and OPTIMIZE-TYPEP exactly.

The cause the packet names was confirmed in U1. The TYPEP compiler macro, when both the object and the type are constants and the type is known, folds the call by asking the host's own TYPEP; on the 64-bit host 536,870,912 is a FIXNUM and not a BIGNUM, which is the opposite of the target's signed 30-bit answer. The retained inherited capture shows exactly that: eight of the twenty-two probes change between the inherited and corrected sessions, the two boundary-adjacent FIXNUM probes and the OR-with-FIXNUM probe from true to false, the two BIGNUM probes, NOT FIXNUM and AND-with-NOT-FIXNUM from false to true, and NOT BIGNUM from true to false; the other fourteen, including the in-range fixnums, the large bignum, the floats, the MEMBER literals and the explicit bounds, are unchanged. OPTIMIZE-CTYPEP's first form returns NIL unless the target backend is the host backend, and the host-ctype control shows what that guard prevents: with host identity forced, the native optimizer emits a host fixnum range test for signed-byte 30 and a host typecode comparison against 231 for the byte-array type, both of which would be wrong lowering for the target.

Assessment of scope: the private wrapper translates FIXNUM through the source NX-TARGET-TYPE, BIGNUM to the integer complement of signed-byte 30, and recurses only through OR, AND and NOT, leaving MEMBER payloads and explicit bounds as data, which the member-data control and the literal probes check; the source OPTIMIZE-TYPEP body is spliced into a lexical scope so that its calls to TYPEP and SPECIFIER-TYPE-IF-KNOWN resolve to the translating versions without any global binding, and the driver checks that the five global functions, the macroexpand hook, the pass-2 hook and the traversal state are restored. The oracle computes membership independently in Python and reconstructs all 170 trace rows including the canonical bounds. The packet states plainly that SPECIFIER-TYPE-IF-KNOWN, TYPEP and the transitive type system still execute natively after translation, that class cells, the RESTART wrapper and the environment remain unqualified native objects, and that aliases, inferred types and nonconstant numeric TYPEP lowering are outside the probes.

One nonblocking observation: the twenty-two probes cover integers, floats, a keyword and MEMBER literals, but no probe exercises a user DEFTYPE alias that expands to FIXNUM, so the translation-before-expansion order in the recursive OPTIMIZE-TYPEP path is not yet witnessed; the packet already lists aliases as open.

Disposition: NATIVE-TARGET-PREDICATES-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. No inventory record changes state by this review. Gate 31 accepted, 16 missing, one unreviewed of 48; S0-LL01-b still awaits the user's acceptance.

## Thirty-second Claude audit — behavioral assertion control S0-LL02-b, at f35d2bfe — 13 September 2026

Recorded by Claude directly. Scope: STANDING-BEHAVIORAL-CONTROL-R1 and the behavioral-control fixture (WAT generator, execution adapter, assertion module, runner). Verdict: acceptable, no defect. The inventory edit is confined to the S0-LL02-b slot's status and runner fields; no shared compiler, kernel, production tool or stub-backend source changed between 08a6d5d2 and this commit.

Verification: the retained verifier passed against the packet with a fresh replay; the producer was re-executed from scratch and all six WAT sources, six binaries and six observation records, plus the observations, summary, slot gate and slot inventory, are byte-identical to the retained packet; that fresh output then passed its own verification. Both gate composites were reproduced from the HEAD inventory: the production gate on the unchanged accepted envelope returns 17 missing reasons including S0-LL01-b and S0-LL02-b, and a slot inventory built from HEAD for each of the two pending records returns only the unreviewed reason, so the ledger is 31 accepted, 15 missing, 2 unreviewed of 48 and the S0-LL01-b binding remains current. Index locator, packet and inventory hashes, all six source pins at HEAD and the catalog at ccl-evidence 8f0127bd with 46 new rows were verified.

Assessment against the LL02 contract, which requires an escaping mutable closure, inspection of every multiple value, observed cleanup order and effects, and mutants that drop an extra value, skip cleanup or remove capture: the factory returns a descriptor whose environment lives in linear memory after the factory frame has returned, two descriptors hold independent state, each B-shaped indirect call returns two Wasm results and six tagged values in the caller area, and the assertion module checks all six values, the primary result and count, the ordered cleanup markers, the separate cleanup effect and the capture contents. The five mutants each change exactly one thing in the WAT generator, confirmed by diffing the retained WAT sources, and the retained failure records show the exact first refusal the runner requires: the dropped fifth value fails "all six values" on the first call with the primary result still correct, the lost capture passes the first call and fails on the second, the skipped and reversed cleanups fail the cleanup-order assertion on the first call, and the exception-only skip passes three calls and fails during the fourth's unwinding with the exception payload and capture mutation preserved. The mutation option enters only the generator; the adapter and assertions take none. Engine traps, build failures and timeouts fail the producer rather than counting as rejections.

Two nonblocking observations. First, the "closure" is a code index plus environment pointer in a fixed-size descriptor, not a CCL function object; the packet says so and claims no layout, collector, threading or generated-code qualification. Second, the cleanup log and effect globals are reset at the start of every call, so cleanup order is observed per call rather than cumulatively; the assertions are consistent with that and it does not weaken the mutant refusals.

Disposition: STANDING-BEHAVIORAL-CONTROL-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. S0-LL02-b [control] is complete at its stated scope and ready for the user's acceptance decision. Together with S0-LL01-b, two slots now await acceptance; accepting both would make 33.

## Thirty-third Claude audit — DEFTYPE expansion before target translation, at bc3b9bd1 — 13 September 2026

Recorded by Claude directly. Scope: NATIVE-TARGET-ALIASES-R1 and the target-aliases fixture (driver, alias definitions, runner, analyzer). Verdict: acceptable, no defect. This packet answers the thirty-first audit's observation and confirms it was a real defect in the reviewed predicate builder, not a gap in coverage only. No shared compiler, kernel, production tool, stub-backend or reviewed predicates-fixture source changed.

Verification: the retained verifier passed; all seven native sessions (normal, repeat, prior, translate-first, shallow, member-data, optimizer-leak) were re-executed from the pinned inputs into a fresh work tree, and every capture, the controls and the summary are byte-identical to the retained packet. The differences record is a preparation artifact, not a runner output, and its content was confirmed independently from the retained captures: exactly ten constant probes and three optimizer probes change between the prior builder and the corrected one, and the optimizer-leak mutant changes no constant probe but the same three optimizer answer vectors, which separately witnesses the canonicalization path. The four corrected-builder mutants' file traversals are byte-identical to the normal traversal; only the prior builder's traversal differs, and the verifier accepts it solely through a bijective renaming of the observer identity fields with every other field equal. Packet manifest, index hash, all 46 source pins at HEAD and the catalog at ccl-evidence fb3d811b with 29 new rows were verified; the three new l1-typesys.lisp spans begin DEFTYPE, EXPAND-TYPE-MACRO and TYPE-EXPAND exactly.

The cause was confirmed in U1: TYPE-EXPAND loops looking up a symbol, or the head of a compound form, in the expander table and applies the expander until none matches, and the native TYPEP and SPECIFIER-TYPE paths call it. So a wrapper that translates FIXNUM before calling native code leaves TARGET-SMALL untouched, and the native expansion afterwards restores the host's FIXNUM. The corrected wrapper expands the supplied aliases first, recursing through OR, AND and NOT, and translates leaves last; the private expanders are built with the same PARSE-MACRO-INTERNAL call and star default that EXPAND-TYPE-MACRO uses, installed only in a dynamic copy of the expander table, and the driver checks that the original table object and its entries are restored. Expansion is bounded by an ancestor check, a depth limit and a per-normalization expander allowance, and the cyclic and growing aliases are tested refusals. The five optimizer expansions contain no host FIXNUMP or BIGNUMP tests; they test membership through builtin type cells for the normalized target type, which is why native evaluation and the Python interpreter agree with target membership on all 25 values.

Assessment of scope: the packet qualifies the ten supplied alias forms and their type positions only; arbitrary expander bodies, resident aliases, element-type positions, native class objects and nonconstant target lowering are stated as open, and the native evaluation of the nonconstant expansions is labelled diagnostic. Three development failures from fixture setup mistakes are retained with their sources and logs.

One nonblocking observation: the prior-builder comparison relies on the identity-bijection equivalence rather than byte identity because loading the prior builder shifts observer identity numbers; the equivalence requires exact equality of every non-identity field and is adequate, but future fixtures that compare against an earlier builder should prefer an arrangement that keeps identities stable.

Disposition: NATIVE-TARGET-ALIASES-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. No inventory record changes state by this review. Gate 31 accepted, 15 missing, 2 unreviewed of 48.

## Thirty-fourth Claude audit — initializer failure control S0-LL01-a, at f9cde02b — 13 September 2026

Recorded by Claude directly. Scope: STANDING-INITIALIZER-CONTROL-R1 and the initializer-control fixture (WAT generator, loader, execution adapter, oracle, cases, runner). Verdict: acceptable, no defect. Also confirmed on this pass: the acceptance commit d7b5b1a5 records the user's words "accept both then" against the thirty-second and thirty-third audits at 8a3a1d34, the combined envelope holds 33 records all with disposition ACCEPTED including S0-LL01-b and S0-LL02-b, and the production gate on that envelope against the HEAD inventory returns BLOCKED with 15 missing reasons and none unreviewed, so the two acceptances stand at 33 accepted.

Verification of the new packet: the retained verifier passed with a fresh replay; the producer was re-executed from scratch and all 45 deterministic files, the three WAT sources and binaries, the manifest, all thirteen observation records, the loader mutants, the ready products, the observations, summary, slot gate and slot inventory, are byte-identical to the retained packet; that fresh output then passed its own verification. A slot inventory built from HEAD for S0-LL01-a returns only the unreviewed reason, so the ledger is 33 accepted, 14 missing, 1 unreviewed of 48. The inventory edit changes the S0-LL01-a status and runner fields and, in two unrelated descriptions, replaces Unicode escapes with the literal characters; the parsed values are unchanged and the gate run above confirms all 33 accepted bindings remain current. Index locator, packet and inventory hashes, all eight source pins at HEAD and the catalog at ccl-evidence 6fa9b36c with 70 new rows were verified. No shared compiler, kernel, production tool, census fixture or earlier Stage 0 fixture changed.

Assessment against the LL01 contract and the slot assertion, which require early, middle and late initializer failures, one omitted required module, a non-passing aggregate, precise first-failure diagnostics, no accepted partial image, and diagnostic continuation that records errors but stays ineligible: nine real Wasm initializers in three modules each write an effect and a call-order record before either throwing a real Wasm exception or recording completion, so a failure leaves observable partial state; the loader preflights every module by digest before running any initializer, blocks dependents of failed steps in diagnostic mode, keeps the first error with a null-coalescing assignment, and publishes the ready artifact only when every step completed and no error is recorded. The oracle is literal: a nine-character state string per case, expected physical memory, expected step records, expected error list and first failure, expected fetches and modules, and the ready snapshot for the one positive case. I inspected the retained mutant outcomes: the accept-partial loader reports PASS and publishes with a failed ninth step, rejected on the aggregate; the overwrite-first loader reports the second failure as first, rejected on the first failure; the publish-before-check loader still reports FAIL but leaves a ready file on disk, rejected on partial publication, which is the case that shows aggregate status alone is not enough; the conceal-step-failure loader labels the failed step completed while its error remains recorded, rejected on the step records. Each mutant is a one-line change to the loader, confirmed by diffing the retained sources, and the loader itself takes no mutation option. The missing-module case is a real ENOENT read of an absent filename, refused before any initializer runs. The three preparatory runs, including the earlier injected-ENOENT variant that the final run replaced, are retained.

Two nonblocking observations. First, the Stage 0 coverage of the LL01 "trigger a timeout" clause rests entirely on the accepted S0-LL01-b child-command timeout; an initializer that never returns is exercised by neither control, and this packet says so explicitly. Second, the harness is a fixed manifest of three modules and nine steps with dependencies only within each chain; cross-chain dependencies, arbitrary manifests and the future Stage 1 loader are stated as out of scope.

Disposition: STANDING-INITIALIZER-CONTROL-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. S0-LL01-a [control] is complete at its stated scope and ready for the user's acceptance decision, which would make 34 accepted. Gate 33 accepted, 14 missing, one unreviewed of 48.

## Thirty-fifth Claude audit — native object serialization witness, at 7cb8d5af — 13 September 2026

Recorded by Claude directly. Scope: NATIVE-TARGET-OBJECTS-R1 and the target-objects fixture (driver, analyzer, runner). Verdict: acceptable, no defect. Also confirmed on this pass: the acceptance commit 6f6914a1 records the user's words "accept ll01-a" against the thirty-fourth audit at 72939647; the new combined envelope holds 34 records all with disposition ACCEPTED, and the production gate on it against the HEAD inventory returns BLOCKED with 14 missing reasons and none unreviewed, so the ledger reads 34 accepted, 14 missing, 0 unreviewed of 48. No shared compiler, kernel, production tool, Stage 0 fixture or earlier census fixture changed across either commit.

Verification: the retained verifier passed; both native sessions were re-executed from the pinned inputs into a fresh work tree, and all seven data FASLs, both captures, the controls and the summary are byte-identical to the retained packet. Packet manifest with 19 files, the index locator hash, all 47 source pins at HEAD and the catalog at ccl-evidence dd52a2e6 with 20 new rows were verified. The four development captures, including the zero-exit run whose fresh-registry observation was wrong and the analyzer refusal replayed on the original bytes, are retained.

The claim was confirmed in U1: the MAKE-LOAD-FORM method on CLASS-CELL in l1-clos.lisp returns a deferred FIND-CLASS-CELL lookup by name; ISTRUCT-CELL-P in nfcomp.lisp recognizes a registered cons by identity in the istruct registry; the FASL writer emits the registered cell under opcode 71 with only its symbol, and the level-0 loader's handler for that opcode calls REGISTER-ISTRUCT-CELL; opcode 39 is DEFPARAMETER. So both runtime objects the restore-file expansions reference already serialize by symbolic key in U1, and the two replaced-payload dumps producing byte-identical files show the native wrapper never enters the FASL. The fixture uses the actual class cell and registered restart pair joined from the 293 source-routed expansion events, dumps only data through the unchanged native scanner and writer, and loads through the unchanged native loader; the independent decoder reads the complete 132-byte file with an allowlist of eleven opcodes and refuses native function or code payloads, and the five roundtrips show that fresh or absent registries yield distinct empty cells while repeated loads preserve keyed identity and the shared class-cell reference.

Assessment of scope: this is a witness of existing native behavior, not a correction, and the packet says so; what it establishes for the port is that the class cell and restart cell must be materialized as target registry objects with their identity, sharing and contents supplied by the port, and that the three lexical-environment uses in this trace are compile-toplevel-only. Arbitrary object constants, the remaining type-system helpers, full macro-environment qualification and the five boundary stops remain open, and no Wasm materialization is claimed.

One nonblocking observation: because the wrapper payload is erased on the way out, the RESTART wrapper's contents will have to come from the target's own class system at load time; the packet names that as the port's obligation, and it should appear as an explicit census node when the registry materialization work is scheduled.

Disposition: NATIVE-TARGET-OBJECTS-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. No inventory record changes state by this review. Gate 34 accepted, 14 missing, zero unreviewed of 48.

## Thirty-sixth Claude audit — artifact identity control S0-LL22-a, at 8670212e — 14 September 2026

Recorded by Claude directly. Scope: STANDING-ARTIFACT-IDENTITY-CONTROL-R1 and the artifact-identity-control fixture (runner, cases). Verdict: acceptable at its stated scope, no defect. The inventory edit is confined to the S0-LL22-a slot's status and runner fields; no shared compiler, kernel, production tool or earlier fixture changed.

Verification: the retained verifier passed; the producer was re-executed from scratch and all 68 deterministic files, the synthetic build bundles, the 38 quarantined input records, the observations, summary, cases, inventory snapshot, slot gate and slot inventory, are byte-identical to the retained packet, and that fresh output passed its own verification. The production gate on the unchanged 34-record accepted envelope against the HEAD inventory returns 14 missing reasons including S0-LL22-a, and a slot inventory built from HEAD returns only the unreviewed reason, so the ledger is 34 accepted, 13 missing, 1 unreviewed of 48. Index locator, packet and inventory hashes, all four source pins at HEAD and the catalog at ccl-evidence 1c7f11b6 with 85 new rows were verified. The retained observations tally two PASS, 35 FAIL and one BLOCKED with the diagnostic families the cases file predicts: 21 hash or missing-file refusals, 11 role refusals including the duplicate that masks the absent ABI artifact, and one each for source revision, changed contract, stale snapshot and unreviewed record.

Assessment against the slot assertion, which requires source, implementation, test, ABI, template and installed-binary identities to be verified against retained hashes and mixed or stale builds rejected: the two synthetic builds share identical template bytes but carry different hand-encoded module bytes, and the case that offers the template's hash for the installed binary is refused, so a checker relying on template identity alone would have been caught. Every refusal comes from the unchanged production gate reading real files at real paths; the inner records keep synthetic PASS labels, so no refusal can come from an execution status. The runner's own bound checks that the mixed, missing-file and omitted-role families each cover all ten roles.

One observation for the acceptance decision, not blocking at the packet's stated scope. The gate's required-role check is driven by the inventory's required_record_roles list. The control's synthetic inventory requires all ten roles, and the gate enforces them; the production inventory at HEAD requires only implementation, test, schema and log. The hash check applies to every declared artifact in production, but a production record that simply omits an ABI, template, installed-binary, image, host-compiler or options artifact would not be refused for the missing role today. The scope report says the role policy is inventory-driven and that the fixture does not change other inventories' roles, which is accurate, but it does not say in so many words that the production policy is narrower than the control. When Stage 1 begins producing real ABI schemas, templates and installed binaries, the production list will need widening for the LL22 obligation's role coverage to hold outside this control.

Disposition: STANDING-ARTIFACT-IDENTITY-CONTROL-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. S0-LL22-a [control] is complete at its stated scope and ready for the user's acceptance decision, which would make 35 accepted, with the role-policy observation recorded as part of that scope.

## Thirty-seventh Claude audit — target architecture macro dispatch, at 4feaef69 — 14 September 2026

Recorded by Claude directly. Scope: NATIVE-TARGET-DISPATCH-R1 and the target-dispatch fixture (driver, analyzer, runner). Verdict: acceptable, no defect. This is a witness of U1's existing lookup chain; the packet claims no new host-versus-target defect and I found none. No shared compiler, kernel, production tool or earlier fixture changed.

Verification: the retained verifier passed; all six native sessions (normal, repeat, host-selector, host-fallback, cached-expander, discard-values) were re-executed from the pinned inputs into a fresh work tree, and every capture, the controls and the summary are byte-identical to the retained packet. Packet manifest with 24 files, the index hash, all 42 source pins at HEAD and the catalog at ccl-evidence 69349297 with 25 new rows were verified.

The four source bodies were confirmed in U1 at the pinned spans: BACKEND-ARCH-MACROEXPAND in compiler/backend.lisp takes the architecture name from the current target backend, looks up the operator and either funcalls the expander or signals the no-arch-specific-macro error; ARCH-MACRO-FUNCTION is a GETHASH on the architecture's macro table; TARGET-ARCH-MACROS signals the unknown-arch error when FIND-TARGET-ARCH finds no registered architecture of that name. The driver rewrites exactly one form in the dispatcher, the FUNCALL site, into an observation bridge that preserves multiple values and nonlocal exit, wraps the lookup to record identities, and compiles the four bodies as lexical functions so the front-end record shows five functions and six lexical edges with no global lookup leak. The two architecture-macro events in the restore file are joined: the kernel-global-pointer macro finds no Wasm binding and is refused before any call, and the batch-flag macro selects the existing description expander and returns the named unimplemented service dependency. The nine registry probes run against a copied descriptor and table under a rebound architecture list, and the same nine are run through the unchanged native dispatcher in the same session with equal results, the only permitted difference being the deliberately fresh environment identity. The four native mutants stand for real inherited risks, selecting the host architecture, falling back to a host macro, caching the first expander and dropping extra values, and each fails at its expected reason with the file traversal unchanged.

One nonblocking observation: the packet qualifies the dispatch path for the two operators that occur in the restore file and the nine transitions probed; as it says, it gives no static bound on the set of architecture macros a wider traversal may reach, so the candidate bound for this computed-call family will grow with the traversal.

Disposition: NATIVE-TARGET-DISPATCH-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. No inventory record changes state by this review. Gate 34 accepted, 13 missing, one unreviewed of 48; S0-LL22-a awaits the user's acceptance.

## Thirty-eighth Claude audit — production artifact policy, at a6138639 — 14 September 2026

Recorded by Claude directly. Scope: PRODUCTION-ARTIFACT-POLICY-R1, its decision record, the gate change and the policy test. Verdict: acceptable, no defect. Also confirmed on this pass: the acceptance commit d6f705c4 records the user's words "accept S0-LL22-a, add the production-policy enhancement, and proceed with LL23 and LL24" against the thirty-sixth audit at 08151452, with the narrower production-role list disclosed in the decision; the combined envelope holds 35 records all with disposition ACCEPTED; the production gate on it against the HEAD inventory returns BLOCKED with 13 missing reasons and none unreviewed at the inventory as it stood before LL23-a and LL24-a were registered.

Verification of the policy change: the gate now takes the union of the inventory-wide required roles and an optional per-test list, refuses a missing or empty global list, and fails the whole inventory on any non-list, duplicate, non-string or blank role entry, which is fail-closed. I recomputed the contract hash of every inventory test at d6f705c4 and at a6138639 with the unchanged binding tool: exactly the ten pending tests named in the policy changed and no accepted slot's hash moved, and the gate run above shows all 35 accepted bindings current. The policy test was replayed against the retained packet and re-executed from scratch with identical summary, observations and binding check: eleven complete synthetic records pass and 102 omission, waiver and malformed-list cases are refused, and the test also shows that a local role change alters only that test's contract. The decision record pins the previous and current inventory by hash and lists each changed entry with both contract identities. Packet manifest, index hashes for the packet and the decision, three source pins at HEAD and the catalog at ccl-evidence 91a05f8b with 19 new rows were verified.

Assessment of scope: the ten lists are appropriate to what each pending slot will produce, six Wasm build roles for the hand-built and generated-code slots, four native roles for the census and repeatability slots, and no additions for controls or the desk review; the policy says Stage 1's inventory must carry the complete applicable set, which is the statement the thirty-sixth audit asked for. The gate implementation change is the first production-tool edit since the alternating schedule began, and its own regression run covers the existing gate, binding and acceptance controls.

Disposition: PRODUCTION-ARTIFACT-POLICY-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. No inventory record changes state by this review.

## Thirty-ninth Claude audit — structured Wasm diagnostics S0-LL23-a, at 39c297c0 — 14 September 2026

Recorded by Claude directly. Scope: STRUCTURED-DIAGNOSTICS-R1 and the diagnostics fixture (program, ABI schema, decoder, reporter, execution adapter, oracle, cases, runner). Verdict: acceptable, no defect. This is the first non-control slot executed since the ABI corpus, evidence kind hand-built Wasm execution, variant full. The inventory edit is confined to the S0-LL23-a status and runner fields; no shared compiler, kernel or earlier fixture changed.

Verification: the retained verifier passed; the producer was re-executed from scratch and the two bundles, all 28 quarantined case directories, the observations, summary, role-omission outcomes, slot gate and slot inventory, 143 files, are byte-identical to the retained packet, and that fresh output passed its own verification. The ten omission records differ from the retained ones only in the run timestamp, the command path and the inventory snapshot hash, because the HEAD inventory has since moved with the LL24-a registration; the S0-LL23-a contract hash is unchanged and a slot inventory built from HEAD returns only the unreviewed reason. I checked one attribution independently: the retained unreachable fault names function index 1 at binary offset 117, and the WABT disassembly of the same binary places the unreachable instruction at 0x75. Packet manifest with 175 files, index locator and packet hashes, all ten source pins at HEAD and the catalog at ccl-evidence 3f86ec4b with 176 new rows were verified.

Assessment against the slot assertion, which requires diagnostics to name the exact build, logical code ID, entry kind, signature, slot and faulting operation, preserve the first error, bound output, and never label a last-observed event as the failing frame: the harness verifies every bundle file by digest before instantiation and refuses a stale binary, a stale map and an ABI signature inconsistent with the binary; the decoder reads the whole small module and refuses unsupported shapes; the reporter parses V8's Wasm stack lines to function index and byte offset and joins them to the decoded map, with the observer event carried as context only. Real V8 traps are exercised for unreachable, memory bounds, division by zero through three nested frames, and for indirect-call signature and table-bounds faults attributed to the dispatcher's call_indirect rather than the unentered target; the second build carries its own identity; three successive failures keep the first; twenty-five failures retain three and count 22 dropped within the 8,192-byte budget; a host import error, a hidden stack and an unreadable top frame yield UNAVAILABLE with no inferred attribution. Twelve one-line reporter mutants are rejected by the same oracle, including the development-found defect where skipping an unreadable top frame attributed the trap to its caller. Ten omissions of the real artifact roles, including the six added by the new production policy, are refused by the production gate on the genuine result.

Two nonblocking observations. First, the unavailable and obscured-top cases simulate a foreign stack format by rewriting the error's stack text inside the harness, and the frame parser's pattern is V8's; the packet states Node/V8 scope and the Stage 1 extension will meet other engines. Second, the entry kinds fixed, generic, bridge and dispatch are fixture labels carried in the ABI schema, not a qualification of B's entry protocol, as the packet says.

Disposition: STRUCTURED-DIAGNOSTICS-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. S0-LL23-a [full] is complete at its stated scope and ready for the user's acceptance decision.

## Fortieth Claude audit — project ledger control S0-LL24-a, at fdb6f0b5 — 14 September 2026

Recorded by Claude directly. Scope: PROJECT-LEDGER-CONTROL-R1, the ledger checker tool, the ledger policy and the ledger-control fixture. Verdict: acceptable, no defect. The inventory edit is confined to the S0-LL24-a status and runner fields.

Verification: the retained verifier passed; the producer was re-executed from scratch and the pinned project snapshot, controls, observed ledger, summary, slot gate and slot inventory are byte-identical to the retained packet, and that fresh output passed its own verification; the live-ledger record in the packet is a preparation artifact produced after publication, not a runner output. I ran the production checker against the live project at HEAD: PASS, project gate BLOCKED, 35 accepted, 11 missing, 2 unreviewed, 35 accepted records preserved, one authorized criterion decision, ten unresolved dependencies. A slot inventory built from HEAD returns only the unreviewed reason. Packet manifest with 30 files, index hashes, all six source pins at HEAD and the catalog at ccl-evidence 19507863 with 31 new rows were verified.

Assessment against the slot assertion, one current ledger with separate history, explicit authorization for criterion changes, preservation of accepted evidence scope, and delivery claims linked to identified artifacts: the checker requires the policy to name one ledger file, one status file and a distinct history file with a dated heading; requires the ledger's inventory pin to match the actual inventory bytes; validates inventory invariants; replays the criterion chain from the pinned baseline inventory through each authorized decision, recomputing every changed entry and both contract identities, and refuses any current criterion not reached by that chain, with status, review disposition and runner treated as operational fields; compares all 35 accepted result objects in full with the pinned baseline; binds every active envelope; requires the accepted aggregate to contain exactly the accepted active records; checks new submissions' artifact bytes, required roles, execution metadata and assertion coverage; recomputes the counts and reasons and requires the status row to be derived from them. The fixture freezes the project as it stood before its own result was published, so the test cannot depend on itself, and the 31 controls damage input bytes in memory while running the unchanged assessor, including competing authorities, a pending record pointed to as the accepted index, a promoted accepted scope, a lost accepted record, a substituted evidence class, unapproved criteria, removal of R6 or a required test, weakened global roles, and missing or corrupted decision authorization.

Two nonblocking observations. First, the checker cannot authenticate that a quoted authorization was spoken; it enforces that one is recorded, dated and consistent, as the packet states. Second, the policy pins the accepted baseline and the criterion decisions by hash, so every future acceptance or authorized criterion change must also update the policy file, and the checker will refuse the ledger until it does; that is the intended discipline but it adds a step to each acceptance.

Disposition: PROJECT-LEDGER-CONTROL-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. S0-LL24-a [control] is complete at its stated scope and ready for the user's acceptance decision. With S0-LL23-a, two slots await acceptance; accepting both would make 37. Gate 35 accepted, 11 missing, 2 unreviewed of 48.

## Forty-first Claude audit — source SETF lookup and expander protocol, at 413b938c — 14 September 2026

Recorded by Claude directly. Scope: NATIVE-TARGET-SETF-R1 and the target-setf fixture (driver, analyzer, runner). Verdict: acceptable, no defect. Also confirmed on this pass: the acceptance commit 7f78dfcb records the user's words "accept them and proceed" against the thirty-ninth and fortieth audits at 5dda6403; the new combined envelope holds 37 records all with disposition ACCEPTED including S0-LL23-a and S0-LL24-a; the production gate on it against the HEAD inventory returns BLOCKED with 11 missing reasons and none unreviewed; and the production ledger checker run live at HEAD returns PASS with the gate BLOCKED at 37 accepted, 11 missing, 0 unreviewed and all 35 baseline records preserved. Codex's note that the frozen baseline need not move for an acceptance is correct and supersedes the fortieth audit's procedural observation: the checker validates new acceptances directly, and only authorized criterion changes extend the decision chain. No shared compiler, kernel, production tool or earlier fixture changed across either commit.

Verification of the new packet: the retained verifier passed; all seven native sessions (normal, repeat, cached-method, drop-values, drop-environment, ignore-local, drop-getter) were re-executed from the pinned inputs into a fresh work tree, and every capture, the controls and the summary are byte-identical to the retained packet. Packet manifest with 30 files, index hashes for the run record, summary and verification, all 46 source pins at HEAD and the catalog at ccl-evidence dc1906c8 with 31 new rows were verified. The four inverse declarations were confirmed at their U1 sources: the saved-method variable in l1-utils.lisp, the simple-vector reference and interrupt level in lib/macros.lisp, and slot-value in l1-clos-boot.lisp; the SETF macro is read from lib/macros.lisp and the registry lookup from lib/setf.lisp, where it is a single GETHASH on the SETF method table.

Assessment of the mechanism: the driver rebuilds SETF from its source form with the lookup helper supplied lexically, rewriting exactly one computed FUNCALL site into an observation bridge and one function-information query into a forwarding wrapper, both transparent in normal mode; the compiled record shows the private entry, the private lookup function and one lexical edge. The five SETF expansions in the restore file are joined to their registry results: four resolve to named setter symbols matching the declarations above, and the stream-state accessor is absent from the registry and expands through its source accessor macro to a simple-vector reference whose own lookup resolves to the setter; no callable expander is invoked anywhere in the file. Nine probes run under target state against a copied registry and are compared with the unchanged native SETF macro in the same session under a bijective renaming of generated temporaries: a callable method's five values reach the expansion with place-once and left-to-right value evaluation, a replacement takes effect immediately, symbol, absent and present-NIL entries take the ordinary paths with distinct presence flags, a local function binding bypasses the registry, a nonlocal exit restores the copied entry, and a direct bridge probe checks all five values including the getter form SETF itself discards. The five native mutants stand for real inherited risks and each fails at its expected reason with the file traversal unchanged; the getter-only mutant is caught only by the direct five-value probe, which is why that probe exists.

Two nonblocking observations. First, the observed callable-expander population for this file is zero, so the packet establishes the branch choices and the protocol, not a bound on later files, as it says. Second, the packet's index entry sits among the current runs with a new evidence kind and no inventory binding, whereas every earlier census diagnostic is listed under auxiliary records; neither the gate nor the ledger checker reads that section, so nothing is affected, but the placement should be made consistent.

Disposition: NATIVE-TARGET-SETF-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. No inventory record changes state by this review. Gate 37 accepted, 11 missing, zero unreviewed of 48.

## Forty-second Claude audit — R6 control on the actual census registration S0-LL22-b, at 23bb1455 — 14 September 2026

Recorded by Claude directly. Scope: R6-REGISTRATION-CONTROL-R1, the new comparator tool, the r6-control fixture (runner, publisher, inputs, cases) and the results envelope published at the evidence root. Verdict: acceptable, no defect. Also noted: f7b11c67 moves the SETF diagnostic's index entry to the auxiliary records with its review block intact, answering the forty-first audit's placement observation; nothing else changes. The inventory edit is confined to the S0-LL22-b status and runner fields; no shared compiler, kernel, census fixture, gate, binding or ledger tool changed, and the registration unit itself is unchanged since its acceptance under S0-LL08-a.

Verification: the retained verifier passed, reapplying and removing the registration unit in its own pristine archive and reproducing all 45 comparison cases; the producer was re-executed from scratch and the summary, observations, source witness, both systems sources, cases, inventory snapshot, slot gate and slot inventory are byte-identical to the retained packet, with the producer envelope equal in every field except timestamp, command, toolchain and the artifact list's execution-specific entries; that fresh output passed its own verification. The production gate on the unchanged 37-record accepted envelope against the HEAD inventory returns 11 missing reasons including S0-LL22-b, a slot inventory built from HEAD with the G0 prerequisite returns only the missing-prerequisite and unreviewed reasons, and the live ledger checker returns PASS with the gate BLOCKED at 37 accepted, 10 missing, 1 unreviewed. Packet manifest with 38 files, index and envelope hashes, publication record with 46 references resolved without copying the native archives, all 15 source pins at HEAD and the catalog at ccl-evidence 166e04d3 with 39 new rows were verified.

Independent check of the intentional change: the two retained systems sources differ by exactly the two module lines the packet names. I decoded nothing myself but accounted for every byte: taking the registered FASL, deleting the 195-byte span the comparator attributes to the two module entries and restoring the original bytes of the file-length field, the source-form span and the list count yields the baseline FASL exactly, and aligning the two files outside the insertion leaves five differing bytes, all inside those three fields. The 64-byte package thunk and every other executable byte are unchanged, which is the LL22 requirement that an intentional shared change be accounted for rather than normalized.

Assessment against the slot assertion, all four R6 categories and evaluated R6a applied to the actual shared-source patch with narrow classification and rejection of unexplained executable or operator differences: the comparator pins the source archive, bootstrap, kernel, commands and environment; requires the 243 target and kernel source files and every other original file except the one declared change to be identical; binds the two observation-only additions individually; requires the full 164-file native manifest with exactly one changed FASL during registration and all 164 restored; decodes both complete FASLs with a bounded reader that refuses executable opcodes and unknown shapes and never executes the thunk or the source-note form; requires the 279 evaluated operator records including 12 reserved slots, the backend and feature snapshot, the vinsn templates, the compiled ABI probe and the module lists to be equal or exactly joined; and reuses both accepted 21,843-pass native suites with identical inventories. The 44 controls alter copies after the real input identities are checked, the FASL cases rebind their hashes so they reach the semantic comparison, and each fails at its literal expected reason, including a normalization request, a whole-file exemption, an equivalent extra no-op, a changed code byte and a swapped operator pair. Four producer failures during development were harness errors and are retained with their sources.

Two nonblocking observations. First, the native behaviour category is discharged by reusing the accepted 12 September run rather than by a new build; the packet says so, and the same-host repeatability claim remains G0's. Second, the comparator profile is deliberately specific to this two-entry patch, so any later shared-source edit needs its own explained profile and R6 run, which the scope report states.

Disposition: R6-REGISTRATION-CONTROL-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. S0-LL22-b [control] is complete at its stated scope and ready for the user's acceptance decision, which would make 38 accepted. Gate 37 accepted, 10 missing, one unreviewed of 48.

## Forty-third Claude audit — lexical callback bounds in the DECLAIM and APPLY expanders, at 41457604 — 14 September 2026

Recorded by Claude directly. Scope: NATIVE-LEXICAL-CALLBACKS-R1 and the lexical-callbacks fixture (driver, analyzer, runner). Verdict: acceptable, no defect. Also confirmed on this pass: the acceptance commit bb439b2e records the user's words "accept it" against the forty-second audit at a668149c; the new combined envelope holds 38 records all with disposition ACCEPTED including S0-LL22-b; the production gate on it against the HEAD inventory returns BLOCKED with exactly the ten missing reasons and none unreviewed; and the live ledger checker returns PASS with the gate BLOCKED at 38 accepted, 10 missing, 0 unreviewed. No shared compiler, kernel, inventory, tool, Stage 0 fixture or earlier census fixture changed across either commit.

Verification of the new packet: the retained verifier passed; both native sessions were re-executed from the pinned inputs into a fresh work tree, and the flow, SETF and traversal captures, the controls, the joins record and the summary are byte-identical to the retained packet. Packet manifest with 17 files, the index hash, all 49 source pins at HEAD and the catalog at ccl-evidence 5def2e77 with 18 new rows were verified. The two pinned source spans were confirmed in U1: the span in nx-basic.lisp begins the DECLAIM macro and contains the MAPCAR over a local lambda, and the span in optimizers.lisp begins the APPLY compiler macro and contains the FLET of QUOTIFY and the MAPCAR over it. The joins record shows the DECLAIM callback bound through one LET* binding to a local prototype and the APPLY callback bound through LET* and FLET to the QUOTIFY prototype, which is what the scope report claims.

Assessment of the mechanism: the driver installs a temporary pass-2 observer on the owner thread that records the untouched acode of the two source-rebuilt expanders before the native compiler consumes it, forwards every return value, and restores the entry on normal and nonlocal exit, with both restorations checked. The analyzer follows a computed call's function variable only through a uniquely identified lexical binding whose body contains the use, accepts a local function value or a chain of immutable lexical references, requires the resulting function to belong to the same lexical owner, scans every use of the variable across the function and its children, and refuses a bound if either the compiler's assignment bit or an observed write is present or any use is unclassified. The eight target-front-end probes exercise exactly those rules, with literal, FLET, shadowed and closed callbacks bound and parameter, reassigned, closure-written and sequential-scope callbacks left unresolved; the two structural-write controls confirm the write scan does not depend on the compiler's flag; and the inherited SETF and traversal checker still passes on the new capture.

Two nonblocking observations. First, this slice has no native mutant sessions; the analyzer's rules are witnessed by the eight probes and the 21 checker mutations, which serve the same purpose, and the packet is explicit that the eight probes stop before code generation. Second, the bounds identify code prototypes for two sites inside the expansion machinery; they say nothing about the 1,729 runtime computed calls in the whole-build observation, and the APPLY branch containing the second site is not claimed to have run during the restore-file traversal, both of which the packet states.

Disposition: NATIVE-LEXICAL-CALLBACKS-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. No inventory record changes state by this review. Gate 38 accepted, 10 missing, zero unreviewed of 48.

## Forty-fourth Claude audit — typed conversions S0-LL07-a, at 1c1022a3 — 14 September 2026

Recorded by Claude directly. Scope: TYPED-CONVERSIONS-R1 and the conversions fixture (program, ABI schema, binary decoder, harness, oracle, cases, runner). Verdict: acceptable, no defect. Also noted: f53e664d is documentation only. It adds the native-runtime-semantics checklist that followed the reviewer's internals assessment to the outline, decisions, plan and risk plan, regenerates the two reading copies, and the document manifest hashes match the committed files. It changes no inventory, criterion, evidence or acceptance, and the ledger checker passes across it. The inventory edit in 1c1022a3 is confined to the S0-LL07-a status and runner fields; no shared compiler, kernel, tool, contract, census fixture or earlier Stage 0 fixture changed.

Verification: the retained verifier passed, rebuilding both variants and all nineteen mutants and regrowing memory in each; the producer was re-executed from scratch and all 216 deterministic files, both bundles with their binaries and observations, the nineteen quarantined bundles, the observations, summary, role-omission outcomes, slot gate and slot inventory, are byte-identical to the retained packet, and that fresh output passed its own verification. The production gate on the unchanged 38-record accepted envelope against the HEAD inventory returns ten missing reasons including S0-LL07-a, a slot inventory built from HEAD returns only the unreviewed reason, and the live ledger checker returns PASS with the gate BLOCKED at 38 accepted, 9 missing, 1 unreviewed. Packet manifest with 253 files, index locator and packet hashes, all ten source pins at HEAD including the layout contract, and the catalog at ccl-evidence 01db2064 with 254 new rows were verified. The retained development record shows two successful producer attempts with identical binaries and observations, the second changing only verifier failure retention and the operational inventory status.

Independent checks: I recomputed the expectations of all sixty stateless integer cases in the fixnum, address and logical-ID families from the case inputs alone and they agree, the only differences being the three host-input cases where the harness refuses out-of-range JavaScript arguments before Wasm is entered, which the oracle models and my naive model did not. The retained observations show real memory grown from one page to 2,147,549,184 bytes with the old view detached, unequal car and cdr payloads stored at 0x7ffffff8, 0x80000000 and 0x8000fff8 and read back through tagged pointers with the D1 displacements, and a header word stored at 0x80000010 and read through a misc-tagged pointer six bytes above it. The harness contains no signed bitwise operation on an address; every address leaving Wasm passes through one unsigned normalization and byte offsets are plain Numbers.

Assessment against the slot assertion, which requires signed fixnum, raw address, logical-ID and typed-slot conversions checked separately, synthetic addresses above 2 GiB retaining bits through JavaScript, and checked exhaustion rejecting invalid IDs and capacities: the four families are separate case groups with separate mutants; boxing refuses both ends of the 30-bit range and unboxing uses an arithmetic shift, with the logical-shift mutant caught at the minimum; a raw address above the fixnum range is refused rather than truncated; synthetic pointers up to 0xfffffff6 round-trip through tag, untag and effective-address arithmetic in 64-bit checked form; the ID registry issues, reserves, validates by issuance rather than by encoding, and reaches exhaustion with the counter set near 2^29; the slot registry keeps capacity, a reserved prefix, an explicit handle kind, a signature key and a role, and refuses an uninstalled entry before dispatch, while a tagged ID with the same bits as a callable slot is refused. All nineteen single-site mutants fail at their named first case, and the ten omissions of required artifact roles are refused by the production gate on the genuine result.

Two nonblocking observations. First, the reserved slot prefix is read from the fixture module's own element segment rather than a C link map, because there is no C module here; the packet says so, and the production reservation rule in D5 remains for the runtime fixtures. Second, the memory maximum is 32,769 pages rather than the full wasm32 range, so the near-4 GiB pointers are converted and never dereferenced; the obligation's real-allocation requirement is met on both sides of the 2 GiB boundary, and the packet states the bound.

Disposition: TYPED-CONVERSIONS-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. S0-LL07-a [full] is complete at its stated scope and ready for the user's acceptance decision, which would make 39 accepted. Gate 38 accepted, 9 missing, one unreviewed of 48.

## Forty-fifth Claude audit — KERNEL-PATH resource replacement, at e3ab84de — 14 September 2026

Recorded by Claude directly. Scope: NATIVE-KERNEL-RESOURCE-R1 and the kernel-resource fixture (contract, replacement, service, driver, analysis and runner). Verdict: acceptable, no defect. Also noted: a8809b40 is the user's acceptance of S0-LL07-a. The decision record quotes the instruction "accept S0-LL07-a, then proceed", binds it to review commit b7f9ce52 and the original result hash, and the 39-record envelope keeps all 38 earlier result objects identical while adding one ACCEPTED record with the original test revision and contract binding. The production gate on that envelope against the HEAD inventory returns only the nine missing reasons, and the live ledger checker returns PASS at 39 accepted, 9 missing, 0 unreviewed. No shared compiler, kernel, tool, contract or earlier fixture changed in either commit; the diff of lib, compiler, level-0, level-1 and lisp-kernel against c994217a is empty.

Verification: the retained verifier passed on the packet. The producer was re-executed from scratch through ten native sessions into the reviewer's scratchpad; all ten capture hashes, the joins, controls, summary, eight mutant sources, retained fixture sources and logs are byte-identical to the packet, the only differences being the run record and command list, which carry the work directory paths and timestamp. The packet manifest with 42 files, the index locator hash, the 25 source pins including lib/macros.lisp, and 43 catalog rows at ccl-evidence 3fc2ca30 were verified. The development archive retains the first attempt's capture, log, run record and sources with the checker failure REPLACEMENT_MACRO_SCOPE, and the service, replacement and driver bytes in that archive are unchanged from the retained execution.

Independent checks: the U1 span 12716–13142 of lib/dumplisp.lisp is the KERNEL-PATH definition that reads the kernel global, decodes a UTF-8 C string, falls back to the realpath of the first command-line argument and applies platform-specific string processing; the retained original capture stops in the front end with no arch-specific macro for %GET-KERNEL-GLOBAL-PTR in the census arch. Position 29147 of lib/macros.lisp is the DEFUN macro, so the one recorded expansion is the enclosing wrapper and the replacement body itself is a single global call. I loaded the service definitions globally into a fresh native session of the pinned image and ran the fourteen probes myself; every value, error message, readiness flag and stored name matches the literal expectations in the analysis module, including the caller-mutation and returned-string-mutation cases and the opaque Unicode name with a parent segment preserved verbatim.

Assessment: the replacement is captured through the registered Wasm front end as a separate source slice with the original failure retained beside it, and it is not presented as an eighth traversed original definition. The reference runs as lexical functions bound in LABELS with only fixture-owned specials dynamically bound; the driver checks that global function cells, the macroexpand hook, the pass-2 backend hook and the service variables are unchanged before it claims restoration. The eight single-site service mutants each fail at their named first case and the nineteen checker controls reject scope promotion, hidden reference status, source offset and body substitutions, an erased gap, relabelled or substituted replacement text, a missing or duplicated service call, substitution of the native call, a substituted macro backend, unbound service text, case omission or duplication, a promoted failure state and a substituted native name. The packet claims no target runtime implementation, no census gate credit and no graph edge removal.

One nonblocking observation. The install guard order, readiness checked before validation, is not exercised by a case: an invalid input after a successful install would report ALREADY-INSTALLED rather than INVALID, and a mutant that swaps the two guards would escape the current fourteen cases. This does not affect the contract as written, which supplies the input once before readiness.

Disposition: NATIVE-KERNEL-RESOURCE-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. No gate credit is claimed or given. Gate 39 accepted, 9 missing, 0 unreviewed of 48.

## Forty-sixth Claude audit — late-Worker shared-state preservation S0-LL13-b, at 8cb81f6b — 14 September 2026

Recorded by Claude directly. Scope: LATE-WORKER-R1 and the late-worker fixture (kernel and lazy modules, ABI schema, loader, worker, supervisor harness, oracle, runner). Verdict: acceptable, no defect. The inventory edit is confined to the S0-LL13-b status and runner fields, and only that slot's contract hash changed between a3e639c6 and HEAD; the 39 accepted bindings are unaffected. The diff of lib, compiler, level-0, level-1, lisp-kernel and library against c994217a is empty.

Verification: the retained verifier passed, rebuilding all 21 bundles and re-executing every scenario with 289 deterministic files identical. The producer was re-executed from scratch into the reviewer's scratchpad: all bundles, binaries, case observations and memory snapshots, the observations, summary, slot gate and role-omission outcomes are byte-identical to the packet; the only differences are the command log, which carries scratchpad paths, and the records that embed it or the timestamp. The production gate on the unchanged 39-record accepted envelope against the HEAD inventory returns nine missing reasons including S0-LL13-b, a slot inventory built from HEAD returns only the unreviewed reason, and the live ledger checker returns PASS with the gate BLOCKED at 39 accepted, 8 missing, 1 unreviewed. The packet manifest with 321 files, the index hashes for results and packet, all eleven source pins at HEAD, and 322 catalog rows at ccl-evidence 3401d97 were verified. The development archive retains the first attempt, whose only source difference from the retained runner is the reserved-slot scenario selecting occupied slot one rather than empty slot zero; the loader, modules, worker, harness and oracle bytes are identical across attempts.

Independent checks from the retained snapshots of the complete scenario, without the oracle: instantiating the third and fourth Workers, the repeated process initializer from the late Worker, and the four observation steps each leave all 65,536 bytes unchanged; at the end every byte outside the declared static, code, heap, staging and sixteen owned private ranges still carries the initial pattern, and the first two Workers' private regions are unchanged from their last mutation through the late instantiations and setups. The final image carries owner words 1000 through 1003, TCR identities 1 through 4, the per-Worker tokens, process word 2, generation 1, and the code record equal to the lazy module's digest. Every table reports slot 1 returning 24 and slot 2 returning the value derived from the live heap byte, which is what the installed function computes. Independent disassembly shows the kernel with no start section, one passive data segment, a shared 1-page memory import and one element segment at slot 1, and the lazy module with no data, element or start section.

Assessment against the slot assertion, which requires mutating shared state, then instantiating a late Worker and preserving that state while initializing only owned private regions: shared data, BSS, heap and staging are mutated three times before and between the late instantiations and survive them; instance creation has no memory-write authority because the image is passive and there is no start function, and the loader refuses modules that have either; the process initializer is an atomic claim whose retry from the late Worker writes nothing; private setup validates the owner and the claimed base and writes only the four owned ranges; lazy code published before the late Workers exist is acquired by them from the shared record. The eleven refusals each stop before the corresponding ready or code publication with memory unchanged, and the nine mutants each fail at their named first transition, including the two whose bypassed preflight lets the engine write the active segment or run the start function on instantiation.

One nonblocking observation. The kernel's second setup refusal, an instance that already has an owner or a process word other than ready, is not exercised by a case; only the wrong-base refusal is. A mutant removing that check would escape the current schedule. Duplicate ownership is instead refused by the supervisor before the Worker exists, which the packet states.

Disposition: LATE-WORKER-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. S0-LL13-b [full] is complete at its stated scope and ready for the user's acceptance decision, which would make 40 accepted. Gate 39 accepted, 8 missing, one unreviewed of 48.

## Forty-seventh Claude audit — source-wide census collection, at 363a964f — 14 September 2026

Recorded by Claude directly. Scope: NATIVE-SOURCE-CLOSURE-R1 and the source-closure fixture (file-compiler driver, D1 layout derivation, flat-graph analysis, lexical bound analyzer, survey, probe, reference, layout and verification scripts, corpora). Verdict: acceptable as a diagnostic collection, no defect. It claims and receives no gate credit; the inventory, criteria, accepted records and historical graph are unchanged, and the diff of lib, compiler, level-0, level-1, lisp-kernel, library and xdump against c994217a is empty. The ledger checker passes at 39 accepted, 8 missing, 1 unreviewed.

Verification: the retained verifier passed in about two and a half minutes, checking the packet's ten files and 46 pins, extracting both archives with their member identities, recomputing the probe results and 34 controls, and recomputing both 164-unit worklists and their totals from the retained flat graphs. I prepared a fresh disposable U1 copy and re-executed the probe suite: the callback, guard, dump and failed-effect captures and the reference and layout outputs are byte-identical, the reference comparison reproduces 3,168 code bytes across 26 functions, and the 34 controls agree. I then re-executed ten survey sessions, five units in native and continue modes including lib/dumplisp.lisp, level-0/l0-float.lisp with its ten target gaps, level-1/l1-boot-1.lisp, lib/ccl-export-syms.lisp and compiler/X86/x862.lisp with 4,709 native compilations and 889 target captures. After replacing the disposable work-directory prefix that the captures embed, every graph, observation, native code prefix, gap, effect and read record is identical to the retained session; the only differences are printed heap addresses inside the bounded form-name previews. Packet hashes, the index locator hash, eleven catalog rows at ccl-evidence e8b0c84 and the development archive's 3,517 members were verified; the archive retains seven failed session records, the four development-noted mutants, and the omitted-success list names 413 superseded captures by hash.

Independent checks: the D1 subtag constants agree with my own reading of x8632-arch.lisp, symbol 58 and function 42 as node subtags 7 and 5 under fulltag 2, single and double float 15 and 23 as immediate-header subtags, character 75, unbound 51 and slot-unbound 83 under fulltag 3. The unresolved-call arithmetic is consistent: 1,084 function-variable calls split into 206 bounded and 878 unresolved, and with 639 computed callees give the stated 1,517. The corpus controls exercise the failure-taint, owner, include, effect-completion, native-result and qualification-promotion paths, and the write-scan control shows the bound analyzer rejects assigned variables without relying on the compiler's assignment bit.

Assessment: the driver calls U1's real file compiler with the same private expander routing already reviewed, forwards native pass 2 unchanged in native mode, and replaces only the census backend's pass 2 with distinct refusal guards in target mode; FASL publication is refused by an overridden entry, and every override, hook and backend slot is checked restored before the capture is written. Native LAP output functions that bypass Lisp pass 2 are kept explicitly unjoined rather than fabricated. The target survey's 259 form failures and 4,363 affected captures are retained as worklists, not converted into criteria, and the packet states that successful target reads use an unqualified macro environment and emit no Wasm.

Two nonblocking observations. First, the captures embed the absolute path of the disposable work directory in every source location and read record, so cross-run comparison requires prefix normalization; the retained verifier compares only the retained bytes, which it does correctly. Second, the packet's statement that target and callback captures reproduce byte-identically held for its own pairs, but my fresh target probe differed from the retained one in the same eight address-bearing previews as the native pair, so that preview field is nondeterministic in both modes and should not be read as a target-mode reproduction property. The disclosed retention gap for two interactive checker mistakes is a stated limitation of a diagnostic packet and does not affect any accepted record.

Disposition: NATIVE-SOURCE-CLOSURE-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. LL15-b/c remain missing; S0-LL13-b still awaits the user's acceptance decision. Gate 39 accepted, 8 missing, one unreviewed of 48.

## Forty-eighth Claude audit — sequential-build IR and call/code joins, at 5c1ca7b0 — 14 September 2026

Recorded by Claude directly. Scope: NATIVE-BUILD-FLOW-R1 and the build-flow fixture (stream collector, format adapter, graph links, controls, finalizer, verifier). Verdict: acceptable as a diagnostic derivative, no defect. It claims and receives no gate credit; no inventory, criterion, accepted record, native session or shared source changed, and the ledger checker passes at 39 accepted, 8 missing, 1 unreviewed. The same commit corrects the source-closure report's reproduction wording per the forty-seventh audit.

Verification: the retained verifier passed in under five minutes against the pinned base graph, whose hash matches the copy this reviewer reproduced during the nineteenth audit. It re-read all 4,006,405 retained rich-build events, rebuilt the 51,601 function and 103,393 call facts, the graph delta, samples, summary and 22 controls, and every one of the six deterministic outputs is byte-identical to the packet; the eight reviewed callback probes keep their four bounds and four refusals after conversion to the older format without assignment bits. Packet hashes, the index locator hash, sixteen catalog rows at ccl-evidence dc94cdd, nineteen source pins at HEAD, the input stream hash and the development archive with its two retained failures and six superseded-success hashes were verified. The U1 claim is correct: compile-named-function returns the front-end result without pass 2 when pass 1 has already supplied the LFUN.

Independent checks from the retained facts: 1,762 calls have no direct target, 200 are bounded and every bounded target is an inner function of the calling function; 1,079 function-variable calls split into 200 bounded and 879 open, and with 683 computed callees give the stated 1,562. All 98,213 global calls carry a symbol descriptor. Every function has exactly one materialized code identity. The 259 front-end-only functions carry zero call sites, which is consistent with assembled LAP bodies, and they remain in the unattached worklist rather than being attached or declared optional. Assessment of the open computed calls: the 879 variable calls are dominated by keyword and functional parameters (KEY 137, TEST 71, TEST-NOT 38, FUNCTION 56, WRITE-FUNCTION 61, THUNK 28, FUN and F 62), hosted mainly by sequences, lists, asdf, unicode and typesys; the 252 non-lexical initializers are bindings whose value comes from a call or conditional; the 683 computed callees are 369 struct-slot reads, mostly the ioblock function slots in l1-streams, 154 vector-slot reads such as the FASL dispatch table, 59 special-variable hooks and 32 call results. None of these can be bounded by the lexical proof; they need parameter-flow, registry or design-level bounds, which the plan correctly leaves open.

One nonblocking observation. The full-graph diagnostic counts rise sharply, to 118,604 unresolved reachable edges and 34,003 unimplemented reachable nodes, because each global call site now carries an explicit binding-value gap. The report says these are not additional Stage 0 slots, which is right, but the next binding-version join should collapse these per-site gaps into per-binding obligations or the diagnostics will stop being readable.

Disposition: NATIVE-BUILD-FLOW-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. LL15-b/c remain missing; S0-LL13-b still awaits the user's acceptance decision. Gate 39 accepted, 8 missing, one unreviewed of 48.

## Forty-ninth Claude audit — sequential-build binding versions, at 91c27067 — 14 September 2026

Recorded by Claude directly. Scope: NATIVE-BINDING-VERSIONS-R1 and the binding-versions fixture (stream collector, history assembler, graph links, controls, runner, finalizer, verifier). Verdict: acceptable as a diagnostic derivative, no defect. It claims and receives no gate credit; no native session ran, and no inventory, criterion, accepted record or shared source changed. The diff of lib, compiler, level-0, level-1, lisp-kernel, library and xdump against c994217a is empty, and the ledger checker passes at 39 accepted, 8 missing, 1 unreviewed. The consolidation of per-site global gaps into one obligation per symbol answers the nonblocking observation of the forty-eighth audit.

Verification: the retained verifier passed in about ninety seconds against the pinned base graph, whose hash matches this reviewer's copy from the nineteenth audit, composed with the reviewed build-flow delta. It re-read all 4,006,405 retained events, rebuilt the facts, histories, graph patch, summary and 22 controls, and all five outputs are byte-identical to the packet. The twelve packet files, the index locator hash, 18 source pins at HEAD, the four pinned inputs (rich stream, build-flow delta, functions and calls), 13 catalog rows at ccl-evidence a77fa44 and the development archive with its three retained failures (READER_LOCATION, BINDING_SYMBOL, SAME_NAME_IDENTITY) were verified. The macro-wrapper marker is the x8664 xloader's macro-apply word from xdump/xx8664-fasload.lisp read as a fixnum, as stated.

Independent checks from the retained histories and patch: 11,483 binding identities, 5,693 called with 98,213 sites, 5,598 with observations and 95 without, 2,060 with more than one ordinary code, zero continuity gaps, and 27 literal-NIL observations; no macro expander code appears in any ordinary-code list, every ordinary-code list equals the function-role observations of its history, and no called symbol has descriptors disagreeing on name. The 95 unwitnessed symbols account for 345 sites, 81 in MAKE, ten in CCL, one in UIOP/STREAM and three uninterned SETF names, and the CCL examples are other-architecture LAP definers, an ARM xload helper and xref accessors that this x8664 build never defines. The patch retargets exactly 98,213 call edges, adds 5,693 unresolved required binding-cell nodes each with one unresolved value edge, adds 7,074 code nodes with 7,683 body edges of which 2,676 are complete and 5,007 unresolved, removes only 5,692 name placeholders, and has no target outside the build namespace. The origin tally of function observations (9,744 resident before, 4,134 exact FASL version, 1,218 materialized, 306 unjoined) sums to the 15,402 function-role observations.

Assessment: the join is by symbol identity, never by name; removal hooks record intent before the store and are not replayed as completion; a missing inventory entry is not read as unbound; macro and special-operator wrappers are classified by the pinned marker and excluded from callable candidates, with unknown wrapper shapes left unclassified. The one unresolved value edge per cell keeps the known designator from being read as a callee bound, and the report says so. The checker in links.py derives the expected patch from the same construction helpers as the producer, which the README discloses; the independent checks are the raw-stream replay and this audit's tallies.

One nonblocking observation. Of the 15,402 function-value observations for called symbols, 9,744 are resident-before values with no source body in this build, which is why 5,007 prototypes still lack body witnesses. Those bodies come from the image the build started in, so the next join must reach the r5 traced heap or the image's own build, not this stream.

Disposition: NATIVE-BINDING-VERSIONS-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. LL15-b/c remain missing; S0-LL13-b still awaits the user's acceptance decision. Gate 39 accepted, 8 missing, one unreviewed of 48.

## Fiftieth Claude audit — starting-image body witnesses, at 579230d5 — 14 September 2026

Recorded by Claude directly. Scope: NATIVE-RESIDENT-BODIES-R1 and the resident-bodies fixture (native exporter, bounded image reader, analysis, controls, runner, finalizer, verifier). Verdict: acceptable as a diagnostic, no defect. It claims and receives no gate credit; no census graph edge, computed-call bound, inventory, criterion, accepted record or shared source changed. The diff of lib, compiler, level-0, level-1, lisp-kernel, library and xdump against c994217a is empty, and the ledger checker passes at 39 accepted, 8 missing, 1 unreviewed. This answers the nonblocking observation of the forty-ninth audit by going to the starting image rather than the build stream.

Verification: the retained verifier passed in under twenty seconds, checking the 21 packet files and 25 pins, extracting the pinned source archive and bootstrap image, reproducing the three analysis outputs byte-identically, rerunning the 24 controls, and running one fresh native export that equals the retained export. The index locator hash, 22 catalog rows at ccl-evidence c194f03, all seven input hashes including the bootstrap image, the two duplicate repeat exports recorded by hash, and the development archive with the prefix-r1 reader failure, the prefix-r2 whole-inventory mismatch at row 14,734, the prefix-r3 region export and the run-r4 INVENTORY_COMPLETION failure were verified. The rich-observation runner does extract the bootstrap image from the pinned archive before observing, so the starting image is the right provenance.

Independent checks: the request list equals the 5,007 unresolved body edges in the binding-versions delta, the 4,373 witnessed and 634 remaining codes partition it, and the witnessed codes are disjoint from the remaining ones. I extracted the image from the bootstrap archive myself, confirmed its hash, walked the read-only section with my own loop over object headers and found the same 14,718 function objects ending exactly at the section end, and compared 300 randomly chosen retained payloads with the image-file spans, all equal. Every literal-slot join lies within the constants area of its function. The image reader's constants agree with the kernel headers: the five section codes 64, 32, 72, 56 and 48 are the static, read-only, dynamic, managed-static and static-cons area codes shifted by the x8664 fixnum shift, platform 83 is Darwin plus x86 plus 64-bit word size, and function subtag 149 is node-header index 9 under fulltag 5.

Assessment: the join is anchored by the complete read-only region rather than a common-prefix cutoff, with three independent agreements required per function: the original inventory record, the fresh native walk and the disk decoder, on ordinal, identity, address, word count and code boundary, plus byte equality of the whole payload including immediate words. The dynamic suffix is reported as different and never joined, and the positive probe shows a changed dynamic descriptor cannot alter any join. Source ranges and the 501 literal references are presented as inputs to the next traversal, not as dependency closure, and the summary carries source_ir_bodies_closed zero with a control against promotion.

One nonblocking observation. The 634 prototypes outside this witness are not characterized beyond needing dynamic-heap, installation or generated-function provenance; a breakdown by where their values first appeared in the stream, dynamic at start versus installed during the build, would tell the next step which witness to build.

Disposition: NATIVE-RESIDENT-BODIES-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. LL15-b/c remain missing; S0-LL13-b still awaits the user's acceptance decision. Gate 39 accepted, 8 missing, one unreviewed of 48.

## Fifty-first Claude audit — remaining body origins and the Swink boundary, at 6aaedc7e — 14 September 2026

Recorded by Claude directly. Scope: NATIVE-BODY-ORIGINS-R1, the body-origins fixture (classifier, runner, inputs) and the Swink disposition worklist. Verdict: acceptable as a diagnostic, no defect. It claims and receives no gate credit; no native process ran, and no graph edge, inventory, criterion, accepted record or shared source changed. The diff of lib, compiler, level-0, level-1, lisp-kernel, library and xdump against c994217a is empty. This answers the nonblocking observation of the fiftieth audit. Also confirmed: 45f21612 is the user's acceptance of S0-LL13-b, quoting "Accept LL13-b" against review commit 71cfeb0f; the 40-record envelope keeps all 39 earlier result objects identical, the gate on it returns only the eight missing reasons, and the ledger checker passes at 40 accepted, 8 missing, 0 unreviewed.

Verification: the retained replay passed in about thirty seconds, checking the fifteen packet files, six pins and five input hashes, re-scanning all 4,006,405 events, and reproducing the capture, witnesses, origins, summary and controls byte-identically with 18 controls and the parser probe. The index locator hash, sixteen catalog rows at ccl-evidence 87a1b3b, the packet copy of the Swink worklist equal to the repository copy, and the development archive with the exploratory scripts and the disclosed headline-count mistake were verified. All eleven Swink source anchors sit at the stated byte offsets and lines in the six pinned U1 files, whose hashes match.

Independent checks from the retained worklist: the 634 codes equal the remaining set of the starting-image witness; 481 are initial-inventory rows and 153 later installations; sixteen initial rows carry the observation installer's source note and 465 none; the later rows are 152 under ASDF compile initializers and one under a Swink load effect; every first descriptor precedes or coincides with its first called binding; every row keeps an unclassified heap area and no complete-body claim. The Swink row is SWINK::READ-SEXP, code 15717373, installed at event 2889318 under load-effect event 2889316. The sixteen annotated rows are observer wrappers of %FHAVE, %UNFHAVE, %FASL-DISPATCH, RESTORE-LISP-POINTERS and similar, as the report says. The 465 unannotated rows are 403 named functions, 61 method-shaped names and one SETF function, with generic functions such as PRINT-OBJECT, CLOSE and INITIALIZE-BASIC-STREAM prominent, which is consistent with dispatch code living outside the read-only region and with the report's refusal to infer a subtype from a name.

Assessment: the classifier proves identity by parsed function descriptors, not by integer literals or names, and the controls cover equal-name substitution, removal intent offered as installation, wrong installed object, missing or cross-process parents, invented heap areas and false credit. The Swink worklist is correctly a disposition request, not an applied disposition: it names the listener and client sockets, the lazy provider and both system registrations, requires explicit service edges and a tested unsupported condition, and does not drop the module's CLOS, stream and thread dependencies.

One nonblocking observation. Swink's footprint in the worklist is larger than the single late installation: 25 of the initially resident prototypes are called through SWINK symbols, so the module's generic functions were already in the starting image. The integration edges should be drawn at module level for all of them, not only for READ-SEXP.

Disposition: NATIVE-BODY-ORIGINS-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. LL15-b/c remain missing. Gate 40 accepted, 8 missing, zero unreviewed of 48.

### Addendum to the fifty-first audit — Swink browser exclusion, at 430cd29f — 14 September 2026

Recorded by Claude directly. 430cd29f is documentation only. It records the user's clarification, quoted in the disposition file and the change log, that Swink will not be used in the browser, and revises the Swink worklist to revision 2: omit the Swink module and its native remote-lisp clients from the browser port, with no implementation, replacement, socket stub or host-service task, and only the shared module loader's tested refusal if a supported entry can request an excluded module. The revision covers all module bindings, including the 25 initially resident prototypes noted in the fifty-first audit, so that observation is closed. Verified: the six pinned U1 source hashes and all eleven anchors still hold at HEAD; the retained NATIVE-BODY-ORIGINS-R1 packet is unchanged and its index hash still matches, with revision 1 preserved inside it; no inventory, criterion, evidence, graph or shared source changed; the ledger checker passes at 40 accepted, 8 missing, 0 unreviewed. The record correctly states that the exclusion is not applied to the census graph and earns no credit; the browser startup dependency check it names remains open work. No defect.

## Fifty-second Claude audit — resident function-literal joins, at 0d4a9b04 — 14 September 2026

Recorded by Claude directly. Scope: NATIVE-RESIDENT-LITERALS-R1 and the resident-literals fixture (join, controls, runner, inputs). Verdict: acceptable as a diagnostic, no defect. It claims and receives no gate credit; no native process ran, and no inventory, criterion, accepted record, earlier graph record or shared source changed. The diff of lib, compiler, level-0, level-1, lisp-kernel, library and xdump against c994217a is empty, and the ledger checker passes at 40 accepted, 8 missing, 0 unreviewed.

Verification: the retained replay passed in under a minute against the pinned base graph composed with the build-flow and binding-version deltas, checking the twelve packet files, ten pins and seven input hashes and reproducing the facts, delta, summary and controls byte-identically with 24 controls and the two positive cases. The index locator hash, thirteen catalog rows at ccl-evidence 8a6ba33, the two U1 source hashes, and the development archive with the first attempt's ORIGIN_EVENT_JOIN refusal were verified.

Independent checks from the retained inputs: the 465 unannotated resident prototypes each carry exactly one function literal in their original first-descriptor events; the targets are the seven read-only functions with counts 275, 85, 57, 45, 1, 1 and 1, all present in the read-only export; the DESCRIBE-OBJECT method's own literal is its internal lambda, code 1761, giving the eighth target; all eight source extents match the pinned U1 files by hash and begin at the expected definitions in level-1/l1-dcode.lisp and lib/describe.lisp; the delta has 466 complete reference edges and six unresolved body edges for the six codes not previously in the graph, and no cross-process target.

Assessment: this confirms what the fifty-first audit suspected from names, now by identity. The 465 resident prototypes are generic-function objects whose literal is one of CCL's dispatch-code routines, so the worklist collapses to five dcode routines and two methods. The packet is right to call this a reference join and not a callee bound: a dispatch routine's targets are the methods installed in the generic function's dispatch table at runtime, which no static literal reveals. The controls cover omitted and inserted facts, unanchored targets, name-only substitution, subtype and IR promotion, altered source extents, earlier-gap completion and seed changes, and the dynamic replay suffix is shown to be inert.

One nonblocking observation. The next witness this points to is the generic-function dispatch state itself: for each of the 465 objects, the method set and the dcode selection are runtime registry contents, so bounding them belongs with the registry bounds already scheduled for the 683 struct-slot and vector callees rather than with source traversal.

Disposition: NATIVE-RESIDENT-LITERALS-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. LL15-b/c remain missing. Gate 40 accepted, 8 missing, zero unreviewed of 48.

## Fifty-third Claude audit — native method registry checkpoint and the stale-dcode defect, at 22d1a9f1 — 14 September 2026

Recorded by Claude directly. Scope: NATIVE-DISPATCH-REGISTRY-R1 and the dispatch-registry fixture (native inspector, removal probe, native runner, analyzer, entry point). Verdict: acceptable as a diagnostic, no defect in the packet. It claims and receives no gate credit; no inventory, criterion, accepted record, census graph or shared source changed. The diff of lib, compiler, level-0, level-1, lisp-kernel, library and xdump against c994217a is empty, and the ledger checker passes at 40 accepted, 8 missing, 0 unreviewed. This takes up the fifty-second audit's observation that generic-function dispatch state is registry work.

Verification: the retained replay passed, preparing a fresh disposable U1 copy with the pinned bootstrap image, running two inspector sessions that agree byte for byte and one independent reproduction, and reproducing all six outputs byte-identically with 23 controls. The twenty packet files, twelve pins at HEAD, the three input hashes, the index locator hash, 21 catalog rows at ccl-evidence 0798774, the byte-identical first and repeat captures, and the development archive with the r1 unbound-slot refusal, the r2 counterexample failure and the r3 first exposure of the defect were verified.

Independent checks from the retained capture: 582 population entries, 581 initialized with 1,514 methods and 1,514 distinct method functions, one uninitialized object with unbound name, methods and combination slots, eight distinct dcode functions of which three are installed method functions used directly, all 581 with the standard combination; every registry entry and only 583 function records carry the generic-function bit, the extra one being the private probe; the twelve probe calls carry the expected values and method counts, with the first call signalling no applicable method and the last returning 10 and 11 with zero methods. The bit test matches library/lispequ.lisp, where the generic-function bit is 27 and the method bit 28. The source explanation is correct: %remove-standard-method-from-containing-gf updates both method lists and calls compute-dcode, and compute-dcode changes dispatch only inside a when-methods form, so an emptied list leaves the previous dcode installed; the direct path comes from dcode-for-universally-applicable-singleton.

I reproduced the defect with my own script in a fresh session on the pinned image, with neither the observer nor the inspector loaded: a generic function with one universal method returns 10 and 11 before and after that method is removed, while a generic function that never had a method signals NO-APPLICABLE-METHOD-EXISTS. I also removed both methods of a two-method generic function; the call still returned the universal method's value. The stale dispatch therefore follows any removal that empties the method list, not only the removal of a lone universally applicable method.

Assessment: the packet treats the installed method list as a snapshot, never as a callee bound, keeps the uninitialized object explicit, and records the defect as REPRODUCED_UNFIXED with a control that refuses concealing it. The literal counterexample properly qualifies the previous audit's inference: an ordinary function holding a dcode literal is refused by the generic-function reader, so the subtype rests on the function bits, not on the literal. The identities are session-local and are not joined to the 465 original prototypes, which the report states. The plan's requirement that the Wasm path signal the empty-registry condition rather than inherit the stale call is the right disposition, and it needs a reviewed decision before native-versus-Wasm comparisons on this case.

One nonblocking observation. The report describes the defect as removal of the last universally applicable method. My two-method case shows the mechanism is general: compute-dcode leaves whatever dcode was last installed whenever the method list becomes empty. The Wasm disposition should be written for the empty registry regardless of the prior dcode kind.

Disposition: NATIVE-DISPATCH-REGISTRY-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. LL15-b/c remain missing. Gate 40 accepted, 8 missing, zero unreviewed of 48.

## Fifty-fourth Claude audit — same-execution registry and compiler-body witness, at f7d2f73a — 15 September 2026

Recorded by Claude directly. Scope: NATIVE-REGISTRY-FLOW-R1 and the registry-flow fixture (observer, flat IR reader, native runner, analyzer, README). Verdict: acceptable as a diagnostic, no defect. It claims and receives no gate credit; no inventory, criterion, accepted record, census graph or shared source changed. The diff of lib, compiler, level-0, level-1, lisp-kernel, library and xdump against c994217a is empty, and the ledger checker passes at 40 accepted, 8 missing, 0 unreviewed. This takes up the fifty-third audit's observation and the registry checkpoint's inability to connect fresh objects to compiler bodies.

Verification: the retained replay passed in about six minutes in a fresh disposable U1 copy, running four native sessions (two observations, the hook-free reference, the real omitted-add-hook run) and reproducing all seven outputs byte-identically with 24 controls. The 23 packet files, sixteen pins at HEAD including eight U1 sources, the three input hashes, the index locator hash, 24 catalog rows at ccl-evidence 07a8396, and the development archive with the r1 loader, r2 unbound-slot, r3 JSON and r4 reference-redefinition failures and the r5 capture pass were verified. The runner also checks that the extracted source archive's eight U1 files equal the repository copies.

Independent checks from the retained capture: 1,435 events with the stated kinds, 118 touched generic functions, 401 emitted functions from 225 pass-2 entries, 936 function records; my own join over prototype identity, completion order and code bytes gives 163 joins and 77 gaps; the reference's 581 rows, 1,514 method codes and 411,368 bytes, and the 14 changed rows with 152 methods and 42,352 bytes, equal the observed run; the omitted-hook capture has 955 events, exactly the observed run minus the 480 add events. The hook points are the single implementations: add-method and %add-method call %add-standard-method-to-standard-gf, remove-method calls %remove-standard-method-from-containing-gf, and compute-dcode stores only through the defsetf to %set-gf-dcode, and only when the dcode or argument index changes, which is why 12 setter calls suffice. Every one of the eight add or remove operations whose before and after dcode differ carries a nested setter event. The one top-level setter event is the allocation of the private probe's generic function with name, methods and combination slots unbound, correctly reported uninitialized.

The 77 gaps are exactly the slot accessor methods: describe.lisp declares 32 accessors and 13 readers, giving 45 reader and 32 writer methods, whose functions are copies of the reader and writer templates in l1-clos-boot.lisp with only two distinct code-byte strings of 72 and 80 bytes, no source note and an opaque name. The 160 describe-phase joins account for every live defmethod form in the file: of its 163 forms, two sit inside a block comment and one under a PPC reader conditional. The three probe joins are the probe's own defmethods, and the probe's three calls return 30 and 31, 30 and 31, then 10 and 11.

Assessment: the join requires prototype identity, compiler completion before installation and equal code bytes; since the installed prototype is the emitted object, the byte equality is a consistency check against mutation rather than an independent identity witness, which is adequate for what the packet claims. The gap disposition is right: accessor functions are template copies and no compiler IR exists for them in any execution, so the next witness is the reader and writer construction path, not a name join. The reference comparison shows observation does not alter emitted method code, restoration is checked after both normal and nonlocal exit, and the report keeps the new namespace separate from the original build's 465 objects.

One nonblocking observation. The setter hook misses the lazy reader optimization: l1-clos.lisp installs reader-constant-location dcode by storing the dcode slot directly at line 2198, and %set-encapsulated-gf-dcode bypasses it for encapsulated functions. Neither occurred in this corpus, but the dispatch-state witness for accessor generic functions must cover that first-call path.

Disposition: NATIVE-REGISTRY-FLOW-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. LL15-b/c remain missing. Gate 40 accepted, 8 missing, zero unreviewed of 48.

## Fifty-fifth Claude audit — plan refocus, integrated development closure and the 95-cell definition batch, at a1013068, 0cb519e4 and a6da3fc3 — 15 September 2026

Recorded by Claude directly. Scope: the plan refocus commit, CENSUS-DEVELOPMENT-CLOSURE-R1 with the closure fixture (runner, composer, seeds, assessor, integration controls, support, inputs), and CENSUS-BINDING-DEFINITIONS-R1 with the binding-definitions fixture (runner, definition join, classification, batch controls, inputs, source worklist). Verdict: acceptable as diagnostics, no defect. Both packets report BLOCKED with exit 2 by design; neither claims or receives gate credit, and no inventory, criterion, accepted record, earlier graph record or shared source changed. The diff of lib, compiler, level-0, level-1, lisp-kernel, library and xdump against c994217a is empty; the census checker and schema are unchanged since 85adc038; the ledger checker passes at 40 accepted, 8 missing, 0 unreviewed. The plan commit records two user rulings quoted by Codex, that Claude's progress assessment was supplied as direction and that another native build is permitted when it speeds completion; these are recorded decisions, not evidence, and nothing in either packet depends on them.

Verification of the closure packet: the retained replay passed in about three minutes, checking the fifteen packet files, 21 source pins and twenty input hashes, reconstructing the reviewed graph through the original materializers, and reproducing all six outputs byte-identically with 20 controls and the regenerated 16,025,655-byte working graph at the recorded hash. The reconstruction's wrapper-stage hash equals the base graph I replayed in the forty-eighth, forty-ninth and fifty-second audits. The index locator hash, sixteen catalog rows at ccl-evidence 10e2551, and the development archive with the r1 boot-materialization failure and the r2 wrong-controls-module failure were verified, with the r2 analysis outputs correctly recorded as shared with the final run.

Verification of the definition batch: the retained replay passed in about nine minutes without the cached base, re-reading all 4,006,405 original events, reconstructing the previous graph at its recorded hash, and reproducing all six outputs byte-identically with 22 controls and the new 16,027,559-byte working graph at the recorded hash. The nineteen packet files, 42 source pins including fourteen unchanged U1 files, six input hashes, index locator hash, nineteen catalog rows at ccl-evidence 404c041, the interpreter identity and the exploratory archive were verified. All ten source anchors in the worklist hold at the stated byte offsets and lines.

Independent checks on the closure: the worklists carry 95 unwitnessed cells over 345 sites, 5,693 cells over 98,213 sites all marked unresolved, 5,007 body obligations partitioned exactly into 4,373 payload witnesses (4,130 with source ranges) and 634 origin rows, 1,562 open computed calls split 879 and 683, and 259 unattached assembly functions. The seed delta has 22 entry bindings, 49 root prototypes, 109 snapshot functions each with one unresolved body edge, 36 roots made of 25 function roots, the two kernel vectors, four startup groups and five gap nodes, and 120 nodes and 128 edges, all in the seed-v2 namespace. The blocker counts of 31,206 and 34,009 equal the prior resident-literals counts plus the 109 and five added, and the 10,700 binding-versions blockers are exactly the 5,693 open cells and 5,007 bodies.

Independent checks on the batch: the 88 witnesses are 80 before-pass-2 events and four expander pairs; every compiled join is the top-level named function at the IR root of its event, 79 from tools/defsystem.lisp in MAKE and UIOP/STREAM::OUTPUT-STRING from tools/asdf.lisp; the four declaration events yield seven entries over five distinct symbols, four readers and three accessors; the delta is 80 compiled edges, seven accessor edges and four construction nodes; the partition 80, 5, 3 and 7 covers all 95 cells. The nfcomp explanation is correct: fcomp-load-%defun emits a fasl-defun operation rather than installing the definition, so a compiled body does not witness a cell value. The setter annotation comes from the observer's lookup in the SETF inverse table and is rightly treated as printed text.

Assessment: the closure command is the right instrument at the right time. It composes only pinned fragments, keeps the historical roots and widening edges intact so nothing can disappear, keeps revision 2 in its own namespace with explicit bridge gaps, and refuses to promote any structural pass into an LL15 result. The batch is honest about what a compilation witness is and is not, and its controls include the promotions that would matter most: a compile edge standing in for a runtime bound, a fresh identity substituted for an original object, and a source name used as an object identity. Neither packet moves the census closer to PASS, and both say so.

One nonblocking observation. The 3,062 unresolved build-flow call edges in the blockers are the 1,562 computed calls plus 1,500 builtin calls, each with one candidate and unqualified lowering. The report's population table names only the 1,562; the builtin edges are counted inside the 31,206 total but appear in no named worklist. They belong with the lowering and store dispositions of the plan's fourth step, and the report should carry them as a named population so they are not lost when the computed-call count reaches zero.

Disposition: CENSUS-DEVELOPMENT-CLOSURE-R1 and CENSUS-BINDING-DEFINITIONS-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. LL15-b/c remain missing. Gate 40 accepted, 8 missing, zero unreviewed of 48.

## Fifty-sixth Claude audit — the fixture commit and the correlated query base, at 6be15c29 and ccl-evidence cb66b4a — 15 September 2026

Recorded by Claude directly. Scope: commit 6be15c29 (86 files, 8,184 insertions, all under `tests/wasm/native-census`: builtin-slots, finite-callees and registry-callees) and CORRELATED-QUERY-BASE-R1 at ccl-evidence cb66b4a. The later commit b269c889 with ON-DEMAND-CENSUS-R1 and the LL15 instrument policy change landed during this audit and is excluded from it at the user's instruction; where it bears on a finding below, that is stated. Verdict: two defects in 6be15c29, packaging and reproducibility, with no gate impact; no defect in the query base. Invariants at both revisions: the diff of lib, compiler, level-0, level-1, lisp-kernel, library and xdump against c994217a is empty, the census checker and schema are unchanged since 85adc038, the ledger checker passes at 40 accepted, 8 missing, 0 unreviewed, and the evidence index check passes.

Defect 1, packaging. The commit adds fixture code and nothing else: no stage0 page, no status row, no change-log entry, no README, no evidence index row and no packet. Its message cites `/private/tmp` paths as the location of its results and its "final working projection", and it bundles roughly fourteen distinct deliverables against the one-deliverable-per-commit rule. None of its quantitative claims (124 resolved sites, 4,373 read-only bodies, 109 startup functions, the 3,482-function projection, 351 gaps, 84 identical native units) is bound to a retained input, a source snapshot or a hash. Codex's earlier packets today set the standard this commit abandons.

Defect 2, reproducibility. The first finite-callees pass, replayed from a clean `git archive` extraction of the commit with its pinned U1 kernel, image, source tarball and base graph, ran in 83 seconds with 15 native probes, 15 checks identical to the 09:04 run, restoration true and 23 controls, but produced 98 finite-expression bounds against the run's 95, three lexical-only bounds against zero, 1,464 remaining against 1,467, 29,630 unresolved edges against 29,633, and graph 6b7e1b82 against e64454f5. The executed-source snapshot Codex's run retained shows the cause: the committed `finite.py` adds LABELS to the binding operators and adds LAMBDA-BIND binding and value handling, and `integrate.py` also differs; `run.py`, `test_finite.py` and `probes.lisp` are unchanged. Those additions belong to the later lexical pass. So each later pass consumed outputs from code that was never committed, and the constructor stage that reports 1,438 retained no executed sources at all. The 1,562-to-1,438 chain is not a claim about the committed code until it is re-run end to end from the commit with sources retained. b269c889's on-demand page repeats the 124 and 1,438 figures with a citation to 6be15c29 and inherits this defect.

What verified. The builtin-slots command replayed from the clean extraction in 130 seconds, exit 2 by design, reproducing all six outputs byte-identically against the 08:49 run: 1,500 call sites over eight operand slots (1, 1, 1, 487, 971, 37, 1 and 1), a delta of eight unresolved slot nodes, eight lowering edges and 1,500 site replacements, worklists preserving 95 unwitnessed cells, 5,693 unbounded cells, 5,007 body obligations, 1,562 open computed calls and 259 assembly bypasses, 32 rejected controls, graph 369,718 nodes and 1,090,032 edges, checker 29,714 unresolved edges and 34,021 unimplemented nodes, and graph hash 27cd554e, which is the pinned base of the finite pass. This closes the fifty-fifth audit's nonblocking observation. The three self-contained registry tests (payloads, source map, nested bodies) pass from the clean layout, and the seed-call test's import chain loads because `bodies.py` puts build-flow on the path; the 16:39 import failure is retained only under `/tmp`. All seven evidence packs the committed code names are retained. No hard-coded machine paths beyond synthetic `/tmp/ccl` strings in `test_payloads.py`.

Verification of CORRELATED-QUERY-BASE-R1: 33 listed files plus the packet all match their hashes; the catalog's 34 rows equal the files on disk; both capture streams are byte-identical to the `/tmp` originals; the packet names 6be15c29 and NOT_REVIEWED. The build stream holds 446,083 contiguous events with no unparsed lines across 25 kinds, including 93,343 resident-function, 46,375 function-materialized, 46,116 before-pass2, 38,579 load-operation-emitted, 25,360 fasl-function-write, 21,564 resident-binding and 259 frontend records. The registry stream holds 123,250 records: 73,872 function, 46,375 compiler-materialization, 1,497 mutation enter and leave pairs, three observer wrappers, two registry and two backend checkpoints, one capture policy and one completion. The capture policy declares front-end IR only for pass-1 bodies that already carry an LFUN, with instruction emissions, lowering frames and macro-expansion traffic omitted; that is the stated bound of what this base can answer. The baseline was reused from r1 for the stated reason that only the observation callback changed; baseline and observed builds each pass 21,843 of 21,843 eligible tests with 75 upstream-disabled. R6 shows 156 of 164 FASLs identical, the same eight intentional differences as earlier observed builds (dumplisp, nfcomp, nx2, vinsn, l1-readloop, nx, l0-def, nfasload) and no unexplained ones; restoration returns 164 of 164 FASLs to baseline with source restored. The kernel hash equals the retained U1 kernel in native-census-r7. The observation unit is byte-identical to the committed rich-observation patch and manifest, with the state file inactive at the recorded patch hash. The three executed sources equal the committed `build-observer.lisp`, `build.py` and `scan.py`. The two retained failures are the r1 observed child dying with a TYPE-ERROR in FD-WRITE during print-call-history, and the r2 manual interrupt with its partial streams and failed image. The record was not bound in the source repository at 6be15c29; b269c889 binds it as an auxiliary record.

Assessment. The code that could be exercised is sound, and the query base is exactly what it claims: a retained, reversed, R6-checked native observation with its failures. The commit around that code is not a deliverable under this project's rules, and its central numeric claims cannot be attributed to the committed sources. Required before re-review: split into per-deliverable commits with retained packets, beginning with builtin-slots whose finalized packet already exists under `/tmp/ccl-unpublished-builtin-slots-packet`; re-run the finite-callees chain from committed code with executed sources retained at every stage; write the stage0 pages and index rows for each.

Nonblocking observations. The `seed_compile.py` subprocess limit of 900 seconds is too short for the full unit set: the 167-unit run completed its reference pass and reached 161 of 167 in the observed pass before the limit killed it. The commit's registry-callees chain also depends on a seed-compile capture of 84 units that exists only under `/tmp`.

Disposition: 6be15c29 DEFECT_FOUND, packaging and reproducibility, no gate impact. CORRELATED-QUERY-BASE-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC. ON-DEMAND-CENSUS-R1 and the LL15 policy change at b269c889 not reviewed here. Gate 40 accepted, 8 missing, zero unreviewed of 48.

## Fifty-seventh Claude audit — on-demand census tools and policy, and the three corrective publications, at b269c889, 26ab5842, 62b7b9dd and 46efc9e0 — 15 September 2026

Recorded by Claude directly. Scope: b269c889 with ON-DEMAND-CENSUS-R1 at ccl-evidence 255dc47 and the LL15 instrument policy change; 26ab5842 with CENSUS-BUILTIN-SLOTS-R1 at e57a138; 62b7b9dd with CENSUS-FINITE-CHAIN-R1 at 65e90d6; 46efc9e0 with CENSUS-REGISTRY-HISTORY-R1 at af0117d. Verdict: no defect in the three corrective publications or the policy mechanics; one narrow provenance defect in ON-DEMAND-CENSUS-R1, with every tool and worklist claim otherwise reproduced. Invariants at 46efc9e0: shared source unchanged against c994217a, census checker and schema unchanged since 85adc038, ledger checker PASS at 40 accepted, 8 missing, 0 unreviewed, evidence index check PASS. The three corrective commits answer the fifty-sixth audit; the original 6be15c29 stays DEFECT_FOUND in history as they say.

Policy mechanics. The inventory keeps 81 records; only S0-LL15-b and S0-LL15-c and their contract assertions change. The pack's contract-change check reports those two with accepted bindings unchanged and no acceptance claim; the ledger check passes with two authorized criterion decisions; the decision record's hash is pinned in ledger-policy.json; the gate result's prior-hash chain closes on the previous gate file; documents.json source and output hashes match the three regenerated documents. The decision's inventory snapshots are production views: they differ from the committed inventories only in status and runner for the four implemented-but-unaccepted slots, which is the ledger tool's operational-field exclusion and the same pattern as the 14 September artifact-policy decision at fdb6f0b5. The quoted user authorization is recorded by Codex and cannot be verified here. On substance the change matches the assessment supplied to the user earlier today: the closure checker stays strict, unresolved populations stay visible, witnesses are not bounds, and Stage 1 keeps the bootstrap obligations.

Verification of the query tool from the committed tree: test_capture passes 33 checks in 126 seconds over CORRELATED-QUERY-BASE-R1 with the same case and status set as the retained controls; test_native passes 20 cases in six seconds in the tool's own pristine U1 copy, again identical to the retained controls; the README example finds three GETF-TEST compiler identities with `exhaustive` false, lists one call-site selection, and the witness returns `(:RETURNED (20))` with two events both resolving COMMON-LISP::EQ before dispatch, scenario results equal, restored code identical and no dropped events. The one semantic change in seed_bodies.py accepts a changed final read only for a function that bit 27 marks as a generic function, that appears in a registry checkpoint or mutation event, that the compiler never emitted, and it keeps that function's first state; selected startup entries still require the same execution. Bit 27 is `$lfbits-gfn-bit` and bit 28 `$lfbits-method-bit` in library/lispequ.lisp. Its control test replayed on the retained capture preserves the first state and rejects its four controls exactly as retained.

Verification of ON-DEMAND-CENSUS-R1: all 20 listed files match; 21 catalog rows; inputs pin the query-base packet, the U1 pins and the retained kernel and image; the 83 delivered sources are byte-identical to b269c889. The 167-unit compile passes with reference and observed code identical, source unchanged and no FASLs written, and its unit list contains every source the 84-unit run had still needed. The retained startup graph has 16,169 nodes and 36,848 edges, 32,287 complete and 4,561 unresolved, with 272 unresolved body edges; the bindings stage reaches 4,369 initial functions, 4,053 IR functions, 2,859 cells and 394 registries, with 27 sourced and 245 unsourced missing bodies and 541 runtime bodies. The five development failures are retained, including the 900-second compile limit later raised to 2,700. I replayed the startup chain from committed code and the retained capture in 227 seconds against the builtin base: the bodies, bindings and graph summaries are identical, the missing-body, needed-source and unjoined-runtime files are byte-identical, and the startup graph's nodes, edges, seeds and initializers are identical; only the inherited profile string differs.

The defect. The combined census retained inside startup-analysis.tar.gz has 411,075 nodes and 1,189,405 edges, so its base had 394,906 nodes and 1,152,557 edges. That is the 15:42 field-inputs working graph from the body-analysis chain that 46efc9e0 withdraws as unqualified. It is retained nowhere, neither in the catalog nor in CENSUS-REGISTRY-HISTORY-R1, and the packet does not identify it; the retained startup graph's profile string inherits four finite-pass segments from it. The analysis-stage commands are also not retained, only the compile commands, though the pinned sources and script interfaces made the replay above possible. Required: regenerate the combined census on a retained base, the builtin graph at 27cd554e or the finite-chain graph at 6d30938b, or drop it and retain the base-independent startup graph alone; record the analysis commands as the finite-chain driver does.

Verification of the corrective publications. CENSUS-BUILTIN-SLOTS-R1: 14 packet files plus the publication record and executed-source archive match, 17 catalog rows; the five outputs are byte-identical to the clean replay in the fifty-sixth audit; all 31 executed sources equal their recorded hashes and the 6be15c29 revision; the publication record states the diagnostic is retained unchanged with no repeated execution. CENSUS-FINITE-CHAIN-R1: 13 files match; the 377 analysis sources are identical to e0c73b10; the reproduce driver run from the committed tree in 327 seconds gives four stage summaries and four graph hashes identical to the packet, 6b7e1b82, cd73a1ae, eec3820a and 6d30938b, a byte-identical remaining-calls file and a final graph equal to the recorded fresh hash, with 98, 19, 2 and 5 proofs and 1,438 sites open; the provenance record labels the first two stages' post-hoc command records honestly. This closes the fifty-sixth audit's reproducibility defect. CENSUS-REGISTRY-HISTORY-R1: both files match; the archive holds exactly the twenty recorded prefixes, six with executed-source snapshots; the archive adds an outer log per record that the retention counts omit for thirteen records; the 4,373-body claim is withdrawn and the superseding inputs are named. Nonblocking: the retention omits the 15:04 to 15:52 function-values, field-routes, structure-values, field-inputs and keyword-inputs outputs, one of which is the base above.

Disposition: CENSUS-BUILTIN-SLOTS-R1 and CENSUS-FINITE-CHAIN-R1 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC; CENSUS-REGISTRY-HISTORY-R1 REVIEWED_NO_DEFECT_FOUND_HISTORICAL; the LL15 criterion change at b269c889 verified in mechanics, no defect; ON-DEMAND-CENSUS-R1 DEFECT_FOUND, one provenance defect, tool and worklist claims verified. LL15-b/c remain NOT_RUN under v0.2. Gate 40 accepted, 8 missing, zero unreviewed of 48.

## Fifty-eighth Claude audit — startup analysis replayed on an explicitly retained base, at 9510a8ee — 15 September 2026

Recorded by Claude directly. Scope: 9510a8ee with ON-DEMAND-STARTUP-R2 at ccl-evidence b212ade, Codex's correction of the fifty-seventh audit's provenance defect in ON-DEMAND-CENSUS-R1. Verdict: no defect; the correction is complete and reproduces exactly. Invariants: shared source unchanged against c994217a, ledger checker PASS at 40 accepted, 8 missing, 0 unreviewed, evidence index check PASS. The code change is one new replay driver and sixteen README lines; no analysis script changed.

Verification of the packet: all ten listed files match their hashes and the catalog carries eleven rows. The base is recorded as the CENSUS-FINITE-CHAIN-R1 constructor graph at 6d30938b, which is the graph my own reproduce produced in the fifty-seventh audit. Seven inputs are pinned by hash. The run record snapshots 232 analysis sources before any stage, and every one is byte-identical to 9510a8ee; each stage's command is written to the record before its subprocess starts, the source hashes are re-verified before each stage and after the last, and the driver pins the base hash and the U1 revision. The comparison record binds all 26 body and binding outputs to ON-DEMAND-CENSUS-R1 by hash rather than duplicating them.

Independent checks. Against my own finite-chain graph the combined graph is the exact concatenation of base and startup worklist: 369,734 plus 16,169 nodes gives 385,903, and 1,090,048 plus 36,848 edges gives 1,126,896, with node, edge and seed lists identical in order. A manual replay of the three stages from the retained capture against that base reproduced both retained graphs byte-identically, the startup graph at 98023b4d and the combined census at a5305470, in 207 seconds. The committed driver run from the tree in 217 seconds passes all three stages, writes a comparison record identical to the packet's, produces all three graph files byte-identical to the packet, and records the same source hash set.

One nonblocking observation. The corrected startup graph is byte-identical to the original packet's startup-graph.json.gz, because the profile text inherited from the unretained field-inputs base happened to equal the text inherited from the finite-chain base; the comparison record shows the old and new profiles equal. The packet's supersession of that file and the change-log sentence that it differs only in inherited profile text therefore describe a provenance change, not a content change. The index now records the fifty-seventh audit's dispositions on the four earlier packets, with the original combined graph marked superseded.

Disposition: ON-DEMAND-STARTUP-R2 REVIEWED_NO_DEFECT_FOUND_DIAGNOSTIC; the fifty-seventh audit's defect on ON-DEMAND-CENSUS-R1 is corrected by it, and that packet's recorded disposition stands as history. LL15-b/c remain NOT_RUN under v0.2. Gate 40 accepted, 8 missing, zero unreviewed of 48.

## Fifty-ninth Claude audit — LL15-b/c instrument qualification under census contract v0.2, at 1f4f9829 — 16 September 2026

Recorded by Claude directly. Scope: 1f4f9829 with CENSUS-INSTRUMENT-R1 (packet CENSUS-INSTRUMENT-QUALIFICATION-R1) at ccl-evidence 50636d9, Codex's execution of the two remaining Stage 0 slots under the on-demand policy the user authorized on 15 September. Verdict: no defect; both native records are complete at their stated scope and ready for the user's acceptance decision. Invariants: shared source unchanged against c994217a; the inventory changed only in the two entries' status and runner fields; ledger checker PASS at 46 accepted, 0 missing, 2 unreviewed with all 46 bindings current; document checks PASS; evidence catalog check PASS at 168,281 files with every earlier row intact.

Verification of the packet. The retained verifier re-executed from the wasm2 tree in 3 minutes 28 seconds: source pins and all 275 artifact hashes match, a fresh query index is built, the GETF-TEST site is reselected and privately recompiled in a fresh disposable U1 image, the 33 capture checks and 20 native cases (including the four real observer mutants) pass with the reviewed inventories, the publication is regenerated byte-identical, the unchanged complete-closure checker refuses it, and all 42 publication controls and 16 role omissions reproduce. The query tools, seed recipe, gate, binding and census checker are unchanged since the fifty-eighth audit's base 9510a8ee, so that audit's review of the instrument stands; the new code is the qualification runner, the publication checker, its controls and inputs.

Independent checks from the pinned inputs, without the checker. The call partition recounted from reached-calls in ON-DEMAND-CENSUS-R1 is 13,641 global-binding, 561 builtin, 326 function-variable plus 288 computed-callee (614), and 230 lexical plus 157 self plus 8 lexical-function-value (395), all 395 with local function targets present as nodes; 22 entry bindings among the 32 seeds. The unresolved populations recounted from the published graph are 272 bodies, 614 calls, 2,859 binding values, 394 future registry states, 415 runtime transfers and 7 builtin lowerings, and the 7,403 unresolved nodes sum from the ten reasons the summary lists. The graph's 32,287 complete edges resolve observed calls to binding cells and cells to their initial functions, with each cell's future value a separate unresolved edge, so `complete` there means an exact designator, not an exhaustive callee bound; the claims block and the partition table say the same. The publication manifest hash matches its file; every claim flag is the honest one; the three witnesses report PASS, NOT_REACHED and TRUNCATED with `exhaustive` false and `bound` false, and the absent query stays NOT_OBSERVED. The 70 initializer records are the cold-process boundary chain only, with acyclic prerequisites, and the publication says so.

Contract coverage. Every requirement in the v0.2 instrument section is met by an executed check: one entry point over identity-bound retained evidence with R6 reuse; the seed recipe, compact graph and named unknowns published; executions and namespaces distinguished; queries with exact identities and input hashes; native probes binding source and site, running a stated scenario, preserving evaluation order, values and nonlocal exits and demonstrating restoration; absent, unreached and truncated observations reported as such. For LL15-c, each named rejection has a control that edits the real publication file and regenerates its manifest, so a shortened publication with a matching summary still fails; native omission, dropped-value and swallowed-exit mutants run through the reviewed native path.

Nonblocking observations. The development record retains two failed attempts and a replay failure: the first attempt expected 71 initializer records where the data holds 70, and the checker's constant was corrected to the observed count, so that count is descriptive rather than independently derived; the replay failure was an unsorted diagnostic list, now sorted. Both slots share one result object and artifact set, as the two LL21-c variants do; LL15-c's distinct evidence is the 216 negative-control artifacts. The controls are Codex-authored, as every mutant set in this project is; their independence is from the producer path, which is what the contract asks.

Disposition: CENSUS-INSTRUMENT-R1 REVIEWED_NO_DEFECT_FOUND_NOT_ACCEPTED; S0-LL15-b [native] and S0-LL15-c [native] are complete at their stated scope. Acceptance is the user's decision; with it the ledger would read 48 accepted of 48, and Stage 0 completion then turns on the outline exit criteria, not on any missing slot. Gate 46 accepted, 0 missing, 2 unreviewed of 48.

## Sixtieth Claude audit — LL15-b/c acceptance composition and Stage 0 completion, at 821eac19 — 16 September 2026

Recorded by Claude directly. Scope: 821eac19 and 7a008120 with LL15-PROJECT-ACCEPTANCE-20260916 and the complete aggregate at ccl-evidence 4a436a4. Verdict: no defect; the acceptance is composed exactly as the six-slot precedent was, and the ledger's PASS at 48 accepted of 48 is reproduced. The authorization recorded is the user's instruction to Codex, "If you agree, accept the 2", after the fifty-ninth audit; that exchange happened in Codex's session and is not visible to this reviewer, so the user's own confirmation of it is the provenance, as it was for the six-slot acceptance.

Independent checks. The acceptance envelope keeps all 275 original artifacts of CENSUS-INSTRUMENT-R1 with identical hashes and adds four acceptance artifacts; the only changed result fields are the review disposition and review record. The complete aggregate carries the 46 prior accepted objects byte-equal to the six-slot aggregate, adds the two LL15 objects with paths re-rooted as the precedent does, covers all 48 inventory variants with every disposition ACCEPTED, and its provenance names the previous report by hash. The unchanged accept-evidence producer was used (no tool changed in either commit), its fourteen controls pass, the production gate on the envelope alone reports only the 46 absent-other-record reasons, the live ledger checker reports PASS at 48 with all 48 bindings current, the document checks pass, and the evidence catalog passes at 168,573 files with every earlier row intact; all 289 packet files match their manifest. The review file copied into the packet is the fifty-ninth audit's text at d507f5a7, and Codex's record of that audit on wasm2 is byte-identical to mine.

Nonblocking observations. Codex imported the audit without the branch's five D6 policy commits (fp_control in the TCR schema, the policy layer in the float fixture, the decision record), which remain on wasm2-claude for Codex to merge and review; wasm2's STATUS rows for those two diagnostics are therefore behind the branch. The per-packet composition script is new, as each acceptance's is; the producer it drives is unchanged.

Disposition: LL15-PROJECT-ACCEPTANCE-20260916 REVIEWED_NO_DEFECT_FOUND. Stage 0's slot inventory is complete at 48 accepted; what remains for Stage 0 completion under the outline is the exit-criteria review, and the user's Stage 1 decisions stand where the plan lists them.


## Sixty-first Claude audit — Stage 0 exit-criteria review, at 821eac19 — 16 September 2026

Recorded by Claude directly. This is the subgate 0F document, [exit-criteria-review.md](exit-criteria-review.md): the outline's Stage 0 sentences and the acceptance policy's standing gate requirements mapped to accepted slots and recorded decisions. Verdict: no gap requiring a scope decision; four census enumerations are carried into Stage 1 by name under census contract v0.2. Invariants: ledger PASS at 48 of 48, document checks PASS, no record changed.

## Sixty-second Claude audit — Stage 1 subgate 1A packet STAGE1-1A-R2, at a5b2d66c — 16 September 2026

Recorded by Claude directly. Reviewed: Codex's commit `a5b2d66c` (registration proposal, generated architecture, first leaf pass 2, cross-loader registration, build bindings, generated fatal diagnostics) and the retained packet `ccl-evidence/2026-09-16-stage1-1a-r2` at evidence commit `8b249e0`; the R1 packet is retained and superseded. Executed sources were replayed from a detached worktree at `a5b2d66c`.

Replays. (1) The documented verifier, `tests/wasm/stage1/build/verify.py`, passed against the packet: qualification summary, negative controls and compiled backend FASLs byte-identical, nine generated modules and 24 calls replayed, 37 binding controls rejected, all eight reporter mutants rejected. (2) A fresh three-build registration run, `registration/run.py` from the pinned U1 inputs and native-census kernel into a new work directory, passed: 164 native FASLs, 162 identical while registered, all 164 identical after removal, 21,843 tests passed on both the baseline and registered images. Every baseline, registered and restored FASL hash, all nine WAT files, the generated cases, the Wasm execution record and both FASL comparison reports are byte-identical to the packet's across the two independent runs. (3) An adversarial probe applied the retained proposal unit to a disposable copy with the retained registered image: eleven further leaf forms (nested IF in test position, constant and NIL/T tests, three-argument selection, quoted literals, two-arm IF, depth-three nesting) produced 18 calls that agree with native evaluation under the registration executor; 19 refusals held (extra IF arm, duplicate and NIL/T/keyword parameters, quoted list, float, character, LET, nested LAMBDA, PROGN, declarations, trailing source, fixnums one past each bound). One source-legal form, `(if (if x nil t) 1 2)`, was refused at pass 2 because U1's front end rewrites it to NOT, which confirms the two-layer boundary rather than an escape. (4) Static: the generated architecture file and descriptions regenerate identically from the committed schemas; the generated code's TCR offsets match tcr.v1 (vsp 64, mv_count 116, mv_base 120); the entry shape matches the accepted candidate B record (self, nargs) → (value0, nvalues) with arguments at VSP + 4i; the platform discriminator uses the last free CPU and OS values without collision; the fatal reporter is the Stage 0 file unchanged since `39c297c0` and its fault offsets are checked against an independent WABT disassembly. Ledger, document and evidence checks pass; the Stage 1 gate recomputes to BLOCKED with 0 accepted, 28 missing, 3 unreviewed; Stage 0 remains 48 of 48.

Verdict: no defect. The three records claim what they executed and disclose their limits (native compiler image bound, no bootstrap heap, fatal arity guard, cross-loader that refuses, V8 stacks only, no browser claim).

Observations, not defects. The 24 native-comparison calls include a duplicated `constant` case, so 23 are distinct. The ABI schema bound under S1-LL22-b is written by the build runner; a later revision should bind the accepted `tests/wasm/stage0/abi-decision/decision.json` instead of restating it. Three post-adoption edits in the same commit are scope matters for the user rather than evidence matters: a CLAUDE.md bullet attributed to the user ("disposable clean U1 copies; retained under `tests/wasm/stage1`"), the adopted plan's 1A bullet narrowed from "cross-fasloader" to "cross-fasloader registration", and two 1A obligations in the ARM lessons (kernel-globals placement, constant index base) deferred to later subgates. Acceptance of S1-LL08-a, S1-LL22-b and S1-LL23-a is the user's decision.

## Sixty-third Claude audit — Stage 1A acceptance, integration and the LL04 representation packet, at b297a5e3 — 16 September 2026

Recorded by Claude directly. Reviewed three Codex commits: `0d4090b1` (acceptance of S1-LL08-a, S1-LL22-b and S1-LL23-a on the user's conditional "If you agree with Claude, I accept them"), `334baf6c` (integration of the reviewed 1A unit into shared source) and `b297a5e3` (S1-LL04-a generated cons access and mutation, packet `ccl-evidence/2026-09-16-stage1-representation-r1` at evidence commit `c4005f4`). Executed sources were replayed from a detached worktree at `b297a5e3`.

Acceptance. The acceptance record quotes the user's direction and provenance, binds the sixty-second audit commit and the R2 envelope hash, and adds only disposition and provenance to the three original rows. The three earlier scope observations are resolved by dated decisions rather than silently: the CLAUDE.md bullet now quotes the user's actual answer and marks the storage directory as Codex's choice; loader construction stays in 1E; globals accessors and constant indexing are prerequisites to their first use. Ledger recomputed to 3 accepted, 28 missing, 0 unreviewed at that commit.

Integration. All five integrated files (`compiler/WASM32/wasm32-arch.lisp`, `compiler/WASM32/wasm32-backend.lisp`, `xdump/xwasm32-fasload.lisp`, `lib/compile-ccl.lisp`, `lib/systems.lisp`) are byte-identical to the reviewed proposal unit in the R2 packet; the integration record binds the unit hash, the two U1 originals and the reversal procedure. The R6 evidence transfers unchanged because the bytes are the same ones both independent three-build runs exercised. Upstream `lisp-kernel` is untouched. Ledger, document and evidence checks pass at HEAD with the modified shared files in place.

LL04 replays. (1) The documented verifier, `tests/wasm/stage1/representation/verify.py`, passed: source pins and packet bytes checked, references hydrated from the accepted 1A packet, native qualification replayed, the corpus and all fifteen mutant compilers recompiled through CCL with byte-identical WAT, Wasm, native oracle and compiler FASL, the positive execution record reproduced and every mutant failing at the same first assertion. (2) The combined envelope carries the three accepted rows with locator relocation only, verified field by field, plus the LL04 row unchanged from its packet envelope. (3) An adversarial probe ran fourteen further forms through the fixture's own three-way oracle (Python model, independently compiled native CCL, target execution at four placements): self-referential and double writes, cons reads in IF test position, IF in operand position, three-deep CDR chains over cycles, writes of both fixnum extrema, a five-deep mixed read/write chain and operand-order variants. 153 cases and 128 raw-tag probes passed at all four placements. Literal non-list operands such as `(car 5)` and `(car t)` are refused, because U1's front end folds them at compile time into an error-signalling form that pass 2 rejects; this is a refusal at the documented boundary, not a mis-lowering.

Static. The lowering evaluates the pair and value operands once each, left to right, into distinct locals allocated before the nested operands, so nested temporaries cannot alias; the NIL test precedes the tag test because canonical NIL carries the cons fulltag; `cons.car` and `cons.cdr` displacements match the layout schema's CDR-first cells; the typed exception import is within the legacy exception-handling feature the engine matrix pins for all four engines. The packet binds the accepted candidate B decision directly, as the sixty-second audit asked.

Verdict: no defect in the three commits. Observations, not defects: the "straddling 2 GiB" placement straddles the boundary between two cells, since 4-aligned cells cannot themselves straddle it; and the grammar admits literal non-list operands that the pipeline then refuses, which the README could state. Acceptance of S1-LL04-a is the user's decision.

## Sixty-fourth Claude audit — LL04 acceptance and integration, and the LL07 typed-conversions packet, at 23681300 — 16 September 2026

Recorded by Claude directly. Reviewed three Codex commits: `999111a9` (acceptance of S1-LL04-a on the user's conditional "If you agree with Claude, I accept LL04-a. If it is fine, please proceed to LL07"), `1048491f` (integration of the accepted LL04 backend) and `23681300` (S1-LL07-a generated typed conversions, packet `ccl-evidence/2026-09-16-stage1-conversions-r1` at evidence commit `8a01ee8`). Executed sources were replayed from a detached worktree at `23681300`.

Acceptance and integration. The acceptance record binds the sixty-third audit commit and the R1 envelope hash and adds only disposition, review record and the acceptance packet's four files to the LL04 row; the three 1A rows are unchanged. The two observations from that audit are carried as exclusions in the decision and in the representation document. The integrated `compiler/WASM32/wasm32-backend.lisp` is byte-identical to the accepted proposal backend and to the fixture copy; the integration record binds the before and after hashes and the reversal. Ledger, document and evidence checks pass at HEAD.

LL07 design. The slice adds a second, explicitly separate entry kind: typed internal primitives with raw i32 parameters and one i32 result, outside the B root stack, reached as ordinary acode calls to a private symbol set. Pass 2 checks representation kinds on the real front-end IR: s32, u32, address and node may enter as parameters; code-id and slot may only be produced by the checked issue, validate-code and validate-slot operations. The inherited LL04 emitter is unchanged apart from the dispatch hook and one defvar, which I diffed directly. Every conversion evaluates its operands once into disjoint locals before any check, and every checked refusal precedes any store. The array limit is the exclusive 2^24 derived from U1's `array-total-size-limit` expression; header reads use the D1 displacement of -6 with a real bounds check against `memory.size`; effective-address and array-byte arithmetic are done in i64 and refused above 2^32-1.

Replays. (1) The documented verifier passed: source pins, packet bytes, references hydrated from the accepted 1A packet, native qualification replayed, the 17-module corpus and all twenty mutant compilers recompiled through CCL with byte-identical WAT, Wasm, native oracle, refusal list and compiler FASL, and every mutant failing at the same first assertion. (2) The combined envelope keeps the four accepted rows unchanged and carries the LL07 row unchanged from its packet envelope; the gate recomputes to 4 accepted, 26 missing, 1 unreviewed. (3) An adversarial probe compiled eleven composite forms through the fixture's own three-way oracle (Python model, independently compiled native mathematical reference, target execution at 32,769 pages): boxing followed by unboxing and address extraction at both fixnum bounds, header reads through tag and effective-address composition at low, 2 GiB and end-of-memory placements, code-ID validation of boxed inputs, two issuances feeding one array-size computation, and slot validation feeding array arithmetic. 69 cases agreed, 27 returns and 42 checked refusals, with registry writes and refusal-preserves-state confirmed; eight further static refusals held, including a slot handle as an entry parameter, an issued ID used as a node, an IF form and a cons operation. Two of my own candidate forms were rejected by the static checker for good reason: an issued code ID cannot feed a node-typed conversion, and a node produced by boxing may feed a header read, where the tag check refuses at run time.

Verdict: no defect in the three commits. Observations, not defects: the primitive language is reached through ordinary global-function calls to `%`-named symbols that exist natively only as the reference implementation, so integrating these primitives into real Lisp call lowering is correctly left to LL05 and the abi.json says so; and the slot registry's signature and role metadata are owner-supplied, so this slice validates handles against an honest owner and not against loader-authenticated metadata, which the coverage record states. Acceptance of S1-LL07-a is the user's decision.

## Sixty-fifth Claude audit — LL07 acceptance and integration, and the generated B call core, at 790318c8 — 16 September 2026

Recorded by Claude directly. Reviewed three Codex commits: `194b95f2` (acceptance of S1-LL07-a on the user's "I accept. Finish integration and proceed to the next item."), `67ad7b51` (integration of the accepted LL07 backend) and `790318c8` (the generated B call core, packet `ccl-evidence/2026-09-16-stage1-b-call-core-r1` at evidence commit `cef0be7`). Executed sources were replayed from a detached worktree at `790318c8`.

Acceptance and integration. The acceptance record binds the sixty-fourth audit commit and the R1 envelope hash and carries both of that audit's observations as exclusions; the four earlier accepted rows are unchanged. The integrated `compiler/WASM32/wasm32-backend.lisp` is byte-identical to the accepted proposal and to the fixture copy; the integration record binds before and after hashes and the reversal. Ledger, document and evidence checks pass at HEAD.

The B call core. This packet claims no inventory slot: the ledger is unchanged at 5 accepted and 26 missing, and the record says so. It is an auxiliary proposal, not an LL05 execution. The emitter implements the accepted candidate B protocol end to end for required arguments: the callee reads arguments at incoming VSP + 4i, checks arity, 16-byte alignment, that the argument extent lies below the caller-owned result region and that the region lies within the owned value stack, reserves a 272-byte frame whose 64 tagged slots form a root record linked at `root_head`, stages results in that record, checks the caller's capacity, copies the complete sequence to `mv_base`, sets `mv_count` and returns (value0, count), with (NIL, 0) for zero values. The caller saves the five TCR words, reserves an argument-plus-result frame, publishes a root record over self and the argument slots, points `vsp`, `mv_base` and `mv_owner_top` at it, calls directly through an imported entry or indirectly through the table after a host resolver that invokes the accepted LL07 slot validator, copies the results into its own staging record and restores every word. Exceptions unwind through `try_table` and `throw_ref` with restoration of VSP, result descriptor and root head, and `mv_count` reset to the caller's count. VALUES, PROGN, IF, PROG1 and MULTIPLE-VALUE-PROG1 stage retained values in rooted frames before any later form can reuse the output descriptor. The inherited LL07 emitter is unchanged apart from the mode hook and two defvars, which I diffed directly. The final exception-handling encoding the emitter uses is the one the engine matrix pinned on all four engines.

Replays. (1) The documented verifier passed: source pins, packet bytes, references hydrated from the accepted 1A packet, native qualification replayed, the 34-module corpus and all fifteen mutant compilers recompiled through CCL with byte-identical WAT, Wasm, native oracle, refusal list and compiler FASL, the positive record reproduced and every mutant failing at the same first assertion. (2) An adversarial probe added twenty forms through the fixture's own three-way oracle (Python model, independently compiled native CCL, target execution at low and above-2 GiB stacks with and without observation wrappers): an eight-deep chain of distinct functions each mutating a cons and passing through a one-value callee, VALUES over multiple- and zero-value calls, a 64-value MULTIPLE-VALUE-PROG1 whose later form produces 64 different values, PROGN passing many values through, a zero-value IF test, PROG1 of zero values, CAR of a VALUES form, RPLACA with a two-value call as the stored value, FUNCALL with nested-call arguments and inside VALUES, a MULTIPLE-VALUE-PROG1 whose middle form fails by designator, a 64-argument call whose every argument is a nested call, and IF whose branches return zero and 64 values. 132 comparisons agreed with 72 returns and 60 checked failures, 600 ownership inspections passed, and the root chain reached 17 frames. Native R6 and R6a were replayed through the qualifier on the retained registered build.

Verdict: no defect. Observations, not defects: function designators are opaque fixture handles resolved by a host import, so nothing about closure objects, symbol-function cells or self-recursion is exercised, and the fixture cannot express recursion at all because each module imports only earlier ones; the corpus reaches root-chain depth 4, and my probe depth 17, which is still far from the stack pressure LL05's tail and cleanup work will bring. Both limits are stated in the packet's scope record. Integration of this core before the remaining LL05 work is the user's call; it changes no accepted behaviour and adds a third entry mode to the shared backend.

## Sixty-sixth Claude audit — B call-core integration and the optional/keyword binding packet, at b0b8c080 — 16 September 2026

Recorded by Claude directly. Reviewed two Codex commits: `d6c38794` (acceptance and integration of the B call core on the user's "I accept. integrate and proceed") and `b0b8c080` (generated optional and keyword binding, packet `ccl-evidence/2026-09-16-stage1-b-bindings-r1` at evidence commit `bf8db0b`). Executed sources were replayed from a detached worktree at `b0b8c080`.

Integration. The integrated `compiler/WASM32/wasm32-backend.lisp` is byte-identical to the reviewed B call-core proposal and to the fixture copy; the integration record binds the user's words, the sixty-fifth audit, both hashes and the reversal, and states that no LL05 slot is accepted. Ledger, document and evidence checks pass at HEAD; the ledger is unchanged at 5 accepted and 26 missing.

The binding unit. It claims no inventory slot and is not integrated. The emitter consumes CCL's real lambda-list IR for `&optional`, `&key`, supplied-p variables, keyword aliases and `&allow-other-keys`. Bound values and presence flags occupy slots appended to the callee's published root record, so the record grows from 64 to 64 plus the bound count, and the frame reservation and root count follow; the inspector derives the expected count from the source lambda list independently of pass 2. Optional defaults run in order only for missing arguments and may read earlier parameters or call generated functions. The keyword scan checks an even tail, walks pairs once, binds each keyword on its first occurrence using the presence slot as the seen flag, treats `:allow-other-keys` with the same first-wins rule, refuses an unknown key unless the lambda list or the first `:allow-other-keys` value allows it, and evaluates key defaults afterwards in order. The argument-extent check now uses the dynamic count. The parts of the backend outside the B section are unchanged from the accepted LL07 and integrated call core, which I diffed directly; the changes inside the B section are the binding code, the keyword import and validator, and the widened arity and extent checks.

Replays. (1) The documented verifier passed: source pins, packet bytes, references hydrated from the accepted 1A packet, native qualification replayed, the 51-module corpus and all sixteen mutant compilers recompiled through CCL with byte-identical WAT, Wasm, native oracle, refusal list and compiler FASL, the positive record reproduced and every mutant failing at the same first assertion. (2) An adversarial probe added eleven forms through the fixture's own three-way oracle (Python model, independently compiled native CCL, target execution at low and above-2 GiB stacks with and without observation wrappers): defaults that read an earlier supplied-p flag for both optionals and keys, a keyword default that calls a keyword function, a keyword passed through from a bound variable, a failing default via a cons type error with heap effects, sixteen required parameters plus an optional, an alias and a plain keyword in one list, optionals followed by keys with `&allow-other-keys`, a MULTIPLE-VALUE-PROG1 inside a default, full 64-word keyword lists with the recognised key first, last and absent, and a caller passing cons reads as keyword values. 180 comparisons agreed, 140 returns and 40 checked failures, with 232 ownership inspections. One further probe case, a 66-argument keyword call, is refused by the emitter and accepted by native CCL; the 64-argument maximum is declared in the packet's scope record, so this is a disclosed limit rather than a defect, and it will need a decision when LL05 is completed.

Verdict: no defect. Observations, not defects: keyword identities are fixture-imported globals and only canonically spelled keyword names are admitted, both stated; the development record shows the spelling-collision finding was fixed with the failing former compiler retained as a regression witness, which is the right handling. Integration of this unit is the user's call.

## Sixty-seventh Claude audit — B binding integration and the rest-list/APPLY packet, at f5c69960 — 16 September 2026

Recorded by Claude directly. Reviewed two Codex commits: `7b4864fb` (acceptance and integration of the optional/keyword binding unit on the user's "accept and proceed") and `f5c69960` (generated rest lists and runtime-sized APPLY, packet `ccl-evidence/2026-09-16-stage1-b-rest-apply-r1` at evidence commit `c92a48a2`). Executed sources were replayed from a detached worktree at `f5c69960`.

Integration. The integrated `compiler/WASM32/wasm32-backend.lisp` is byte-identical to the binding unit reviewed in the sixty-sixth audit; the integration record's before hash equals the call-core record's after hash and its after hash equals the file at `7b4864fb`. The record binds the user's words, the sixty-sixth audit and the reversal, and states that no LL05 slot is accepted. Ledger, document and evidence checks pass at HEAD; the ledger is unchanged at 5 accepted and 26 missing. Codex left the STATUS row for the binding unit at its reviewed wording; I set it to accepted and integrated.

The rest/APPLY unit. It claims no inventory slot and is not integrated. Pass 2 now accepts a rest variable from CCL's lambda-list IR and a spread call from its call IR. Rest cells are 8-byte D1 conses bump-allocated in the TCR's owned allocation area: the emitter refuses a pointer below the area base or misaligned, an area limit beyond linear memory, or an i64 extent past the limit, then writes CDR and CAR of every cell from the last argument backwards before publishing the allocation pointer and the bound root; an empty rest list allocates nothing; nothing can call, poll or collect during construction. APPLY evaluates the designator, prefix operands and final list once in source order into a rooted record, walks the list with a tag and memory check on every CDR, a half-speed cursor for cycle detection and a stack-capacity check on every step, computes the frame extent in i64, checks it against the stack limit before any write, builds a second rooted record holding self and the spread arguments, dispatches through the checked table entry, and restores VSP, result ownership and the root chain on ordinary and exceptional return. The fixed 64-argument arity ceiling is gone: callees with keys or a rest variable accept any count the incoming-extent check admits, and that check now multiplies in i64. The parts of the backend outside the B section are unchanged from the integrated binding unit, which I diffed directly and the verifier also enforces.

Replays. (1) The documented verifier passed: source pins, packet bytes, references hydrated from the accepted 1A packet, native qualification replayed, the 71-module corpus and all eighteen mutant compilers recompiled through CCL with byte-identical WAT, Wasm, native oracle, refusal list and compiler FASL, the positive record reproduced and every mutant failing at the same first assertion. The evidence repository head equals the commit pinned in `repository.json`, and the development archive retains both recorded failures. (2) An adversarial probe added nine forms and 38 cases through the fixture's own three-way oracle: a rest variable read by a keyword default and returned by identity, mutation through the CDR of a rest list including the empty and one-cell cases, APPLY whose prefix operand truncates the list the tail then names, a sixteen-operand prefix before the spread list, a rest variable spread after a fixed operand, APPLY inside a keyword default with and without the key supplied, 129 required parameters followed by a rest variable at 128, 129 and 131 arguments, a rest list stored into the heap before a failing call, the same list applied twice with a mutation between, APPLY with T as the list, a keyword as the designator and a dotted tail, a 127- and 128-element spread into the 129-required function, and a 2,047-argument rest call. 152 comparisons agreed, 92 returns and 60 checked failures, with 2,292 ownership inspections and allocation counts and heap tails matching the model at every placement.

Verdict: no defect. Observations, not defects: the direct-link branch of the APPLY emitter is never reached, because the source walker refuses a quoted or function-quoted designator, so every corpus APPLY dispatches indirectly and that branch is untested code within this fixture; the spread copy re-reads the list validated by the walk without re-checking, which is sound because no call or safepoint separates them, and a concurrent mutator is undefined under native CCL too, but it should be re-examined when the threaded design fixes the allocation and safepoint rules; heap exhaustion is a checked refusal with no collector slow path, and the 64-value result limit remains, both stated in the scope record. Integration of this unit is the user's call.

## Sixty-eighth Claude audit — rest/APPLY integration and the runtime result-capacity packet, at 96c9bbb7 — 16 September 2026

Recorded by Claude directly. Reviewed two Codex commits: `c58f791e` (acceptance and integration of the rest/APPLY unit on the user's "integrate and proceed") and `96c9bbb7` (runtime-sized result storage and retention, packet `ccl-evidence/2026-09-16-stage1-b-results-r1` at evidence commit `46ccc302`). Executed sources were replayed from a detached worktree at `96c9bbb7`.

Integration. The integrated `compiler/WASM32/wasm32-backend.lisp` is byte-identical to the rest/APPLY unit reviewed in the sixty-seventh audit; the integration record's before hash equals the binding record's after hash and its after hash equals the file at `c58f791e`. The record binds the user's words, the sixty-seventh audit, the reversal and both of my observations (the unreached direct-link APPLY branch and the no-intervening-execution requirement between the two list traversals), and states that no LL05 slot is accepted. Ledger, document and evidence checks pass at HEAD; the ledger is unchanged at 5 accepted and 26 missing.

The result-capacity unit. It claims no inventory slot and is not integrated. The caller-owned output reservation, measured at entry as the distance from the multiple-value base to the owner top with a floor of four words, becomes the callee's result budget: the scratch region of that many words sits first in the published root record, bound variables follow it at a runtime offset, and the root count is capacity plus bound count, filled with NIL by a runtime loop before publication. Every child call and APPLY reserves the same budget for its own output region, so the budget propagates down the call tree unchanged. VALUES stages its operands in a static rooted frame and refuses when the static count exceeds the runtime capacity; MULTIPLE-VALUE-PROG1 retains the actual count in a runtime-sized rooted frame and PROG1 retains one word; the epilogue refuses when the count would overrun the real reservation. All extents are computed in i64 before narrowing and before any frame write. The fixed 64-value limit and the walker's 64-operand caps on VALUES, PROGN and the PROG1 forms are gone. The parts of the backend outside the B section are unchanged from the integrated rest/APPLY unit, which I diffed directly and the verifier also enforces.

Replays. (1) The documented verifier passed: source pins, packet bytes, references hydrated from the accepted 1A packet, native qualification replayed, the 95-module corpus and all sixteen mutant compilers recompiled through CCL with byte-identical WAT, Wasm, native oracle, refusal list and compiler FASL, the positive record reproduced and every mutant failing at the same first assertion. The evidence repository head equals the commit pinned in `repository.json`, and the development archive retains both escaping runs of the scratch-offset control together with the exact-capacity case that now rejects it. (2) An adversarial probe added eleven forms and 41 cases through the fixture's own three-way oracle, at capacities of 0, 4, 8, 132, 256, 512, 516 and 1,028 words: a rest list and keyword bound after a 130-value discarded call, an optional default and a rest list at an eight-word budget, MULTIPLE-VALUE-PROG1 retaining zero values across a 130-value form with a heap effect, MULTIPLE-VALUE-PROG1 and PROG1 filling a four-word budget exactly, scalar chains and discarded multiple values at an empty reservation, three levels of retained 130-value frames, IF branches with different counts at four words, keyword defaults returning 130 values with and without the key supplied, APPLY after a large discarded result, a four-argument direct call and APPLY at four words, a 512-value APPLY with a prefix, and 1,024 and 512 values through direct calls. 164 comparisons agreed, 148 returns and 16 checked failures, with 3,094 ownership inspections and a peak of ten root frames.

Verdict: no defect. Observations, not defects: the budget is a resource, so the stack cost of a call is the reservation times the depth, and at a 1,024-word budget an APPLY into a 1,024-value function does not fit the fixture's 32 KiB stack, which is why the corpus stops APPLY at 512 values; the packet says so and production adapters will need a reservation policy. The VALUES capacity check runs after all operand effects, so a refused VALUES has already performed them, which the packet also states. The minimum four-word scratch region lets a callee with an empty reservation evaluate scalars, confirmed by the probe. Integration of this unit is the user's call.
