# Generated B qualification: S1-LL05-a and S1-LL05-b

Executed and **ACCEPTED** 17 September 2026 after Claude’s eighty-fourth audit,
under [the user’s acceptance](acceptance-ll05.json). Exact reviewed integration follows.
The conditions R2 and result-scratch predecessors were accepted and integrated
at `97326319`; this proposal adds implicit call-error signalling and qualifies
the complete B call and installation corpus. Constants (S1-LL10-a) remain the
only unexecuted 1B slot. The [Stage 1 ledger](../evidence/current-stage1-gate-result.json)
is 7 accepted, 24 missing and 0 unreviewed.

The real CCL front end and Wasm pass 2 produce 587 inherited modules
with 7,332 native/model comparisons, 62 explicit-condition
modules with 848 comparisons, and 46 call-error
modules with 288 comparisons. All run eagerly and through
cold installation. The inherited corpus includes 48 chains of 100,000 transfers
inside a 2 KiB explicit stack, with zero compiled dispatches through public wrappers.

Arity and designator errors enter the generated Lisp handler search while the
failing dynamic context is live. Native-first cases distinguish ordinary
PROGRAM-ERROR, keyword SIMPLE-PROGRAM-ERROR, TYPE-ERROR and UNDEFINED-FUNCTION.
Keyword validation precedes parameter special bindings and rest allocation.
Handlers can decline, transfer values, signal another error or trigger cleanup;
a decline at the outer boundary retains the structured fatal code.

The implicit service roots a newly allocated private condition vector and its
result descriptor, then restores the interrupted context on transfer. This is
a private condition representation, not production CLOS construction or slots.
Restarts, debugger entry, other implicit errors and collector integration remain
later obligations. The single Worker trusts its owner-supplied catalog and symbol
registry. Host re-entry and cross-Worker publication are not claimed.

Qualification rejects 19 inherited compiler mutants, 10 dispatcher
mutants, 10 call-error mutants, and all 14 loader/stub mutants.
The 36 installation cases include real signature/role substitutions
and public-stub closure SELF and count preservation. Native R6/R6a proves 162 of
164 registered FASLs unchanged, the two explained module-registration changes,
164 of 164 restored, and 21,843 passing tests in both native suites.

`coverage.json` joins each inventory assertion to its executed evidence;
`abi-binding.json` pins the accepted B project decision directly. The retained
verifier recompiles the corpus and mutants and repeats the native qualification,
loader checks, cold installation, storage and source-scope controls.
See [the runner and replay instructions](../../../tests/wasm/stage1/b-call-errors/README.md).

Packet: `ccl-evidence/2026-09-17-stage1-ll05-r1`. Its development archive retains
failed attempts, including the bounded-result transfer defect, the escaped public
stub mutation and the outdated rest-allocation expectation. No timing claim is made.

The first publication attempt expected a local toolchain file rather than its
retained reference. The corrected publisher passed all 24 role-omission controls.
`post-publication-verification.json` confirms that this was the only source change
after the full replay and that all 5,142 replayed payloads remained unchanged; the
original publisher and failure are retained in `development-publication/`.

Acceptance carries the audit’s limits explicitly: APPLY with a non-list or dotted
final argument remains a checked type refusal, deferred to LL19; conditions are
private vectors with class masks; no-handler failure remains a checked exception
rather than a debugger boundary.
