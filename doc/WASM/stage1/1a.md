# Subgate 1A: registration, build identity and generated diagnostics

Executed and **accepted 16 September 2026**, following Claude’s no-defect
sixty-second audit at `3c2a38bc` and the user’s conditional acceptance, which
Codex endorses. The [decision](acceptance-1a.json) records the exact scope.
The exact reviewed unit is now [integrated](integration-1a.json). S1-LL08-a, S1-LL22-b and S1-LL23-a are registered in the adopted
[Stage 1 inventory](inventory.json). At 1A acceptance, Stage 1 was BLOCKED: 3 accepted,
28 missing, 0 unreviewed. Stage 0 remains PASS at 48 accepted.

## Delivered

The reversible proposal adds a schema-generated wasm32 architecture, a real
pass 2 for a declared leaf subset, and a cross-loader registration. It adds
three entries to `lib/systems.lisp` and a Wasm clause to each of the compiler
and cross-loader module selectors in `lib/compile-ccl.lisp`. All changes were
applied in disposable pristine U1 copies. After review and acceptance, the same five files were integrated byte for
byte; the two modified originals matched pristine U1 first. Upstream kernel
files remain untouched. The [integration record](integration-1a.json) binds
the reviewed unit and its existing R6/R6a evidence.

CCL's actual front end and registered pass 2 produce nine Wasm functions:
fixnum/NIL/T constants, argument references and nested IF. Twenty-four calls (23 distinct)
in one real Worker agree with native evaluation. Unsupported forms are
refused before front-end folding, and unsupported IR is refused again by
pass 2. The cross-loader refuses heap/image construction until Stage 1E;
its registration is not a bootstrap implementation.

## Qualification

| Check | Result |
| --- | --- |
| Native baseline and registered suites | 21,843 passed in each; 75 upstream-disabled disclosed |
| Native FASLs while registered | 162/164 identical; two intentional changes fully compared |
| After removing the complete proposal | All 164 FASLs identical to baseline; archived source restored |
| Intentionally changed compiler FASL | All 144 forms decoded; only the two module selectors change executable components; 154 source-note occurrences follow the exact insertions |
| Intentionally changed systems FASL | Three data entries plus count/length/source-span bytes; remaining bytes, including the 64-byte package thunk, identical |
| Existing selections and operator identities | Five architecture lists, seventeen target module profiles; 279 shared operator IDs/flags including twelve reserved slots, re-evaluated under all five architecture reader profiles |
| Generated execution | Nine rebuilt modules, 24 calls (23 distinct), ten unsupported-source/service refusals |
| Independent native-publication controls | 17 rejected |
| Build-binding and installed-slot controls | 37 rejected |
| Generated fatal diagnostics | Three actual Wasm faults joined to binary offsets; independently checked against WABT disassembly |
| Diagnostic bounds and mutants | First failure preserved; 25 failures retain three, count 22 drops, stay within 8 KiB; two unavailable-stack cases and eight rejected mutants |
| Fresh retained verifier | PASS, including native-image replays, identical generated binaries and diagnostic observations |

The registered rebuild loads the complete backend before compiling the
native corpus; a control rejects a missing loaded-backend witness. Dependency
preparation uses native XFASLOAD on both sides, from the same source state.

The native compiler image is bound as an input. **No Wasm bootstrap heap
exists yet.** The diagnostic entries use the B structural signature and
actual installed slots, but do not discharge the general B ABI, condition,
GC, closure or image obligations. In particular, the initial arity invariant
check is fatal; ordinary Lisp arity conditions remain S1-LL05 work. Foreign
platform execution and non-V8 stack formats are not claimed.

## Findings retained

A native TYPEP compiler macro folded 536,870,912 as a host fixnum before the
new pass 2 could reject it. The first compiler API now admits only the
explicit implemented source grammar before invoking the front end. TYPEP,
BIGNUM tests, EQL and arithmetic folds outside that grammar are tested
refusals. This does not implement those operations; integrating the qualified
target macro/helper environment is a prerequisite to widening the grammar.

Development also found missing NIL/T IR cases and a weak first-error mutant:
the last failure duplicated the first, hiding overwrites. The final flood
uses a different operation and rejects that mutant. Original attempts,
logs, sources and necessary binaries are retained by the development record.
The earlier R1 packet omitted loading the backend before the full rebuild
(its tests and snapshots did load it). It is superseded, unaccepted evidence.
The added rebuild check first exposed dependency-preparation differences;
those failed attempts remain retained. No generated symbol names or code
bytes are normalized to make the final comparison pass.

The [ARM survey follow-up](runtime-obligations.md) records runtime-global
ownership, binding growth, interrupt masking, recoverable stack exhaustion,
trap semantics and callback installation. Tags/NIL and constant-index limits
come from x8632; separate-code handling uses ARM as a reference. These are
implementation obligations, not new Stage 0 acceptance claims.

## Review and reproduction

Packet: `ccl-evidence/2026-09-16-stage1-1a-r2`.
The [registration recipe](../../../tests/wasm/stage1/registration/README.md)
and [build/diagnostic recipe](../../../tests/wasm/stage1/build/README.md)
contain the commands. The verifier is:

```sh
python3 tests/wasm/stage1/build/verify.py \
  --packet ../ccl-evidence/2026-09-16-stage1-1a-r2 \
  --evidence ../ccl-evidence
```

Claude’s independent three-build replay and additional adversarial probes
found no defect. Acceptance preserves all three original execution records
and their artifacts, adding only disposition and provenance. The production
acceptance gate reported 3 accepted, 28 missing and no unreviewed records.

The decision distinguishes the user’s actual workflow authorization from
Codex’s choice of storage directory. It accepts cross-loader registration
in 1A while retaining image construction in 1E, globals schema/accessors
before the first global access, and constant indexing before heap constants.
The runner-written leaf ABI schema remains the reviewed artifact; 1B must
add a direct binding to the accepted B decision before expanding that API.
These obligations are not waived by accepting the three slots.
