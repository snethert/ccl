{
  "name": "web-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node --test tests/browser.test.mjs"
  },
  "devDependencies": {
    "playwright": "^1.58.0"
  }
}
