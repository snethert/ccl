# Source SETF lookup witness

Read the U1 SETF macro, its registry helper and four inverse declarations in the
Wasm census context. Join the five actual restore-file lookups, then compare
nine callable/symbol/local-binding probes with the unchanged native macro.
Five native mutants and 25 checker controls reject. No complete census or
runtime implementation claim.

See [scope and commands](../../../../doc/WASM/stage0/target-setf.md).
