--- a/level-0/l0-def.lisp
+++ b/level-0/l0-def.lisp
@@ -137,7 +137,26 @@
 
 (defun %fhave (name def)
   (let* ((fname (validate-function-name name)))
-    (setf (%svref (symptr->symvector (%symbol->symptr fname)) target::symbol.fcell-cell) def)))
+    (let* ((census-old (%svref (symptr->symvector (%symbol->symptr fname)) target::symbol.fcell-cell))) (multiple-value-prog1 (setf (%svref (symptr->symvector (%symbol->symptr fname)) target::symbol.fcell-cell) def)
+(let* ((census-state (and (boundp '*boot-census-state*) (%sym-global-value '*boot-census-state*))))
+  (when (and census-state (%car census-state)
+             (eql (%current-tcr) (%car (%cdr (%cdr (%cdr census-state))))))
+    (let* ((census-bindings (%car (%cdr (%cdr (%cdr (%cdr (%cdr (%cdr census-state)))))))) (census-scan census-bindings))
+      (tagbody census-find-binding
+        (if (null census-scan)
+          (progn (%rplaca (%cdr (%cdr (%cdr (%cdr (%cdr (%cdr census-state)))))) (cons (cons fname census-old) census-bindings))
+                 (go census-binding-done)))
+        (if (eq fname (%car (%car census-scan))) (go census-binding-done))
+        (setq census-scan (%cdr census-scan))
+        (go census-find-binding)
+        census-binding-done))))
+(let* ((census-state (and (boundp '*boot-census-state*) (%sym-global-value '*boot-census-state*))))
+  (when (and census-state (%car census-state))
+    (if (eql (%current-tcr) (%car (%cdr (%cdr (%cdr census-state)))))
+      (let* ((census-sequence (1+ (%car (%cdr census-state)))))
+        (%rplaca (%cdr census-state) census-sequence)
+        (%rplaca (%cdr (%cdr census-state)) (cons (list census-sequence :binding-installed (list fname census-old def)) (%car (%cdr (%cdr census-state))))))
+      (%rplaca (%cdr (%cdr (%cdr (%cdr census-state)))) t))))))))
 
 ;;; FBOUNDP is true of any symbol whose function-cell contains something other
 ;;; than %unbound-function%; we expect FBOUNDP to return that something.
@@ -155,7 +174,26 @@
   (let* ((symvec (symptr->symvector (%symbol->symptr sym)))
          (old (%svref symvec target::symbol.fcell-cell))
          (unbound %unbound-function%))
-    (setf (%svref symvec target::symbol.fcell-cell) unbound)
+    (let* ((census-state (and (boundp '*boot-census-state*) (%sym-global-value '*boot-census-state*))))
+  (when (and census-state (%car census-state)
+             (eql (%current-tcr) (%car (%cdr (%cdr (%cdr census-state))))))
+    (let* ((census-bindings (%car (%cdr (%cdr (%cdr (%cdr (%cdr (%cdr census-state)))))))) (census-scan census-bindings))
+      (tagbody census-find-binding
+        (if (null census-scan)
+          (progn (%rplaca (%cdr (%cdr (%cdr (%cdr (%cdr (%cdr census-state)))))) (cons (cons sym old) census-bindings))
+                 (go census-binding-done)))
+        (if (eq sym (%car (%car census-scan))) (go census-binding-done))
+        (setq census-scan (%cdr census-scan))
+        (go census-find-binding)
+        census-binding-done))))
+(let* ((census-state (and (boundp '*boot-census-state*) (%sym-global-value '*boot-census-state*))))
+  (when (and census-state (%car census-state))
+    (if (eql (%current-tcr) (%car (%cdr (%cdr (%cdr census-state)))))
+      (let* ((census-sequence (1+ (%car (%cdr census-state)))))
+        (%rplaca (%cdr census-state) census-sequence)
+        (%rplaca (%cdr (%cdr census-state)) (cons (list census-sequence :binding-removing (list sym old unbound)) (%car (%cdr (%cdr census-state))))))
+      (%rplaca (%cdr (%cdr (%cdr (%cdr census-state)))) t))))
+(setf (%svref symvec target::symbol.fcell-cell) unbound)
     (not (eq old unbound))))
 
 ;;; It's guaranteed that lfun-bits is a fixnum.  Might be a 30-bit fixnum ...
--- a/level-0/nfasload.lisp
+++ b/level-0/nfasload.lisp
@@ -235,8 +235,25 @@
   #+debug
   (format t "~& dispatch: op = ~d at ~x" (logand op (lognot (ash 1 $fasl-epush-bit)))
           (1- (%fasl-get-file-pos s)))
-  (funcall (svref (faslstate.fasldispatch s) (logand op (lognot (ash 1 $fasl-epush-bit)))) 
-           s))
+  (let* ((census-reader (svref (faslstate.fasldispatch s)
+                           (logand op (lognot (ash 1 $fasl-epush-bit))))))
+    (if (= (logand op (lognot (ash 1 $fasl-epush-bit))) $fasl-lfuncall)
+      (progn (let* ((census-state (and (boundp '*boot-census-state*) (%sym-global-value '*boot-census-state*))))
+  (when (and census-state (%car census-state))
+    (if (eql (%current-tcr) (%car (%cdr (%cdr (%cdr census-state)))))
+      (let* ((census-sequence (1+ (%car (%cdr census-state)))))
+        (%rplaca (%cdr census-state) census-sequence)
+        (%rplaca (%cdr (%cdr census-state)) (cons (list census-sequence :reader-enter (list (faslstate.faslfname s) (1- (%fasl-get-file-pos s)) op census-reader (faslstate.fasldispatch s))) (%car (%cdr (%cdr census-state))))))
+      (%rplaca (%cdr (%cdr (%cdr (%cdr census-state)))) t))))
+        (multiple-value-prog1 (funcall census-reader s)
+          (let* ((census-state (and (boundp '*boot-census-state*) (%sym-global-value '*boot-census-state*))))
+  (when (and census-state (%car census-state))
+    (if (eql (%current-tcr) (%car (%cdr (%cdr (%cdr census-state)))))
+      (let* ((census-sequence (1+ (%car (%cdr census-state)))))
+        (%rplaca (%cdr census-state) census-sequence)
+        (%rplaca (%cdr (%cdr census-state)) (cons (list census-sequence :reader-return (list census-reader)) (%car (%cdr (%cdr census-state))))))
+      (%rplaca (%cdr (%cdr (%cdr (%cdr census-state)))) t))))))
+      (funcall census-reader s))))
 
 (defun %fasl-expr (s)
   (%fasl-dispatch s (%fasl-read-byte s))
@@ -455,7 +472,22 @@
 (deffaslop $fasl-lfuncall (s)
   (let* ((fun (%fasl-expr-preserve-epush s)))
     ;(break "fun = ~s" fun)
-     (%epushval s (funcall fun))))
+     (progn (let* ((census-state (and (boundp '*boot-census-state*) (%sym-global-value '*boot-census-state*))))
+  (when (and census-state (%car census-state))
+    (if (eql (%current-tcr) (%car (%cdr (%cdr (%cdr census-state)))))
+      (let* ((census-sequence (1+ (%car (%cdr census-state)))))
+        (%rplaca (%cdr census-state) census-sequence)
+        (%rplaca (%cdr (%cdr census-state)) (cons (list census-sequence :call-enter (list fun)) (%car (%cdr (%cdr census-state))))))
+      (%rplaca (%cdr (%cdr (%cdr (%cdr census-state)))) t))))
+       (let* ((census-value (funcall fun)))
+         (let* ((census-state (and (boundp '*boot-census-state*) (%sym-global-value '*boot-census-state*))))
+  (when (and census-state (%car census-state))
+    (if (eql (%current-tcr) (%car (%cdr (%cdr (%cdr census-state)))))
+      (let* ((census-sequence (1+ (%car (%cdr census-state)))))
+        (%rplaca (%cdr census-state) census-sequence)
+        (%rplaca (%cdr (%cdr census-state)) (cons (list census-sequence :call-return (list fun)) (%car (%cdr (%cdr census-state))))))
+      (%rplaca (%cdr (%cdr (%cdr (%cdr census-state)))) t))))
+         (%epushval s census-value)))))
 
 (deffaslop $fasl-globals (s)
   (setf (faslstate.faslgsymbols s) (%fasl-expr s)))
@@ -944,6 +976,14 @@
 (%fhave 'target-fasl-max-version #'bootstrapping-fasl-max-version)
 
 (defun %fasload (string &optional (table *fasl-dispatch-table*))
+(let* ((census-state (and (boundp '*boot-census-state*) (%sym-global-value '*boot-census-state*))))
+  (when (and census-state (%car census-state))
+    (if (eql (%current-tcr) (%car (%cdr (%cdr (%cdr census-state)))))
+      (let* ((census-sequence (1+ (%car (%cdr census-state)))))
+        (%rplaca (%cdr census-state) census-sequence)
+        (%rplaca (%cdr (%cdr census-state)) (cons (list census-sequence :load-enter (list string table)) (%car (%cdr (%cdr census-state))))))
+      (%rplaca (%cdr (%cdr (%cdr (%cdr census-state)))) t))))
+(multiple-value-prog1 (progn
   ;;(dbg string) 
   (when (and *%fasload-verbose*
 	     (not *load-verbose*))
@@ -1015,6 +1055,13 @@
                   (bug msg)))
               (values nil err))
             (values t nil)))))))
+(let* ((census-state (and (boundp '*boot-census-state*) (%sym-global-value '*boot-census-state*))))
+  (when (and census-state (%car census-state))
+    (if (eql (%current-tcr) (%car (%cdr (%cdr (%cdr census-state)))))
+      (let* ((census-sequence (1+ (%car (%cdr census-state)))))
+        (%rplaca (%cdr census-state) census-sequence)
+        (%rplaca (%cdr (%cdr census-state)) (cons (list census-sequence :load-return (list string table)) (%car (%cdr (%cdr census-state))))))
+      (%rplaca (%cdr (%cdr (%cdr (%cdr census-state)))) t))))))
 
 
 (defun %new-package-hashtable (size)
@@ -1213,8 +1260,31 @@
       ;; Need to make %ALL-PACKAGES-LOCK% early, so that we can casually
       ;; do SET-PACKAGE in cold load functions.
       (setq %all-packages-lock% (make-read-write-lock))
+      (%set-sym-global-value '*boot-census-state*
+        (list t 0 nil (%current-tcr) nil *xload-cold-load-functions* nil))
+      (let* ((census-state (and (boundp '*boot-census-state*) (%sym-global-value '*boot-census-state*))))
+  (when (and census-state (%car census-state))
+    (if (eql (%current-tcr) (%car (%cdr (%cdr (%cdr census-state)))))
+      (let* ((census-sequence (1+ (%car (%cdr census-state)))))
+        (%rplaca (%cdr census-state) census-sequence)
+        (%rplaca (%cdr (%cdr census-state)) (cons (list census-sequence :boot-start (list *xload-cold-load-functions* *xload-startup-file*)) (%car (%cdr (%cdr census-state))))))
+      (%rplaca (%cdr (%cdr (%cdr (%cdr census-state)))) t))))
       (dolist (f (prog1 *xload-cold-load-functions* (setq *xload-cold-load-functions* nil)))
-        (funcall f))
+        (let* ((census-state (and (boundp '*boot-census-state*) (%sym-global-value '*boot-census-state*))))
+  (when (and census-state (%car census-state))
+    (if (eql (%current-tcr) (%car (%cdr (%cdr (%cdr census-state)))))
+      (let* ((census-sequence (1+ (%car (%cdr census-state)))))
+        (%rplaca (%cdr census-state) census-sequence)
+        (%rplaca (%cdr (%cdr census-state)) (cons (list census-sequence :cold-enter (list f)) (%car (%cdr (%cdr census-state))))))
+      (%rplaca (%cdr (%cdr (%cdr (%cdr census-state)))) t))))
+        (funcall f)
+        (let* ((census-state (and (boundp '*boot-census-state*) (%sym-global-value '*boot-census-state*))))
+  (when (and census-state (%car census-state))
+    (if (eql (%current-tcr) (%car (%cdr (%cdr (%cdr census-state)))))
+      (let* ((census-sequence (1+ (%car (%cdr census-state)))))
+        (%rplaca (%cdr census-state) census-sequence)
+        (%rplaca (%cdr (%cdr census-state)) (cons (list census-sequence :cold-return (list f)) (%car (%cdr (%cdr census-state))))))
+      (%rplaca (%cdr (%cdr (%cdr (%cdr census-state)))) t)))))
       (dolist (pair (prog1 *early-class-cells* (setq *early-class-cells* nil)))
         (setf (gethash (car pair) %find-classes%) (cdr pair)))
       (dolist (p %all-packages%)
--- a/level-1/level-1.lisp
+++ b/level-1/level-1.lisp
@@ -109,4 +109,13 @@
                            *loading-toplevel-location* nil)
                      (toplevel-loop)))
   (set-user-environment t)
-  (toplevel))
+  (let* ((census-handoff-state (and (boundp '*boot-census-state*) (%sym-global-value '*boot-census-state*))))
+    (let* ((census-state (and (boundp '*boot-census-state*) (%sym-global-value '*boot-census-state*))))
+  (when (and census-state (%car census-state))
+    (if (eql (%current-tcr) (%car (%cdr (%cdr (%cdr census-state)))))
+      (let* ((census-sequence (1+ (%car (%cdr census-state)))))
+        (%rplaca (%cdr census-state) census-sequence)
+        (%rplaca (%cdr (%cdr census-state)) (cons (list census-sequence :handoff nil) (%car (%cdr (%cdr census-state))))))
+      (%rplaca (%cdr (%cdr (%cdr (%cdr census-state)))) t))))
+    (when census-handoff-state (%rplaca census-handoff-state nil))
+    (toplevel)))
